// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 05544e8e8ff3

import Foundation
import Testing
import ArchAstroPlatform

@Suite(.serialized, .enabled(if: ContractSupport.channelTestsEnabled)) struct ApiChatChannelContractTests {

    @Test func api_chat_channel_join_team_thread_joins_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { _, socket in
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            #expect(channel.joinResponse != nil)
        }
    }

    @Test func api_chat_channel_join_team_thread_surfaces_server_error_reply_as_channel_error() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "replyError", "payload": ["reason": "test_error"]]],
            ])
            await #expect(throws: ChannelError.self) {
                _ = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            }
        }
    }

    @Test func api_chat_channel_join_team_keyed_joins_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { _, socket in
            let channel = try await ApiChatChannel.joinTeamKeyed(socket: socket, teamId: "test-id", key: "test-key", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            #expect(channel.joinResponse != nil)
        }
    }

    @Test func api_chat_channel_join_team_keyed_surfaces_server_error_reply_as_channel_error() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:key:test-key",
                "onJoin": [["type": "replyError", "payload": ["reason": "test_error"]]],
            ])
            await #expect(throws: ChannelError.self) {
                _ = try await ApiChatChannel.joinTeamKeyed(socket: socket, teamId: "test-id", key: "test-key", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            }
        }
    }

    @Test func api_chat_channel_join_team_transient_joins_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { _, socket in
            let channel = try await ApiChatChannel.joinTeamTransient(socket: socket, teamId: "test-id", key: "test-key", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            #expect(channel.joinResponse != nil)
        }
    }

    @Test func api_chat_channel_join_team_transient_surfaces_server_error_reply_as_channel_error() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:transient:test-key",
                "onJoin": [["type": "replyError", "payload": ["reason": "test_error"]]],
            ])
            await #expect(throws: ChannelError.self) {
                _ = try await ApiChatChannel.joinTeamTransient(socket: socket, teamId: "test-id", key: "test-key", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            }
        }
    }

    @Test func api_chat_channel_join_user_thread_joins_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { _, socket in
            let channel = try await ApiChatChannel.joinUserThread(socket: socket, threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1, localToolProviderId: "test-id", localTools: [["function": ["description": "test description", "name": "test-name", "parameters": [:]], "type": "function"]])
            #expect(channel.joinResponse != nil)
        }
    }

    @Test func api_chat_channel_join_user_thread_surfaces_server_error_reply_as_channel_error() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:user:thread:test-id",
                "onJoin": [["type": "replyError", "payload": ["reason": "test_error"]]],
            ])
            await #expect(throws: ChannelError.self) {
                _ = try await ApiChatChannel.joinUserThread(socket: socket, threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1, localToolProviderId: "test-id", localTools: [["function": ["description": "test description", "name": "test-name", "parameters": [:]], "type": "function"]])
            }
        }
    }

    @Test func api_chat_channel_join_user_keyed_joins_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { _, socket in
            let channel = try await ApiChatChannel.joinUserKeyed(socket: socket, key: "test-key", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            #expect(channel.joinResponse != nil)
        }
    }

    @Test func api_chat_channel_join_user_keyed_surfaces_server_error_reply_as_channel_error() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:user:key:test-key",
                "onJoin": [["type": "replyError", "payload": ["reason": "test_error"]]],
            ])
            await #expect(throws: ChannelError.self) {
                _ = try await ApiChatChannel.joinUserKeyed(socket: socket, key: "test-key", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            }
        }
    }

    @Test func api_chat_channel_join_user_transient_joins_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { _, socket in
            let channel = try await ApiChatChannel.joinUserTransient(socket: socket, key: "test-key", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            #expect(channel.joinResponse != nil)
        }
    }

    @Test func api_chat_channel_join_user_transient_surfaces_server_error_reply_as_channel_error() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:user:transient:test-key",
                "onJoin": [["type": "replyError", "payload": ["reason": "test_error"]]],
            ])
            await #expect(throws: ChannelError.self) {
                _ = try await ApiChatChannel.joinUserTransient(socket: socket, key: "test-key", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            }
        }
    }

    @Test func api_chat_channel_api_chat_fork_thread_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:fork_thread": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatForkThread(payload: ApiChatForkThreadInput(messageId: "test-id", title: "test-value"))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:fork_thread")
            #expect(observed.count == 1)
            #expect(observed.first?["params"]?["message_id"] == "test-id")
        }
    }

    @Test func api_chat_channel_api_chat_fork_thread_returns_error_envelope_when_required_missing() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            // Raw push bypasses the typed payload struct.
            let reply = try await channel.channel.push("api:chat:fork_thread", payload: [:] as [String: JSONValue])
            #expect(reply.status == "error")
        }
    }

    @Test func api_chat_channel_api_chat_mark_thread_read_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:mark_thread_read": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatMarkThreadRead(payload: ApiChatMarkThreadReadInput(messageId: "test-id"))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:mark_thread_read")
            #expect(observed.count == 1)
            #expect(observed.first?["params"]?["message_id"] == "test-id")
        }
    }

    @Test func api_chat_channel_api_chat_mark_thread_read_returns_error_envelope_when_required_missing() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            // Raw push bypasses the typed payload struct.
            let reply = try await channel.channel.push("api:chat:mark_thread_read", payload: [:] as [String: JSONValue])
            #expect(reply.status == "error")
        }
    }

    @Test func api_chat_channel_api_chat_list_messages_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:list_messages": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatListMessages(payload: [:])
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:list_messages")
            #expect(observed.count == 1)
            #expect(observed.first?["params"] != nil)
        }
    }

    @Test func api_chat_channel_api_chat_load_more_messages_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:load_more_messages": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatLoadMoreMessages(payload: ApiChatLoadMoreMessagesInput(afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:load_more_messages")
            #expect(observed.count == 1)
            #expect(observed.first?["params"] != nil)
        }
    }

    @Test func api_chat_channel_api_chat_post_message_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:post_message": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatPostMessage(payload: ApiChatPostMessageInput(content: "test content", idempotencyKey: "test-key", replyTo: "test-value", uploads: [[:]]))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:post_message")
            #expect(observed.count == 1)
            #expect(observed.first?["params"]?["content"] == "test content")
        }
    }

    @Test func api_chat_channel_api_chat_post_message_returns_error_envelope_when_required_missing() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            // Raw push bypasses the typed payload struct.
            let reply = try await channel.channel.push("api:chat:post_message", payload: [:] as [String: JSONValue])
            #expect(reply.status == "error")
        }
    }

    @Test func api_chat_channel_api_chat_post_simple_message_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:post_simple_message": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatPostSimpleMessage(payload: ApiChatPostSimpleMessageInput(content: "test content", idempotencyKey: "test-key", replyTo: "test-value"))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:post_simple_message")
            #expect(observed.count == 1)
            #expect(observed.first?["params"] != nil)
        }
    }

    @Test func api_chat_channel_api_chat_edit_message_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:edit_message": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatEditMessage(payload: ApiChatEditMessageInput(content: "test content", messageId: "test-id"))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:edit_message")
            #expect(observed.count == 1)
            #expect(observed.first?["params"]?["content"] == "test content")
            #expect(observed.first?["params"]?["message_id"] == "test-id")
        }
    }

    @Test func api_chat_channel_api_chat_edit_message_returns_error_envelope_when_required_missing() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            // Raw push bypasses the typed payload struct.
            let reply = try await channel.channel.push("api:chat:edit_message", payload: [:] as [String: JSONValue])
            #expect(reply.status == "error")
        }
    }

    @Test func api_chat_channel_api_chat_delete_message_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:delete_message": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatDeleteMessage(payload: ApiChatDeleteMessageInput(messageId: "test-id"))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:delete_message")
            #expect(observed.count == 1)
            #expect(observed.first?["params"]?["message_id"] == "test-id")
        }
    }

    @Test func api_chat_channel_api_chat_delete_message_returns_error_envelope_when_required_missing() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            // Raw push bypasses the typed payload struct.
            let reply = try await channel.channel.push("api:chat:delete_message", payload: [:] as [String: JSONValue])
            #expect(reply.status == "error")
        }
    }

    @Test func api_chat_channel_api_chat_add_reaction_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:add_reaction": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatAddReaction(payload: ApiChatAddReactionInput(emoji: "test-value", messageId: "test-id"))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:add_reaction")
            #expect(observed.count == 1)
            #expect(observed.first?["params"]?["emoji"] == "test-value")
            #expect(observed.first?["params"]?["message_id"] == "test-id")
        }
    }

    @Test func api_chat_channel_api_chat_add_reaction_returns_error_envelope_when_required_missing() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            // Raw push bypasses the typed payload struct.
            let reply = try await channel.channel.push("api:chat:add_reaction", payload: [:] as [String: JSONValue])
            #expect(reply.status == "error")
        }
    }

    @Test func api_chat_channel_api_chat_remove_reaction_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:remove_reaction": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatRemoveReaction(payload: ApiChatRemoveReactionInput(emoji: "test-value", messageId: "test-id"))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:remove_reaction")
            #expect(observed.count == 1)
            #expect(observed.first?["params"]?["emoji"] == "test-value")
            #expect(observed.first?["params"]?["message_id"] == "test-id")
        }
    }

    @Test func api_chat_channel_api_chat_remove_reaction_returns_error_envelope_when_required_missing() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            // Raw push bypasses the typed payload struct.
            let reply = try await channel.channel.push("api:chat:remove_reaction", payload: [:] as [String: JSONValue])
            #expect(reply.status == "error")
        }
    }

    @Test func api_chat_channel_api_chat_typing_sends_valid_push_and_receives_contract_valid_reply() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
                "onMessage": ["api:chat:typing": [["type": "autoReply"]]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let reply = try await channel.apiChatTyping(payload: ApiChatTypingInput(isTyping: true))
            #expect(reply.status == "ok")

            let observed = try await harness.observations(topic: "api:chat:team:test-id:thread:test-id", event: "api:chat:typing")
            #expect(observed.count == 1)
            #expect(observed.first?["params"]?["is_typing"] == true)
        }
    }

    @Test func api_chat_channel_api_chat_typing_returns_error_envelope_when_required_missing() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            // Raw push bypasses the typed payload struct.
            let reply = try await channel.channel.push("api:chat:typing", payload: [:] as [String: JSONValue])
            #expect(reply.status == "error")
        }
    }

    @Test func api_chat_channel_on_message_added_delivers_contract_valid_payloads() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [
                    ["type": "autoReply"],
                    ["type": "autoPush", "event": "message_added"],
                ],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let payload = try await ContractSupport.awaitFirst { yield in
                channel.onMessageAdded { payload in yield(payload) }
            }
            ContractSupport.use(payload)
        }
    }

    @Test func api_chat_channel_on_message_updated_delivers_contract_valid_payloads() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [
                    ["type": "autoReply"],
                    ["type": "autoPush", "event": "message_updated"],
                ],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let payload = try await ContractSupport.awaitFirst { yield in
                channel.onMessageUpdated { payload in yield(payload) }
            }
            ContractSupport.use(payload)
        }
    }

    @Test func api_chat_channel_on_thread_event_delivers_contract_valid_payloads() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [
                    ["type": "autoReply"],
                    ["type": "autoPush", "event": "thread_event"],
                ],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let payload = try await ContractSupport.awaitFirst { yield in
                channel.onThreadEvent { payload in yield(payload) }
            }
            ContractSupport.use(payload)
        }
    }

    @Test func api_chat_channel_on_system_event_delivers_contract_valid_payloads() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [
                    ["type": "autoReply"],
                    ["type": "autoPush", "event": "system_event"],
                ],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let payload = try await ContractSupport.awaitFirst { yield in
                channel.onSystemEvent { payload in yield(payload) }
            }
            ContractSupport.use(payload)
        }
    }

    @Test func api_chat_channel_on_typing_delivers_contract_valid_payloads() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [
                    ["type": "autoReply"],
                    ["type": "autoPush", "event": "typing"],
                ],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            let payload = try await ContractSupport.awaitFirst { yield in
                channel.onTyping { payload in yield(payload) }
            }
            ContractSupport.use(payload)
        }
    }

    @Test func api_chat_channel_leave_leaves_cleanly_through_generated_leave() async throws {
        try await ContractSupport.withHarnessSocket { harness, socket in
            try await harness.registerScenario([
                "topic": "api:chat:team:test-id:thread:test-id",
                "onJoin": [["type": "autoReply"]],
            ])
            let channel = try await ApiChatChannel.joinTeamThread(socket: socket, teamId: "test-id", threadId: "test-id", afterCursor: "test-value", beforeCursor: "test-value", includeMetadata: true, limit: 1)
            try await channel.leave()
        }
    }
}
