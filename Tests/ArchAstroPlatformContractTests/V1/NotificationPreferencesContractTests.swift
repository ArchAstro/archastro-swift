// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 1ac324b45de8

import Foundation
import Testing
import ArchAstroPlatform

@Suite(.serialized) struct NotificationPreferencesContractTests {

    @Test func notification_preferences_remove_success() async throws {
        let client = try await ContractSupport.client()
        try await client.v1.notificationPreferences.remove()
    }

    @Test func notification_preferences_remove_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            try await client.v1.notificationPreferences.remove()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func notification_preferences_remove_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            try await client.v1.notificationPreferences.remove()
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func notification_preferences_remove_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            try await client.v1.notificationPreferences.remove()
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func notification_preferences_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.notificationPreferences.list()
        ContractSupport.use(result)
    }

    @Test func notification_preferences_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.notificationPreferences.list()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func notification_preferences_list_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.notificationPreferences.list()
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func notification_preferences_replace_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.notificationPreferences.replace(input: NotificationPreferenceReplaceInput(channel: "test-value", enabled: true, type: "test"))
        ContractSupport.use(result)
    }

    @Test func notification_preferences_replace_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.notificationPreferences.replace(input: NotificationPreferenceReplaceInput(channel: "test-value", enabled: true, type: "test"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func notification_preferences_replace_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.notificationPreferences.replace(input: NotificationPreferenceReplaceInput(channel: "test-value", enabled: true, type: "test"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func notification_preferences_replace_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.notificationPreferences.replace(input: NotificationPreferenceReplaceInput(channel: "test-value", enabled: true, type: "test"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }
}
