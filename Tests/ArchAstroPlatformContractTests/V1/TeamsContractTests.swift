// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: d663dc041ae4

import Foundation
import Testing
import ArchAstroPlatform

@Suite(.serialized) struct TeamsContractTests {

    @Test func teams_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.list()
        ContractSupport.use(result.data)
        ContractSupport.use(result)
    }

    @Test func teams_list_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.teams.list()
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func teams_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.list()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_list_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.teams.list()
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_create_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.create(input: TeamCreateInput(name: "test-name"))
        ContractSupport.use(result)
    }

    @Test func teams_create_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.create(input: TeamCreateInput(name: "test-name"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_create_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.teams.create(input: TeamCreateInput(name: "test-name"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_create_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.teams.create(input: TeamCreateInput(name: "test-name"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func teams_join_by_code_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.joinByCode(input: TeamJoinByCodeInput())
        ContractSupport.use(result)
    }

    @Test func teams_join_by_code_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.teams.joinByCode(input: TeamJoinByCodeInput())
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func teams_join_by_code_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.joinByCode(input: TeamJoinByCodeInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_join_by_code_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.joinByCode(input: TeamJoinByCodeInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_join_by_code_error_429() async throws {
        let client = try await ContractSupport.errorClient(429)
        do {
            _ = try await client.v1.teams.joinByCode(input: TeamJoinByCodeInput())
            Issue.record("Expected ApiError with status 429")
        }
        catch let error as ApiError {
            #expect(error.status == 429)
        }
    }

    @Test func teams_delete_success() async throws {
        let client = try await ContractSupport.client()
        try await client.v1.teams.delete(team: "test-value")
    }

    @Test func teams_delete_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            try await client.v1.teams.delete(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_delete_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            try await client.v1.teams.delete(team: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_delete_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            try await client.v1.teams.delete(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_get_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.get(team: "test-value")
        ContractSupport.use(result)
    }

    @Test func teams_get_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.get(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_get_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.teams.get(team: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_get_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.get(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_update_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.update(team: "test-value", input: TeamUpdateInput())
        ContractSupport.use(result)
    }

    @Test func teams_update_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.update(team: "test-value", input: TeamUpdateInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_update_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.teams.update(team: "test-value", input: TeamUpdateInput())
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_update_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.update(team: "test-value", input: TeamUpdateInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_update_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.teams.update(team: "test-value", input: TeamUpdateInput())
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func teams_artifacts_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.artifacts(team: "test-value")
        ContractSupport.use(result.data)
        ContractSupport.use(result)
    }

    @Test func teams_artifacts_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.artifacts(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_artifacts_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.artifacts(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_invite_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.invite(team: "test-value")
        ContractSupport.use(result)
    }

    @Test func teams_invite_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.invite(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_invite_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.teams.invite(team: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_invite_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.invite(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_invites_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.invites(team: "test-value")
        ContractSupport.use(result)
    }

    @Test func teams_invites_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.invites(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_invites_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.invites(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_join_success() async throws {
        let client = try await ContractSupport.client()
        try await client.v1.teams.join(team: "test-value", input: TeamJoinInput())
    }

    @Test func teams_join_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            try await client.v1.teams.join(team: "test-value", input: TeamJoinInput())
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func teams_join_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            try await client.v1.teams.join(team: "test-value", input: TeamJoinInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_join_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            try await client.v1.teams.join(team: "test-value", input: TeamJoinInput())
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_join_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            try await client.v1.teams.join(team: "test-value", input: TeamJoinInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_leave_success() async throws {
        let client = try await ContractSupport.client()
        try await client.v1.teams.leave(team: "test-value")
    }

    @Test func teams_leave_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            try await client.v1.teams.leave(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_leave_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            try await client.v1.teams.leave(team: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_leave_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            try await client.v1.teams.leave(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_leave_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            try await client.v1.teams.leave(team: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func teams_custom_objects_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.customObjects.list(team: "test-value", type: "test")
        ContractSupport.use(result.data)
        ContractSupport.use(result)
    }

    @Test func teams_custom_objects_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.customObjects.list(team: "test-value", type: "test")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_custom_objects_list_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.customObjects.list(team: "test-value", type: "test")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_custom_objects_create_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.customObjects.create(team: "test-value", input: TeamCustomObjectCreateInput(fields: [:], type: "test"))
        ContractSupport.use(result)
    }

    @Test func teams_custom_objects_create_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.customObjects.create(team: "test-value", input: TeamCustomObjectCreateInput(fields: [:], type: "test"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_custom_objects_create_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.customObjects.create(team: "test-value", input: TeamCustomObjectCreateInput(fields: [:], type: "test"))
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_custom_objects_create_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.teams.customObjects.create(team: "test-value", input: TeamCustomObjectCreateInput(fields: [:], type: "test"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func teams_members_remove_success() async throws {
        let client = try await ContractSupport.client()
        try await client.v1.teams.members.remove(team: "test-value")
    }

    @Test func teams_members_remove_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            try await client.v1.teams.members.remove(team: "test-value")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func teams_members_remove_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            try await client.v1.teams.members.remove(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_members_remove_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            try await client.v1.teams.members.remove(team: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_members_remove_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            try await client.v1.teams.members.remove(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_members_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.members.list(team: "test-value")
        ContractSupport.use(result.data)
        ContractSupport.use(result)
    }

    @Test func teams_members_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.members.list(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_members_list_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.members.list(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_members_create_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.members.create(team: "test-value", input: MemberCreateInput())
        ContractSupport.use(result)
    }

    @Test func teams_members_create_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.members.create(team: "test-value", input: MemberCreateInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_members_create_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.members.create(team: "test-value", input: MemberCreateInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_members_create_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.teams.members.create(team: "test-value", input: MemberCreateInput())
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func teams_members_update_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.members.update(team: "test-value", user: "test-value", input: MemberUpdateInput(role: "user"))
        ContractSupport.use(result)
    }

    @Test func teams_members_update_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.members.update(team: "test-value", user: "test-value", input: MemberUpdateInput(role: "user"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_members_update_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.teams.members.update(team: "test-value", user: "test-value", input: MemberUpdateInput(role: "user"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func teams_members_update_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.members.update(team: "test-value", user: "test-value", input: MemberUpdateInput(role: "user"))
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_members_update_error_409() async throws {
        let client = try await ContractSupport.errorClient(409)
        do {
            _ = try await client.v1.teams.members.update(team: "test-value", user: "test-value", input: MemberUpdateInput(role: "user"))
            Issue.record("Expected ApiError with status 409")
        }
        catch let error as ApiError {
            #expect(error.status == 409)
        }
    }

    @Test func teams_members_update_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.teams.members.update(team: "test-value", user: "test-value", input: MemberUpdateInput(role: "user"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func teams_threads_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.threads.list(team: "test-value")
        ContractSupport.use(result.data)
        ContractSupport.use(result)
    }

    @Test func teams_threads_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.threads.list(team: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_threads_list_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.threads.list(team: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_threads_create_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.teams.threads.create(team: "test-value", input: TeamThreadCreateInput(thread: TeamThreadCreateInputThread()))
        ContractSupport.use(result)
    }

    @Test func teams_threads_create_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.teams.threads.create(team: "test-value", input: TeamThreadCreateInput(thread: TeamThreadCreateInputThread()))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func teams_threads_create_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.teams.threads.create(team: "test-value", input: TeamThreadCreateInput(thread: TeamThreadCreateInputThread()))
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func teams_threads_create_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.teams.threads.create(team: "test-value", input: TeamThreadCreateInput(thread: TeamThreadCreateInputThread()))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }
}
