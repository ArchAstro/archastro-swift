// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: a3e1247c89c3

import Foundation
import Testing
import ArchAstroPlatform

@Suite(.serialized) struct ThreadMessagesContractTests {

    @Test func thread_messages_delete_success() async throws {
        let client = try await ContractSupport.client()
        try await client.v1.threadMessages.delete(message: "test-value")
    }

    @Test func thread_messages_delete_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            try await client.v1.threadMessages.delete(message: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func thread_messages_delete_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            try await client.v1.threadMessages.delete(message: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func thread_messages_delete_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            try await client.v1.threadMessages.delete(message: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func thread_messages_delete_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            try await client.v1.threadMessages.delete(message: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func thread_messages_replace_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.threadMessages.replace(message: "test-value", input: ThreadMessageReplaceInput())
        ContractSupport.use(result)
    }

    @Test func thread_messages_replace_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.threadMessages.replace(message: "test-value", input: ThreadMessageReplaceInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func thread_messages_replace_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.threadMessages.replace(message: "test-value", input: ThreadMessageReplaceInput())
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func thread_messages_replace_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.threadMessages.replace(message: "test-value", input: ThreadMessageReplaceInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func thread_messages_replace_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.threadMessages.replace(message: "test-value", input: ThreadMessageReplaceInput())
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func thread_messages_replies_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.threadMessages.replies(message: "test-value")
        ContractSupport.use(result)
    }

    @Test func thread_messages_replies_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.threadMessages.replies(message: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func thread_messages_replies_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.threadMessages.replies(message: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func thread_messages_replies_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.threadMessages.replies(message: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func thread_messages_replies_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.threadMessages.replies(message: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func thread_messages_reactions_remove_success() async throws {
        let client = try await ContractSupport.client()
        try await client.v1.threadMessages.reactions.remove(message: "test-value")
    }

    @Test func thread_messages_reactions_remove_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            try await client.v1.threadMessages.reactions.remove(message: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func thread_messages_reactions_remove_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            try await client.v1.threadMessages.reactions.remove(message: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func thread_messages_reactions_remove_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            try await client.v1.threadMessages.reactions.remove(message: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func thread_messages_reactions_remove_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            try await client.v1.threadMessages.reactions.remove(message: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func thread_messages_reactions_create_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.threadMessages.reactions.create(message: "test-value", input: ReactionCreateInput(emoji: "test-value"))
        ContractSupport.use(result)
    }

    @Test func thread_messages_reactions_create_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.threadMessages.reactions.create(message: "test-value", input: ReactionCreateInput(emoji: "test-value"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func thread_messages_reactions_create_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.threadMessages.reactions.create(message: "test-value", input: ReactionCreateInput(emoji: "test-value"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func thread_messages_reactions_create_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.threadMessages.reactions.create(message: "test-value", input: ReactionCreateInput(emoji: "test-value"))
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func thread_messages_reactions_create_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.threadMessages.reactions.create(message: "test-value", input: ReactionCreateInput(emoji: "test-value"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }
}
