// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 782eb876e4c2

import Foundation
import Testing
import ArchAstroPlatform

@Suite(.serialized) struct ConfigContractTests {

    @Test func config_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.list()
        ContractSupport.use(result.data)
        ContractSupport.use(result)
    }

    @Test func config_list_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.list()
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.list()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_create_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.create(input: ConfigCreateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
        ContractSupport.use(result)
    }

    @Test func config_create_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.create(input: ConfigCreateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_create_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.create(input: ConfigCreateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_create_error_409() async throws {
        let client = try await ContractSupport.errorClient(409)
        do {
            _ = try await client.v1.config.create(input: ConfigCreateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
            Issue.record("Expected ApiError with status 409")
        }
        catch let error as ApiError {
            #expect(error.status == 409)
        }
    }

    @Test func config_create_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.create(input: ConfigCreateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_encrypt_secret_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.encryptSecret(input: ConfigEncryptSecretInput(plaintext: "test-value"))
        ContractSupport.use(result)
    }

    @Test func config_encrypt_secret_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.encryptSecret(input: ConfigEncryptSecretInput(plaintext: "test-value"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_encrypt_secret_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.encryptSecret(input: ConfigEncryptSecretInput(plaintext: "test-value"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_encrypt_secret_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.encryptSecret(input: ConfigEncryptSecretInput(plaintext: "test-value"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_facets_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.facets()
        ContractSupport.use(result)
    }

    @Test func config_facets_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.facets()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_facets_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.facets()
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_validate_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.validate(input: ConfigValidateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
        ContractSupport.use(result)
    }

    @Test func config_validate_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.validate(input: ConfigValidateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_validate_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.validate(input: ConfigValidateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_validate_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.validate(input: ConfigValidateInput(kind: "test", mimeType: "application/json", rawContent: "test content"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_delete_success() async throws {
        let client = try await ContractSupport.client()
        try await client.v1.config.delete(config: "test-value")
    }

    @Test func config_delete_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            try await client.v1.config.delete(config: "test-value")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_delete_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            try await client.v1.config.delete(config: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_delete_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            try await client.v1.config.delete(config: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_delete_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            try await client.v1.config.delete(config: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_delete_error_409() async throws {
        let client = try await ContractSupport.errorClient(409)
        do {
            try await client.v1.config.delete(config: "test-value")
            Issue.record("Expected ApiError with status 409")
        }
        catch let error as ApiError {
            #expect(error.status == 409)
        }
    }

    @Test func config_delete_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            try await client.v1.config.delete(config: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_get_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.get(config: "test-value")
        ContractSupport.use(result)
    }

    @Test func config_get_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.get(config: "test-value")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_get_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.get(config: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_get_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.get(config: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_get_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.get(config: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_get_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.get(config: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_update_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.update(config: "test-value", input: ConfigUpdateInput())
        ContractSupport.use(result)
    }

    @Test func config_update_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.update(config: "test-value", input: ConfigUpdateInput())
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_update_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.update(config: "test-value", input: ConfigUpdateInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_update_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.update(config: "test-value", input: ConfigUpdateInput())
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_update_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.update(config: "test-value", input: ConfigUpdateInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_update_error_409() async throws {
        let client = try await ContractSupport.errorClient(409)
        do {
            _ = try await client.v1.config.update(config: "test-value", input: ConfigUpdateInput())
            Issue.record("Expected ApiError with status 409")
        }
        catch let error as ApiError {
            #expect(error.status == 409)
        }
    }

    @Test func config_update_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.update(config: "test-value", input: ConfigUpdateInput())
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_archive_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.archive(config: "test-value", input: ConfigArchiveInput())
        ContractSupport.use(result)
    }

    @Test func config_archive_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.archive(config: "test-value", input: ConfigArchiveInput())
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_archive_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.archive(config: "test-value", input: ConfigArchiveInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_archive_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.archive(config: "test-value", input: ConfigArchiveInput())
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_archive_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.archive(config: "test-value", input: ConfigArchiveInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_archive_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.archive(config: "test-value", input: ConfigArchiveInput())
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_change_owner_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.changeOwner(config: "test-value", input: ConfigChangeOwnerInput())
        ContractSupport.use(result)
    }

    @Test func config_change_owner_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.changeOwner(config: "test-value", input: ConfigChangeOwnerInput())
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_change_owner_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.changeOwner(config: "test-value", input: ConfigChangeOwnerInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_change_owner_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.changeOwner(config: "test-value", input: ConfigChangeOwnerInput())
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_change_owner_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.changeOwner(config: "test-value", input: ConfigChangeOwnerInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_change_owner_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.changeOwner(config: "test-value", input: ConfigChangeOwnerInput())
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_content_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.content(config: "test-value")
        #expect(!result.mimeType.isEmpty)
    }

    @Test func config_content_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.content(config: "test-value")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_content_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.content(config: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_content_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.content(config: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_content_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.content(config: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_content_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.content(config: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_unarchive_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.unarchive(config: "test-value", input: ConfigUnarchiveInput())
        ContractSupport.use(result)
    }

    @Test func config_unarchive_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.unarchive(config: "test-value", input: ConfigUnarchiveInput())
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_unarchive_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.unarchive(config: "test-value", input: ConfigUnarchiveInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_unarchive_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.unarchive(config: "test-value", input: ConfigUnarchiveInput())
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_unarchive_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.unarchive(config: "test-value", input: ConfigUnarchiveInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_unarchive_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.unarchive(config: "test-value", input: ConfigUnarchiveInput())
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_versions_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.versions(config: "test-value")
        ContractSupport.use(result)
    }

    @Test func config_versions_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.versions(config: "test-value")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_versions_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.versions(config: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_versions_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.config.versions(config: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func config_versions_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.versions(config: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_versions_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.versions(config: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func config_kinds_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.kinds.list()
        ContractSupport.use(result.data)
        ContractSupport.use(result)
    }

    @Test func config_kinds_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.kinds.list()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_kinds_schema_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.kinds.schema(kind: "test")
        ContractSupport.use(result)
    }

    @Test func config_kinds_schema_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.kinds.schema(kind: "test")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_kinds_schema_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.kinds.schema(kind: "test")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_system_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.system.list()
        ContractSupport.use(result.data)
        ContractSupport.use(result)
    }

    @Test func config_system_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.system.list()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_system_facets_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.system.facets()
        ContractSupport.use(result)
    }

    @Test func config_system_facets_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.system.facets()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_system_get_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.system.get(system: "test-value")
        ContractSupport.use(result)
    }

    @Test func config_system_get_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.system.get(system: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_system_get_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.system.get(system: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_system_clone_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.config.system.clone(system: "test-value", input: SystemCloneInput())
        ContractSupport.use(result)
    }

    @Test func config_system_clone_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.config.system.clone(system: "test-value", input: SystemCloneInput())
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func config_system_clone_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.config.system.clone(system: "test-value", input: SystemCloneInput())
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func config_system_clone_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.config.system.clone(system: "test-value", input: SystemCloneInput())
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func config_system_clone_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.config.system.clone(system: "test-value", input: SystemCloneInput())
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }
}
