// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: b8aaf22b0880

import Foundation
import Testing
import ArchAstroPlatform

@Suite(.serialized) struct SlackChannelBindingsContractTests {

    @Test func slack_channel_bindings_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.list()
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_list_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.list()
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.list()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_list_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.list()
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_create_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_create_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_create_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_create_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_create_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func slack_channel_bindings_create_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func slack_channel_bindings_delete_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.delete(channel: "test-value")
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_delete_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_delete_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_delete_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_delete_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func slack_channel_bindings_delete_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func slack_channel_bindings_get_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_get_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_get_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_get_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_get_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func slack_channel_bindings_get_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }
}
