// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 873c44d69a43

import Foundation
import Testing
import ArchAstroPlatform

@Suite(.serialized) struct SlackChannelBindingsContractTests {

    @Test func slack_channel_bindings_list_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.list()
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_list_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.list()
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_list_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.list()
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_list_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.list()
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_create_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_create_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_create_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_create_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_create_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func slack_channel_bindings_create_error_409() async throws {
        let client = try await ContractSupport.errorClient(409)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 409")
        }
        catch let error as ApiError {
            #expect(error.status == 409)
        }
    }

    @Test func slack_channel_bindings_create_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.create(input: SlackChannelBindingCreateInput(agentUserIds: ["test-value"], channelId: "test-id", slackTeamId: "test-id", teamId: "test-id"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func slack_channel_bindings_assign_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.assign(input: SlackChannelBindingAssignInput(agentUserId: "test-id", channelId: "test-id", slackTeamId: "test-id"))
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_assign_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.assign(input: SlackChannelBindingAssignInput(agentUserId: "test-id", channelId: "test-id", slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_assign_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.assign(input: SlackChannelBindingAssignInput(agentUserId: "test-id", channelId: "test-id", slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_assign_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.assign(input: SlackChannelBindingAssignInput(agentUserId: "test-id", channelId: "test-id", slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_assign_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.slackChannelBindings.assign(input: SlackChannelBindingAssignInput(agentUserId: "test-id", channelId: "test-id", slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func slack_channel_bindings_assign_error_409() async throws {
        let client = try await ContractSupport.errorClient(409)
        do {
            _ = try await client.v1.slackChannelBindings.assign(input: SlackChannelBindingAssignInput(agentUserId: "test-id", channelId: "test-id", slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 409")
        }
        catch let error as ApiError {
            #expect(error.status == 409)
        }
    }

    @Test func slack_channel_bindings_assign_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.assign(input: SlackChannelBindingAssignInput(agentUserId: "test-id", channelId: "test-id", slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func slack_channel_bindings_provision_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_provision_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_provision_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_provision_error_402() async throws {
        let client = try await ContractSupport.errorClient(402)
        do {
            _ = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
            Issue.record("Expected ApiError with status 402")
        }
        catch let error as ApiError {
            #expect(error.status == 402)
        }
    }

    @Test func slack_channel_bindings_provision_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_provision_error_409() async throws {
        let client = try await ContractSupport.errorClient(409)
        do {
            _ = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
            Issue.record("Expected ApiError with status 409")
        }
        catch let error as ApiError {
            #expect(error.status == 409)
        }
    }

    @Test func slack_channel_bindings_provision_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func slack_channel_bindings_provision_error_500() async throws {
        let client = try await ContractSupport.errorClient(500)
        do {
            _ = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
            Issue.record("Expected ApiError with status 500")
        }
        catch let error as ApiError {
            #expect(error.status == 500)
        }
    }

    @Test func slack_channel_bindings_provision_error_502() async throws {
        let client = try await ContractSupport.errorClient(502)
        do {
            _ = try await client.v1.slackChannelBindings.provision(input: SlackChannelBindingProvisionInput(customerKey: "test-key", customerLabel: "test-value", slackTeamId: "test-id", templateConfigId: "test-id"))
            Issue.record("Expected ApiError with status 502")
        }
        catch let error as ApiError {
            #expect(error.status == 502)
        }
    }

    @Test func slack_channel_bindings_delete_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.delete(channel: "test-value")
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_delete_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_delete_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_delete_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_delete_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func slack_channel_bindings_delete_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.delete(channel: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func slack_channel_bindings_get_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_get_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_get_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_get_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_get_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func slack_channel_bindings_get_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.get(channel: "test-value", slackTeamId: "test-id")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func slack_channel_bindings_delivery_outcomes_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.deliveryOutcomes(channel: "test-value")
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_delivery_outcomes_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.deliveryOutcomes(channel: "test-value")
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_delivery_outcomes_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.deliveryOutcomes(channel: "test-value")
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_delivery_outcomes_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.deliveryOutcomes(channel: "test-value")
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_delivery_outcomes_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.deliveryOutcomes(channel: "test-value")
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }

    @Test func slack_channel_bindings_deposit_thread_success() async throws {
        let client = try await ContractSupport.client()
        let result = try await client.v1.slackChannelBindings.depositThread(channel: "test-value", input: SlackChannelBindingDepositThreadInput(slackTeamId: "test-id"))
        ContractSupport.use(result)
    }

    @Test func slack_channel_bindings_deposit_thread_error_400() async throws {
        let client = try await ContractSupport.errorClient(400)
        do {
            _ = try await client.v1.slackChannelBindings.depositThread(channel: "test-value", input: SlackChannelBindingDepositThreadInput(slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 400")
        }
        catch let error as ApiError {
            #expect(error.status == 400)
        }
    }

    @Test func slack_channel_bindings_deposit_thread_error_401() async throws {
        let client = try await ContractSupport.errorClient(401)
        do {
            _ = try await client.v1.slackChannelBindings.depositThread(channel: "test-value", input: SlackChannelBindingDepositThreadInput(slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 401")
        }
        catch let error as ApiError {
            #expect(error.status == 401)
        }
    }

    @Test func slack_channel_bindings_deposit_thread_error_403() async throws {
        let client = try await ContractSupport.errorClient(403)
        do {
            _ = try await client.v1.slackChannelBindings.depositThread(channel: "test-value", input: SlackChannelBindingDepositThreadInput(slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 403")
        }
        catch let error as ApiError {
            #expect(error.status == 403)
        }
    }

    @Test func slack_channel_bindings_deposit_thread_error_404() async throws {
        let client = try await ContractSupport.errorClient(404)
        do {
            _ = try await client.v1.slackChannelBindings.depositThread(channel: "test-value", input: SlackChannelBindingDepositThreadInput(slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 404")
        }
        catch let error as ApiError {
            #expect(error.status == 404)
        }
    }

    @Test func slack_channel_bindings_deposit_thread_error_422() async throws {
        let client = try await ContractSupport.errorClient(422)
        do {
            _ = try await client.v1.slackChannelBindings.depositThread(channel: "test-value", input: SlackChannelBindingDepositThreadInput(slackTeamId: "test-id"))
            Issue.record("Expected ApiError with status 422")
        }
        catch let error as ApiError {
            #expect(error.status == 422)
        }
    }
}
