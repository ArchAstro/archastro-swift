// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 160e293c79c0

import Foundation

public struct CompletionCreateInputMessagesItemToolCallsItem: Codable, Sendable {
    /// Arguments the model wants to pass to the tool, as a key-value map. Deserialize and validate these against the tool's input schema before executing.
    public var arguments: [String: JSONValue]
    /// Unique identifier for this tool call, assigned by the model. Use this value as `id` when submitting the corresponding tool result.
    public var id: String
    /// Name of the tool or function the model wants to invoke, e.g. `"web_search"` or `"run_code"`.
    public var name: String
    /// Opaque signature representing the model's internal reasoning that led to this tool call. `null` when the provider does not expose chain-of-thought data.
    public var thoughtSignature: String?

    public init(
        arguments: [String: JSONValue],
        id: String,
        name: String,
        thoughtSignature: String? = nil
    ) {
        self.arguments = arguments
        self.id = id
        self.name = name
        self.thoughtSignature = thoughtSignature
    }

    private enum CodingKeys: String, CodingKey {
        case arguments
        case id
        case name
        case thoughtSignature = "thought_signature"
    }
}

public struct CompletionCreateInputMessagesItemToolResultsItem: Codable, Sendable {
    /// Plain-text output produced by the tool execution. `null` when the result is expressed entirely through `resolution`.
    public var content: String?
    /// ID of the tool call this result satisfies. Must match the `id` from the corresponding `AIToolCall`.
    public var id: String
    /// Name of the tool or function that was executed, e.g. `"web_search"`. Must match the `name` from the corresponding `AIToolCall`.
    public var name: String
    /// Structured result data from the tool execution. Shape varies by tool. `null` when the result is expressed as plain text in `content`.
    public var resolution: JSONValue?

    public init(content: String? = nil, id: String, name: String, resolution: JSONValue? = nil) {
        self.content = content
        self.id = id
        self.name = name
        self.resolution = resolution
    }

    private enum CodingKeys: String, CodingKey {
        case content
        case id
        case name
        case resolution
    }
}

public struct CompletionCreateInputMessagesItem: Codable, Sendable {
    /// Plain-text content of the message. Present for `system`, `user`, and `assistant` messages. `null` when the message body is expressed through `content_parts` or `tool_calls`.
    public var content: String?
    /// Multimodal content parts for the message, used when the body includes images or mixed media. Each part is a map with a `type` key (`"text"`, `"image_url"`, or `"image_data"`). `null` when `content` is set.
    public var contentParts: [[String: JSONValue]]?
    /// Opaque token that can be passed on a subsequent request to resume this conversation from the current state. `null` when the provider does not support conversation resumption.
    public var resumeToken: String?
    /// The speaker role for this message. One of `"system"`, `"user"`, `"assistant"`, or `"tool"`.
    public var role: String
    /// Parsed structured data returned by the model when a JSON schema or structured-output mode was requested. Shape varies by the schema supplied at call time. `null` when structured output was not requested.
    public var structuredOutput: JSONValue?
    /// Tool calls requested by the model in an `assistant` message. Present only on assistant messages that invoke one or more tools. `null` on all other message roles.
    public var toolCalls: [CompletionCreateInputMessagesItemToolCallsItem]?
    /// Tool execution results provided in a `tool` message. Each entry corresponds to a prior tool call by its `id`. `null` on all other message roles.
    public var toolResults: [CompletionCreateInputMessagesItemToolResultsItem]?

    public init(
        content: String? = nil,
        contentParts: [[String: JSONValue]]? = nil,
        resumeToken: String? = nil,
        role: String,
        structuredOutput: JSONValue? = nil,
        toolCalls: [CompletionCreateInputMessagesItemToolCallsItem]? = nil,
        toolResults: [CompletionCreateInputMessagesItemToolResultsItem]? = nil
    ) {
        self.content = content
        self.contentParts = contentParts
        self.resumeToken = resumeToken
        self.role = role
        self.structuredOutput = structuredOutput
        self.toolCalls = toolCalls
        self.toolResults = toolResults
    }

    private enum CodingKeys: String, CodingKey {
        case content
        case contentParts = "content_parts"
        case resumeToken = "resume_token"
        case role
        case structuredOutput = "structured_output"
        case toolCalls = "tool_calls"
        case toolResults = "tool_results"
    }
}

public struct CompletionCreateInputOptsToolsItemFunction: Codable, Sendable {
    /// Human-readable description of what the function does. The model uses this to decide when to call the function. `null` if not provided.
    public var description: String?
    /// Unique name of the function that the model can invoke, e.g. `"get_weather"`.
    public var name: String
    /// JSON Schema object describing the function's accepted parameters. Must be a valid JSON Schema of type `"object"`.
    public var parameters: [String: JSONValue]

    public init(description: String? = nil, name: String, parameters: [String: JSONValue]) {
        self.description = description
        self.name = name
        self.parameters = parameters
    }

    private enum CodingKeys: String, CodingKey {
        case description
        case name
        case parameters
    }
}

public struct CompletionCreateInputOptsToolsItem: Codable, Sendable {
    /// Callable function this tool exposes, including its name, description, and parameter schema.
    public var function: CompletionCreateInputOptsToolsItemFunction
    /// The tool type. Currently always `"function"`.
    public var type: String

    public init(function: CompletionCreateInputOptsToolsItemFunction, type: String) {
        self.function = function
        self.type = type
    }

    private enum CodingKeys: String, CodingKey {
        case function
        case type
    }
}

public struct CompletionCreateInputOpts: Codable, Sendable {
    /// Maximum number of tokens the model may generate in the completion. Omit to use the model's default limit.
    public var maxTokens: Int?
    /// Model identifier to use for the completion, e.g. `"gpt-4o"` or `"claude-3-7-sonnet-latest"`.
    public var model: String
    /// Server-managed tool declarations executed before the response is returned. Each entry must include a `type` key; currently only `"search"` is supported.
    public var serverTools: [[String: JSONValue]]?
    /// Sampling temperature between `0.0` and `2.0`. Higher values produce more random output. Omit to use the model's default.
    public var temperature: Double?
    /// Controls how the model selects tools. One of `"auto"`, `"required"`, or `"none"`. Omit to let the model decide.
    public var toolChoice: String?
    /// OpenAI-compatible tool definitions available to the model. Omit when not using function calling.
    public var tools: [CompletionCreateInputOptsToolsItem]?

    public init(
        maxTokens: Int? = nil,
        model: String,
        serverTools: [[String: JSONValue]]? = nil,
        temperature: Double? = nil,
        toolChoice: String? = nil,
        tools: [CompletionCreateInputOptsToolsItem]? = nil
    ) {
        self.maxTokens = maxTokens
        self.model = model
        self.serverTools = serverTools
        self.temperature = temperature
        self.toolChoice = toolChoice
        self.tools = tools
    }

    private enum CodingKeys: String, CodingKey {
        case maxTokens = "max_tokens"
        case model
        case serverTools = "server_tools"
        case temperature
        case toolChoice = "tool_choice"
        case tools
    }
}

/// Create a chat completion
public struct CompletionCreateInput: Codable, Sendable {
    /// Key-value map used to resolve template variables in message content. Omit if messages contain no templates.
    public var context: [String: JSONValue]?
    /// Ordered list of conversation messages to send to the model.
    public var messages: [CompletionCreateInputMessagesItem]
    /// Model and sampling configuration for this request.
    public var opts: CompletionCreateInputOpts

    public init(
        context: [String: JSONValue]? = nil,
        messages: [CompletionCreateInputMessagesItem],
        opts: CompletionCreateInputOpts
    ) {
        self.context = context
        self.messages = messages
        self.opts = opts
    }

    private enum CodingKeys: String, CodingKey {
        case context
        case messages
        case opts
    }
}

public struct CompletionStreamInputMessagesItemToolCallsItem: Codable, Sendable {
    /// Arguments the model wants to pass to the tool, as a key-value map. Deserialize and validate these against the tool's input schema before executing.
    public var arguments: [String: JSONValue]
    /// Unique identifier for this tool call, assigned by the model. Use this value as `id` when submitting the corresponding tool result.
    public var id: String
    /// Name of the tool or function the model wants to invoke, e.g. `"web_search"` or `"run_code"`.
    public var name: String
    /// Opaque signature representing the model's internal reasoning that led to this tool call. `null` when the provider does not expose chain-of-thought data.
    public var thoughtSignature: String?

    public init(
        arguments: [String: JSONValue],
        id: String,
        name: String,
        thoughtSignature: String? = nil
    ) {
        self.arguments = arguments
        self.id = id
        self.name = name
        self.thoughtSignature = thoughtSignature
    }

    private enum CodingKeys: String, CodingKey {
        case arguments
        case id
        case name
        case thoughtSignature = "thought_signature"
    }
}

public struct CompletionStreamInputMessagesItemToolResultsItem: Codable, Sendable {
    /// Plain-text output produced by the tool execution. `null` when the result is expressed entirely through `resolution`.
    public var content: String?
    /// ID of the tool call this result satisfies. Must match the `id` from the corresponding `AIToolCall`.
    public var id: String
    /// Name of the tool or function that was executed, e.g. `"web_search"`. Must match the `name` from the corresponding `AIToolCall`.
    public var name: String
    /// Structured result data from the tool execution. Shape varies by tool. `null` when the result is expressed as plain text in `content`.
    public var resolution: JSONValue?

    public init(content: String? = nil, id: String, name: String, resolution: JSONValue? = nil) {
        self.content = content
        self.id = id
        self.name = name
        self.resolution = resolution
    }

    private enum CodingKeys: String, CodingKey {
        case content
        case id
        case name
        case resolution
    }
}

public struct CompletionStreamInputMessagesItem: Codable, Sendable {
    /// Plain-text content of the message. Present for `system`, `user`, and `assistant` messages. `null` when the message body is expressed through `content_parts` or `tool_calls`.
    public var content: String?
    /// Multimodal content parts for the message, used when the body includes images or mixed media. Each part is a map with a `type` key (`"text"`, `"image_url"`, or `"image_data"`). `null` when `content` is set.
    public var contentParts: [[String: JSONValue]]?
    /// Opaque token that can be passed on a subsequent request to resume this conversation from the current state. `null` when the provider does not support conversation resumption.
    public var resumeToken: String?
    /// The speaker role for this message. One of `"system"`, `"user"`, `"assistant"`, or `"tool"`.
    public var role: String
    /// Parsed structured data returned by the model when a JSON schema or structured-output mode was requested. Shape varies by the schema supplied at call time. `null` when structured output was not requested.
    public var structuredOutput: JSONValue?
    /// Tool calls requested by the model in an `assistant` message. Present only on assistant messages that invoke one or more tools. `null` on all other message roles.
    public var toolCalls: [CompletionStreamInputMessagesItemToolCallsItem]?
    /// Tool execution results provided in a `tool` message. Each entry corresponds to a prior tool call by its `id`. `null` on all other message roles.
    public var toolResults: [CompletionStreamInputMessagesItemToolResultsItem]?

    public init(
        content: String? = nil,
        contentParts: [[String: JSONValue]]? = nil,
        resumeToken: String? = nil,
        role: String,
        structuredOutput: JSONValue? = nil,
        toolCalls: [CompletionStreamInputMessagesItemToolCallsItem]? = nil,
        toolResults: [CompletionStreamInputMessagesItemToolResultsItem]? = nil
    ) {
        self.content = content
        self.contentParts = contentParts
        self.resumeToken = resumeToken
        self.role = role
        self.structuredOutput = structuredOutput
        self.toolCalls = toolCalls
        self.toolResults = toolResults
    }

    private enum CodingKeys: String, CodingKey {
        case content
        case contentParts = "content_parts"
        case resumeToken = "resume_token"
        case role
        case structuredOutput = "structured_output"
        case toolCalls = "tool_calls"
        case toolResults = "tool_results"
    }
}

public struct CompletionStreamInputOptsToolsItemFunction: Codable, Sendable {
    /// Human-readable description of what the function does. The model uses this to decide when to call the function. `null` if not provided.
    public var description: String?
    /// Unique name of the function that the model can invoke, e.g. `"get_weather"`.
    public var name: String
    /// JSON Schema object describing the function's accepted parameters. Must be a valid JSON Schema of type `"object"`.
    public var parameters: [String: JSONValue]

    public init(description: String? = nil, name: String, parameters: [String: JSONValue]) {
        self.description = description
        self.name = name
        self.parameters = parameters
    }

    private enum CodingKeys: String, CodingKey {
        case description
        case name
        case parameters
    }
}

public struct CompletionStreamInputOptsToolsItem: Codable, Sendable {
    /// Callable function this tool exposes, including its name, description, and parameter schema.
    public var function: CompletionStreamInputOptsToolsItemFunction
    /// The tool type. Currently always `"function"`.
    public var type: String

    public init(function: CompletionStreamInputOptsToolsItemFunction, type: String) {
        self.function = function
        self.type = type
    }

    private enum CodingKeys: String, CodingKey {
        case function
        case type
    }
}

public struct CompletionStreamInputOpts: Codable, Sendable {
    /// Maximum number of tokens the model may generate. Omit for the model default.
    public var maxTokens: Int?
    /// Model identifier, e.g. `"gpt-4o"` or `"claude-3-7-sonnet-latest"`.
    public var model: String
    /// Server-managed tool declarations executed before the response. Each entry must include a `type` key; currently only `"search"` is supported.
    public var serverTools: [[String: JSONValue]]?
    /// Sampling temperature between `0.0` and `2.0`. Higher values produce more random output. Omit for the model default.
    public var temperature: Double?
    /// Controls tool selection. One of `"auto"`, `"required"`, or `"none"`. Omit to let the model decide.
    public var toolChoice: String?
    /// OpenAI-compatible tool definitions available to the model. Omit when not using function calling.
    public var tools: [CompletionStreamInputOptsToolsItem]?

    public init(
        maxTokens: Int? = nil,
        model: String,
        serverTools: [[String: JSONValue]]? = nil,
        temperature: Double? = nil,
        toolChoice: String? = nil,
        tools: [CompletionStreamInputOptsToolsItem]? = nil
    ) {
        self.maxTokens = maxTokens
        self.model = model
        self.serverTools = serverTools
        self.temperature = temperature
        self.toolChoice = toolChoice
        self.tools = tools
    }

    private enum CodingKeys: String, CodingKey {
        case maxTokens = "max_tokens"
        case model
        case serverTools = "server_tools"
        case temperature
        case toolChoice = "tool_choice"
        case tools
    }
}

/// Stream a chat completion
public struct CompletionStreamInput: Codable, Sendable {
    /// Key-value map used to resolve template variables in message content. Omit if messages contain no templates.
    public var context: [String: JSONValue]?
    /// Ordered list of conversation messages to send to the model.
    public var messages: [CompletionStreamInputMessagesItem]
    /// Model and sampling configuration for this request.
    public var opts: CompletionStreamInputOpts

    public init(
        context: [String: JSONValue]? = nil,
        messages: [CompletionStreamInputMessagesItem],
        opts: CompletionStreamInputOpts
    ) {
        self.context = context
        self.messages = messages
        self.opts = opts
    }

    private enum CodingKeys: String, CodingKey {
        case context
        case messages
        case opts
    }
}

public struct ImageEditsInputImagesItem: Codable, Sendable {
    /// The raw image content encoded as a base64 string (standard encoding, no line breaks).
    public var imageData: String
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. Must match the actual encoding of `image_data`.
    public var imageType: String

    public init(imageData: String, imageType: String) {
        self.imageData = imageData
        self.imageType = imageType
    }

    private enum CodingKeys: String, CodingKey {
        case imageData = "image_data"
        case imageType = "image_type"
    }
}

/// Edit an image with a text prompt
public struct ImageEditsInput: Codable, Sendable {
    /// Desired aspect ratio of the output, e.g. `"1:1"` or `"16:9"`. Not supported by all models; omit to use the model's default.
    public var aspectRatio: String?
    /// Background treatment for the output. Accepted values and behavior are model-dependent.
    public var background: String?
    /// Explicit output height in pixels. Takes precedence over `size` when both are provided. Not supported by all models.
    public var height: Int?
    /// Output resolution tier for Gemini models, e.g. `"1K"`, `"2K"`, or `"4K"`. Ignored by non-Gemini models.
    public var imageSize: String?
    /// One or more source images to edit. Each image must be supplied as a base64-encoded object.
    public var images: [ImageEditsInputImagesItem]
    /// Model identifier to use for editing. Omit to use the platform default image model.
    public var model: String?
    /// Desired MIME type or format for the returned image. Common values: `"png"`, `"jpeg"`, `"webp"`. Defaults to the model's native format.
    public var outputFormat: String?
    /// Natural-language description of the edit to apply to the source image(s).
    public var prompt: String
    /// Quality preset for the output image. Accepted values and behavior are model-dependent.
    public var quality: String?
    /// Output dimensions as a WxH string, e.g. `"1024x1024"`. Applies to OpenAI-compatible models. Omit to use the model's default.
    public var size: String?
    /// Style preset applied to the edit. Accepted values and behavior are model-dependent.
    public var style: String?
    /// Explicit output width in pixels. Takes precedence over `size` when both are provided. Not supported by all models.
    public var width: Int?

    public init(
        aspectRatio: String? = nil,
        background: String? = nil,
        height: Int? = nil,
        imageSize: String? = nil,
        images: [ImageEditsInputImagesItem],
        model: String? = nil,
        outputFormat: String? = nil,
        prompt: String,
        quality: String? = nil,
        size: String? = nil,
        style: String? = nil,
        width: Int? = nil
    ) {
        self.aspectRatio = aspectRatio
        self.background = background
        self.height = height
        self.imageSize = imageSize
        self.images = images
        self.model = model
        self.outputFormat = outputFormat
        self.prompt = prompt
        self.quality = quality
        self.size = size
        self.style = style
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case aspectRatio = "aspect_ratio"
        case background
        case height
        case imageSize = "image_size"
        case images
        case model
        case outputFormat = "output_format"
        case prompt
        case quality
        case size
        case style
        case width
    }
}

/// Generate an image from a text prompt
public struct ImageGenerationsInput: Codable, Sendable {
    /// Desired aspect ratio of the output, e.g. `"1:1"` or `"16:9"`. Not supported by all models; omit to use the model's default.
    public var aspectRatio: String?
    /// Background treatment for the output. Accepted values and behavior are model-dependent.
    public var background: String?
    /// Explicit output height in pixels. Takes precedence over `size` when both are provided. Not supported by all models.
    public var height: Int?
    /// Output resolution tier for Gemini models, e.g. `"1K"`, `"2K"`, or `"4K"`. Ignored by non-Gemini models.
    public var imageSize: String?
    /// Model identifier to use for generation. Omit to use the platform default image model.
    public var model: String?
    /// Number of images to generate. Defaults to `1`. Values greater than `1` are only supported by models that allow batch generation.
    public var n: Int?
    /// Desired MIME type or format for the returned image. Common values: `"png"`, `"jpeg"`, `"webp"`. Defaults to the model's native format.
    public var outputFormat: String?
    /// Natural-language description of the image to generate.
    public var prompt: String
    /// Quality preset for the output image. Accepted values and behavior are model-dependent.
    public var quality: String?
    /// Output dimensions as a WxH string, e.g. `"1024x1024"`. Applies to OpenAI-compatible models. Omit to use the model's default.
    public var size: String?
    /// Style preset applied to the generated image. Accepted values and behavior are model-dependent.
    public var style: String?
    /// Explicit output width in pixels. Takes precedence over `size` when both are provided. Not supported by all models.
    public var width: Int?

    public init(
        aspectRatio: String? = nil,
        background: String? = nil,
        height: Int? = nil,
        imageSize: String? = nil,
        model: String? = nil,
        n: Int? = nil,
        outputFormat: String? = nil,
        prompt: String,
        quality: String? = nil,
        size: String? = nil,
        style: String? = nil,
        width: Int? = nil
    ) {
        self.aspectRatio = aspectRatio
        self.background = background
        self.height = height
        self.imageSize = imageSize
        self.model = model
        self.n = n
        self.outputFormat = outputFormat
        self.prompt = prompt
        self.quality = quality
        self.size = size
        self.style = style
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case aspectRatio = "aspect_ratio"
        case background
        case height
        case imageSize = "image_size"
        case model
        case n
        case outputFormat = "output_format"
        case prompt
        case quality
        case size
        case style
        case width
    }
}

public struct ChatModelsResponseDataItem: Codable, Sendable {
    /// Machine-readable model capabilities. `"image"` marks image-input chat, `"search"` marks built-in web search, and `"thinking"` marks models with configurable reasoning. Empty when the catalog entry declares no special capabilities.
    public var capabilities: [String]
    /// `true` for the model the platform selects when an agent has no `default_model` configured, or for the system-wide fallback in image-generation contexts. Exactly one entry in any given model list carries this flag.
    public var `default`: Bool
    /// Provider-assigned model identifier used when specifying a model on API requests, e.g. `"claude-sonnet-4-6"` or `"gemini-2.5-flash"`.
    public var id: String
    /// MIME types accepted in chat `content_parts` image/file inputs for this model. For image-capable chat models this includes values such as `"image/png"`. Empty for text-only models.
    public var inputMediaFormats: [String]
    /// Human-readable display label for this model, e.g. `"Claude Sonnet 4.6"` or `"Gemini 3.5 Flash (thinking)"`. Render this value directly in pickers rather than attempting to parse or transform `id`. Falls back to the `id` string when the catalog entry does not declare an explicit name.
    public var name: String
    /// MIME types this model can emit as media in chat responses. Empty for text-output models, including image-understanding models that only return text.
    public var outputMediaFormats: [String]

    public init(
        capabilities: [String],
        `default`: Bool,
        id: String,
        inputMediaFormats: [String],
        name: String,
        outputMediaFormats: [String]
    ) {
        self.capabilities = capabilities
        self.`default` = `default`
        self.id = id
        self.inputMediaFormats = inputMediaFormats
        self.name = name
        self.outputMediaFormats = outputMediaFormats
    }

    private enum CodingKeys: String, CodingKey {
        case capabilities
        case `default`
        case id
        case inputMediaFormats = "input_media_formats"
        case name
        case outputMediaFormats = "output_media_formats"
    }
}

/// Successful response
public struct ChatModelsResponse: Codable, Sendable {
    /// Array of available model objects. At least one entry is always present.
    public var data: [ChatModelsResponseDataItem]

    public init(data: [ChatModelsResponseDataItem]) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data
    }
}

public struct ImageModelsResponseDataItem: Codable, Sendable {
    /// Machine-readable model capabilities. `"image"` marks image-input chat, `"search"` marks built-in web search, and `"thinking"` marks models with configurable reasoning. Empty when the catalog entry declares no special capabilities.
    public var capabilities: [String]
    /// `true` for the model the platform selects when an agent has no `default_model` configured, or for the system-wide fallback in image-generation contexts. Exactly one entry in any given model list carries this flag.
    public var `default`: Bool
    /// Provider-assigned model identifier used when specifying a model on API requests, e.g. `"claude-sonnet-4-6"` or `"gemini-2.5-flash"`.
    public var id: String
    /// MIME types accepted in chat `content_parts` image/file inputs for this model. For image-capable chat models this includes values such as `"image/png"`. Empty for text-only models.
    public var inputMediaFormats: [String]
    /// Human-readable display label for this model, e.g. `"Claude Sonnet 4.6"` or `"Gemini 3.5 Flash (thinking)"`. Render this value directly in pickers rather than attempting to parse or transform `id`. Falls back to the `id` string when the catalog entry does not declare an explicit name.
    public var name: String
    /// MIME types this model can emit as media in chat responses. Empty for text-output models, including image-understanding models that only return text.
    public var outputMediaFormats: [String]

    public init(
        capabilities: [String],
        `default`: Bool,
        id: String,
        inputMediaFormats: [String],
        name: String,
        outputMediaFormats: [String]
    ) {
        self.capabilities = capabilities
        self.`default` = `default`
        self.id = id
        self.inputMediaFormats = inputMediaFormats
        self.name = name
        self.outputMediaFormats = outputMediaFormats
    }

    private enum CodingKeys: String, CodingKey {
        case capabilities
        case `default`
        case id
        case inputMediaFormats = "input_media_formats"
        case name
        case outputMediaFormats = "output_media_formats"
    }
}

/// Successful response
public struct ImageModelsResponse: Codable, Sendable {
    /// Array of available image generation models, including their identifiers, human-readable names, and which one is the platform default.
    public var data: [ImageModelsResponseDataItem]

    public init(data: [ImageModelsResponseDataItem]) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data
    }
}

public final class CompletionResource: Sendable {
    private let http: HttpClient

    public init(http: HttpClient) {
        self.http = http
    }

    /// Create a chat completion
    ///
    /// Sends a list of messages to the configured AI provider and returns a single
    /// completion. Use this endpoint when you want direct, low-level access to the
    /// underlying model without any workflow or agent orchestration.
    /// The authenticated app must have the `llm_calls` entitlement enabled on its
    /// plan. Requests that exceed the plan quota are rejected with `402`. Token
    /// usage is recorded against the authenticated app and organization.
    /// Supply `tools` and `tool_choice` to enable OpenAI-compatible function
    /// calling. Use `server_tools` to activate platform-managed tools such as
    /// search that run on the server side before the response is returned.
    ///
    /// - Parameters:
    ///   - input: Request body.
    /// - Returns: The completed AI response, including the generated message, finish reason, and token usage.
    public func create(input: CompletionCreateInput) async throws -> AICompletionResult {
        return try await http.request("/api/v1/ai/chat/completions", method: "POST", body: input)
    }

    /// Stream a chat completion
    ///
    /// Streams a chat completion over Server-Sent Events. Emits `message_delta`,
    /// `message_complete`, `tool_call_*`, `tool_result`, and a terminal `done` (or
    /// `error`) event. Same request shape as the non-streaming completion endpoint;
    /// the app must have the `llm_calls` entitlement.
    ///
    /// - Parameters:
    ///   - input: Request body.
    /// - Returns: Server-Sent Events stream
    public func stream(input: CompletionStreamInput) -> AsyncThrowingStream<SSEEvent, any Error> {
        return http.streamSSE("/api/v1/ai/chat/completions/stream", method: "POST", body: input)
    }
}

public final class ChatResource: Sendable {
    private let http: HttpClient
    public let completions: CompletionResource

    public init(http: HttpClient) {
        self.http = http
        self.completions = CompletionResource(http: http)
    }

    /// List available AI models
    ///
    /// Returns the set of AI models that can be used with the chat completion and
    /// workflow endpoints. The list reflects models currently enabled for the
    /// platform and includes each model's identifier and whether it is the default.
    /// Use the `model` field from any entry in `data` as the value for
    /// `opts.model` when calling the completions or workflows endpoint.
    ///
    /// - Returns: Successful response
    public func models() async throws -> ChatModelsResponse {
        return try await http.request("/api/v1/ai/chat/models")
    }
}

public final class ImageResource: Sendable {
    private let http: HttpClient

    public init(http: HttpClient) {
        self.http = http
    }

    /// Edit an image with a text prompt
    ///
    /// Applies a text-guided edit to one or more source images and returns the
    /// resulting image. Pass the source images as base64-encoded objects in the
    /// `images` array alongside a `prompt` describing the desired modification.
    /// The underlying provider is selected by the `model` parameter. Omit `model`
    /// to use the platform default. Size, quality, style, and format options are
    /// forwarded to the provider as-is; unsupported combinations for a given model
    /// return a 422 error with the provider's error message.
    /// This endpoint requires authentication. The request is billed against the
    /// workspace associated with the authenticated user.
    ///
    /// - Parameters:
    ///   - input: Request body.
    /// - Returns: The resulting edited image, including base64 data or a URL depending on the model.
    public func edits(input: ImageEditsInput) async throws -> AIImageResult {
        return try await http.request("/api/v1/ai/image/edits", method: "POST", body: input)
    }

    /// Generate an image from a text prompt
    ///
    /// Generates one or more images from a natural-language `prompt` using the
    /// specified AI image model. The response contains the first generated image;
    /// use `n` to request additional images (where supported by the model).
    /// The underlying provider is selected by the `model` parameter. Omit `model`
    /// to use the platform default. Size, quality, style, and format options are
    /// forwarded to the provider as-is; unsupported combinations for a given model
    /// return a 422 error with the provider's error message.
    /// This endpoint requires authentication. The request is billed against the
    /// workspace associated with the authenticated user.
    ///
    /// - Parameters:
    ///   - input: Request body.
    /// - Returns: The generated image, including base64 data or a URL depending on the model.
    public func generations(input: ImageGenerationsInput) async throws -> AIImageResult {
        return try await http.request("/api/v1/ai/image/generations", method: "POST", body: input)
    }

    /// List available image generation models
    ///
    /// Returns the list of image generation models available on the platform.
    /// Exactly one entry in the list carries `default: true`, indicating the model
    /// used when no `model` parameter is supplied to the generation or editing
    /// endpoints.
    /// This endpoint requires authentication and reflects the models enabled for
    /// the authenticated user's workspace.
    ///
    /// - Returns: Successful response
    public func models() async throws -> ImageModelsResponse {
        return try await http.request("/api/v1/ai/image/models")
    }
}

public final class AiResource: Sendable {
    private let http: HttpClient
    public let chat: ChatResource
    public let image: ImageResource

    public init(http: HttpClient) {
        self.http = http
        self.chat = ChatResource(http: http)
        self.image = ImageResource(http: http)
    }
}
