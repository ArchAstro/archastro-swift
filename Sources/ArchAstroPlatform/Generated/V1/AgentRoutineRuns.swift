// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 706524ea8c2c

import Foundation

public final class AgentRoutineRunResource: Sendable {
    private let http: HttpClient

    public init(http: HttpClient) {
        self.http = http
    }

    /// List agent routine runs
    ///
    /// Returns a paginated list of agent routine runs across all routines visible to
    /// the authenticated app scope. Results are ordered by creation time descending
    /// (most recent first).
    /// Use the `agent` parameter to filter runs to one or more specific agents. Use
    /// `status` to narrow results to runs in a particular state. Pagination is
    /// bidirectional: supply `after_cursor` to page forward through newer runs or
    /// `before_cursor` to page backward through older runs.
    /// This endpoint requires an app scope. Requests without a valid app credential
    /// return 403.
    ///
    /// - Parameters:
    ///   - agent: Filter by one or more agent IDs (`agi_...`) or `lookup_key` values. Repeat the parameter (e.g. `?agent[]=agi_a&agent[]=agi_b`) to OR multiple agents. Omit to return runs for all agents.
    ///   - status: Filter by run status. One of `"pending"`, `"running"`, `"completed"`, `"failed"`, `"skipped"`, or `"cancelled"`. Omit to return runs in any status.
    ///   - limit: Maximum number of runs to return. Defaults to 50; maximum is 100.
    ///   - beforeCursor: Opaque cursor from a previous response's `before_cursor` field. Returns the page of runs older than that cursor position.
    ///   - afterCursor: Opaque cursor from a previous response's `after_cursor` field. Returns the page of runs newer than that cursor position.
    /// - Returns: Paginated list of agent routine runs.
    public func list(agent: [String]? = nil, status: String? = nil, limit: Int? = nil, beforeCursor: String? = nil, afterCursor: String? = nil) async throws -> AgentRoutineRunListResponse {
        var query: [(String, String)] = []
        if let agent = agent {
            for item in agent {
                query.append(("agent", item))
            }
        }
        if let status = status {
            query.append(("status", status))
        }
        if let limit = limit {
            query.append(("limit", String(limit)))
        }
        if let beforeCursor = beforeCursor {
            query.append(("before_cursor", beforeCursor))
        }
        if let afterCursor = afterCursor {
            query.append(("after_cursor", afterCursor))
        }
        return try await http.request("/api/v1/agent_routine_runs", query: query)
    }
}
