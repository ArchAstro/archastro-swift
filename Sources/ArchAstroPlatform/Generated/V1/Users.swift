// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 438f399885f6

import Foundation

public struct UserTaskCreateInputTask: Codable, Sendable {
    /// Optional long-form description or notes for the task. Supports plain text.
    public var description: String?
    /// Date and time by which the task should be completed (ISO 8601). Omit to create the task without a due date.
    public var dueDate: Date?
    /// Optional free-form grouping label.
    public var epic: String?
    /// Arbitrary key-value map of named URLs or references associated with the task (e.g. external ticket links).
    public var links: [String: JSONValue]?
    /// Arbitrary key-value map for storing application-specific data alongside the task. Omit to create the task with no metadata.
    public var metadata: [String: JSONValue]?
    /// Human-readable title for the task.
    public var name: String
    /// ID of the agent to assign as owner (`agi_...`). Mutually exclusive with `owner_user`; omit to leave the task unassigned.
    public var ownerAgent: String?
    /// ID of the user to assign as owner (`usr_...`). Mutually exclusive with `owner_agent`; omit to leave the task unassigned.
    public var ownerUser: String?
    /// Create this task as a subtask of an existing top-level task (`tsk_...`). Subtasks nest exactly one level.
    public var parent: String?
    /// Priority level from `0` (highest) to `4` (lowest). Defaults to `2` (medium) when omitted.
    public var priority: Int?
    /// Source object identity (for example `ArchAstro/firstlanding`).
    public var sourceId: String?
    /// Container of the work this task is about (for example `github.com`). Must be supplied with `source_type` and `source_id`.
    public var sourceScope: String?
    /// Kind of source object (for example `repository`).
    public var sourceType: String?
    /// Initial status for the task. One of `"open"`, `"in_progress"`, or `"done"`. Defaults to `"open"` when omitted.
    public var status: String?
    /// Labels for grouping and filtering (max 20, each up to 40 characters). Stored canonically: lowercase, trimmed, de-duplicated.
    public var tags: [String]?
    /// Bind the task to a thread (`thr_...`) owned by the same team or user as the task. A bound task appears in that thread's task scope, exactly like a task filed from inside the conversation. Omit for a task not tied to a conversation.
    public var thread: String?

    public init(
        description: String? = nil,
        dueDate: Date? = nil,
        epic: String? = nil,
        links: [String: JSONValue]? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String,
        ownerAgent: String? = nil,
        ownerUser: String? = nil,
        parent: String? = nil,
        priority: Int? = nil,
        sourceId: String? = nil,
        sourceScope: String? = nil,
        sourceType: String? = nil,
        status: String? = nil,
        tags: [String]? = nil,
        thread: String? = nil
    ) {
        self.description = description
        self.dueDate = dueDate
        self.epic = epic
        self.links = links
        self.metadata = metadata
        self.name = name
        self.ownerAgent = ownerAgent
        self.ownerUser = ownerUser
        self.parent = parent
        self.priority = priority
        self.sourceId = sourceId
        self.sourceScope = sourceScope
        self.sourceType = sourceType
        self.status = status
        self.tags = tags
        self.thread = thread
    }

    private enum CodingKeys: String, CodingKey {
        case description
        case dueDate = "due_date"
        case epic
        case links
        case metadata
        case name
        case ownerAgent = "owner_agent"
        case ownerUser = "owner_user"
        case parent
        case priority
        case sourceId = "source_id"
        case sourceScope = "source_scope"
        case sourceType = "source_type"
        case status
        case tags
        case thread
    }
}

/// Create a task for an owner
public struct UserTaskCreateInput: Codable, Sendable {
    /// Explicit acting agent (`agi_...`) for a developer or server-to-server call. Mutually exclusive with an acting `user`; the agent must belong to the task owner.
    public var agent: String?
    /// Explicit organization (`org_...`) for developer and server-to-server calls. Pass null when the owner is not organization-scoped. The value must match the selected user or team.
    public var org: String?
    /// Attributes for the task to create. `name` is required; all other fields are optional.
    public var task: UserTaskCreateInputTask
    /// Team ID (`tem_...`). The task will be owned by this team.
    public var team: String?

    public init(
        agent: String? = nil,
        org: String? = nil,
        task: UserTaskCreateInputTask,
        team: String? = nil
    ) {
        self.agent = agent
        self.org = org
        self.task = task
        self.team = team
    }

    private enum CodingKeys: String, CodingKey {
        case agent
        case org
        case task
        case team
    }
}

public struct UserThreadCreateInputThreadMembersItem: Codable, Sendable {
    /// Public user (`usr_...`) or agent (`agt_...`) ID matching `type`.
    public var id: String
    /// Member kind. Use `user` for a user ID or `agent` for an agent ID.
    public var type: String

    public init(id: String, type: String) {
        self.id = id
        self.type = type
    }

    private enum CodingKeys: String, CodingKey {
        case id
        case type
    }
}

public struct UserThreadCreateInputThreadProfilePicture: Codable, Sendable {
    /// Base64-encoded image bytes.
    public var data: String?
    /// Original filename of the uploaded image, used for display and content-type inference.
    public var filename: String?
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`.
    public var mimeType: String?

    public init(data: String? = nil, filename: String? = nil, mimeType: String? = nil) {
        self.data = data
        self.filename = filename
        self.mimeType = mimeType
    }

    private enum CodingKeys: String, CodingKey {
        case data
        case filename
        case mimeType = "mime_type"
    }
}

public struct UserThreadCreateInputThreadSettings: Codable, Sendable {
    /// Whether the AI agent is active for this thread. `true` enables AI responses; `false` disables them. Defaults to `true` when settings have not been explicitly configured. `null` when a client explicitly cleared the setting.
    public var agentEnabled: Bool??

    public init(agentEnabled: Bool?? = nil) {
        self.agentEnabled = agentEnabled
    }

    private enum CodingKeys: String, CodingKey {
        case agentEnabled = "agent_enabled"
    }
}

public struct UserThreadCreateInputThread: Codable, Sendable {
    /// When `true`, provisions a legacy chat agent alongside the thread. Only needed for integrations that depend on the pre-v2 agent model.
    public var createLegacyAgent: Bool?
    /// Optional longer description of the thread's purpose. `null` if not provided.
    public var description: String?
    /// When `true`, the thread is hidden from the default thread list and accessible only by direct link or ID.
    public var isUnlisted: Bool?
    /// Client-assigned unique key for idempotent creation or later lookup. Must be unique within the owning organization.
    public var key: String?
    /// Optional behavioral subtype. `personal` is accepted only for a user-owned thread and limits membership to that user and agents currently owned by them. Mirror kinds remain server-derived and cannot be selected by callers.
    public var kind: String?
    /// Users and agents to add atomically when the thread is created. Each target must pass the same authorization rules as a post-creation member add. Slack mirror threads reject non-empty caller-supplied rosters because their membership is sync-owned.
    public var members: [UserThreadCreateInputThreadMembersItem]?
    /// Arbitrary key-value pairs stored alongside the thread. Values must be strings or numbers.
    public var metadata: [String: JSONValue]?
    /// When `true`, push and in-app notifications for this thread are suppressed for the creating user.
    public var muted: Bool?
    /// ID of the organization to create the thread under. Defaults to the authenticated user's primary organization when omitted.
    public var orgId: String?
    /// Optional profile image for the thread, provided as a base64-encoded payload.
    public var profilePicture: UserThreadCreateInputThreadProfilePicture?
    /// Configuration overrides for the thread, such as AI model selection and context window settings.
    public var settings: UserThreadCreateInputThreadSettings?
    /// Optional URL-safe identifier. Derived from the title when omitted and unique within the thread owner.
    public var slug: String?
    /// Display name for the thread. `null` if omitted, which causes the thread to be untitled.
    public var title: String?
    /// Thread visibility. A team-owned thread with members must explicitly use `restricted` or `private`. User- and agent-owned threads with members default to `private` and reject every other value.
    public var visibility: String?

    public init(
        createLegacyAgent: Bool? = nil,
        description: String? = nil,
        isUnlisted: Bool? = nil,
        key: String? = nil,
        kind: String? = nil,
        members: [UserThreadCreateInputThreadMembersItem]? = nil,
        metadata: [String: JSONValue]? = nil,
        muted: Bool? = nil,
        orgId: String? = nil,
        profilePicture: UserThreadCreateInputThreadProfilePicture? = nil,
        settings: UserThreadCreateInputThreadSettings? = nil,
        slug: String? = nil,
        title: String? = nil,
        visibility: String? = nil
    ) {
        self.createLegacyAgent = createLegacyAgent
        self.description = description
        self.isUnlisted = isUnlisted
        self.key = key
        self.kind = kind
        self.members = members
        self.metadata = metadata
        self.muted = muted
        self.orgId = orgId
        self.profilePicture = profilePicture
        self.settings = settings
        self.slug = slug
        self.title = title
        self.visibility = visibility
    }

    private enum CodingKeys: String, CodingKey {
        case createLegacyAgent = "create_legacy_agent"
        case description
        case isUnlisted = "is_unlisted"
        case key
        case kind
        case members
        case metadata
        case muted
        case orgId = "org_id"
        case profilePicture = "profile_picture"
        case settings
        case slug
        case title
        case visibility
    }
}

/// Create a thread for a user
public struct UserThreadCreateInput: Codable, Sendable {
    /// When `true`, suppresses the automatic welcome message that is otherwise sent into the thread on creation. Defaults to `false`.
    public var skipWelcomeMessage: Bool?
    /// Attributes for the new thread. See ThreadCreateParams for the full set of accepted fields.
    public var thread: UserThreadCreateInputThread

    public init(skipWelcomeMessage: Bool? = nil, thread: UserThreadCreateInputThread) {
        self.skipWelcomeMessage = skipWelcomeMessage
        self.thread = thread
    }

    private enum CodingKeys: String, CodingKey {
        case skipWelcomeMessage = "skip_welcome_message"
        case thread
    }
}

/// Create a personal access token
public struct TokenCreateInput: Codable, Sendable {
    /// Lifetime in days. One of `7`, `30`, `60`, `90`, or `365`. Defaults to `30`.
    public var expiresInDays: Int?
    /// Human-readable label for the token (e.g. `"Codex MCP"`). Stored as metadata only.
    public var name: String?
    /// Optional OAuth scopes to stamp on the token. Omit for `full_access`.
    public var scopes: [String]?

    public init(expiresInDays: Int? = nil, name: String? = nil, scopes: [String]? = nil) {
        self.expiresInDays = expiresInDays
        self.name = name
        self.scopes = scopes
    }

    private enum CodingKeys: String, CodingKey {
        case expiresInDays = "expires_in_days"
        case name
        case scopes
    }
}

public struct UserInvitesInputInvite: Codable, Sendable {
    /// Arbitrary key-value metadata to attach to the invite. Returned as-is on the resulting invite object.
    public var metadata: [String: JSONValue]?
    /// ID of the persona to associate with this invite (`per_...`). `null` if the invite is not bound to a persona.
    public var personaId: String?
    /// ID of the thread to associate with this invite (`thr_...`). `null` if the invite is not bound to a thread.
    public var threadId: String?

    public init(
        metadata: [String: JSONValue]? = nil,
        personaId: String? = nil,
        threadId: String? = nil
    ) {
        self.metadata = metadata
        self.personaId = personaId
        self.threadId = threadId
    }

    private enum CodingKeys: String, CodingKey {
        case metadata
        case personaId = "persona_id"
        case threadId = "thread_id"
    }
}

/// Create a user invite
public struct UserInvitesInput: Codable, Sendable {
    /// Parameters for the new invite. See the UserInviteCreateParams schema for field details.
    public var invite: UserInvitesInputInvite

    public init(invite: UserInvitesInputInvite) {
        self.invite = invite
    }

    private enum CodingKeys: String, CodingKey {
        case invite
    }
}

public struct UserProfileInputProfilePicture: Codable, Sendable {
    /// Base64-encoded binary content of the image file.
    public var data: String?
    /// Original filename of the image, used for storage metadata.
    public var filename: String?
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`.
    public var mimeType: String?

    public init(data: String? = nil, filename: String? = nil, mimeType: String? = nil) {
        self.data = data
        self.filename = filename
        self.mimeType = mimeType
    }

    private enum CodingKeys: String, CodingKey {
        case data
        case filename
        case mimeType = "mime_type"
    }
}

/// Update the current user's profile
public struct UserProfileInput: Codable, Sendable {
    /// Short display alias shown in place of the full name in compact UI contexts.
    public var alias: String?
    /// Updated display name for the user.
    public var fullName: String?
    /// Arbitrary key-value metadata to associate with the user. Existing keys are merged; pass `null` for a key to remove it.
    public var metadata: [String: JSONValue]?
    /// New profile picture to upload as a base64-encoded image. Replaces any existing picture.
    public var profilePicture: UserProfileInputProfilePicture?

    public init(
        alias: String? = nil,
        fullName: String? = nil,
        metadata: [String: JSONValue]? = nil,
        profilePicture: UserProfileInputProfilePicture? = nil
    ) {
        self.alias = alias
        self.fullName = fullName
        self.metadata = metadata
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case fullName = "full_name"
        case metadata
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskListResponseDataItemCreatedByActorProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserTaskListResponseDataItemCreatedByActor: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserTaskListResponseDataItemCreatedByActorProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserTaskListResponseDataItemCreatedByActorProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskListResponseDataItemCurrentLease: Codable, Sendable {
    /// Server-calculated lease expiry in ISO 8601 format.
    public var expiresAt: Date
    /// Bounded harness identifier for the coding session.
    public var harness: String
    /// Display name supplied by the coding session that holds the lease.
    public var sessionName: String

    public init(expiresAt: Date, harness: String, sessionName: String) {
        self.expiresAt = expiresAt
        self.harness = harness
        self.sessionName = sessionName
    }

    private enum CodingKeys: String, CodingKey {
        case expiresAt = "expires_at"
        case harness
        case sessionName = "session_name"
    }
}

public struct UserTaskListResponseDataItemOwnerActorProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserTaskListResponseDataItemOwnerActor: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserTaskListResponseDataItemOwnerActorProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserTaskListResponseDataItemOwnerActorProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskListResponseDataItem: Codable, Sendable {
    /// ID of the agent that owns this task (`agi_...`). `null` if the task is scoped to a team or user.
    public var agent: String??
    /// Number of tasks marked as blocking this task, whether or not they are done (see `GET /tasks/{task}/blockers`). Computed on list/show reads; create/update responses may lag one read behind.
    public var blockedByCount: Int?
    /// When the task was marked as done or otherwise closed (ISO 8601). `null` if the task is still open.
    public var closedAt: Date??
    /// Total number of comments posted on this task.
    public var commentsCount: Int?
    /// When the task was created (ISO 8601).
    public var createdAt: Date?
    /// Resolved creator details including `id`, `name`, `alias`, and `profile_picture`. `null` if no creator is set or the creator cannot be resolved (e.g. creating agent was deleted).
    public var createdByActor: UserTaskListResponseDataItemCreatedByActor??
    /// ID of the agent that created this task (`agi_...`). `null` if the task was created by a human user, or if the creating agent was later deleted.
    public var createdByAgent: String??
    /// ID of the user who created this task (`usr_...`). `null` if the task was created by an agent, or if creator provenance was cleared after the creator was deleted.
    public var createdByUser: String??
    /// Viewer-safe live coding-session lease summary. `null` when the task is unleased or the projected lease has expired. Fencing identifiers are never included.
    public var currentLease: UserTaskListResponseDataItemCurrentLease??
    /// Long-form description or notes for the task. `null` if no description has been provided.
    public var description: String??
    /// Date and time by which the task should be completed (ISO 8601). `null` if no due date is set.
    public var dueDate: Date??
    /// Free-form grouping label. `null` when the task is not in an epic.
    public var epic: String??
    /// Task ID (`tsk_...`).
    public var id: String
    /// `true` while at least one blocking task is not yet done. Informational only a blocked task can still change status and derived at read time, so the task un-blocks automatically when its last open blocker completes. Computed on list/show reads; create/update responses report `false` until the next read.
    public var isBlocked: Bool?
    /// Key-value map of named URLs or references associated with the task. Returns an empty object when no links have been set.
    public var links: [String: JSONValue]?
    /// Arbitrary key-value map of application-specific data stored alongside the task. Returns an empty object when no metadata has been set.
    public var metadata: [String: JSONValue]?
    /// Human-readable title of the task.
    public var name: String
    /// ID of the organization this task belongs to (`org_...`). `null` for tasks outside an org context.
    public var org: String??
    /// Resolved owner details including `id`, `name`, `alias`, and `profile_picture`. `null` if the task is unassigned or the owner cannot be resolved (e.g. assigned agent was deleted).
    public var ownerActor: UserTaskListResponseDataItemOwnerActor??
    /// ID of the agent assigned as owner (`agi_...`). `null` if the owner is a human user, the task is unassigned, or the assigned agent was deleted.
    public var ownerAgent: String??
    /// ID of the user assigned as owner (`usr_...`). `null` if the owner is an agent, the task is unassigned, or the assigned agent was deleted.
    public var ownerUser: String??
    /// ID of the parent task when this task is a subtask (`tsk_...`). `null` for top-level tasks. Subtasks nest exactly one level.
    public var parent: String??
    /// Priority level of the task from `0` (highest) to `4` (lowest). Defaults to `2` (medium) when not explicitly set.
    public var priority: Int?
    /// ID of the developer sandbox this task is scoped to (`dsb_...`). `null` for tasks outside a sandbox environment.
    public var sandbox: String??
    /// Source object identity (for example `ArchAstro/firstlanding`). `null` when the task has no source.
    public var sourceId: String??
    /// Container of the work this task is about (for example `github.com`). `null` when the task has no source. Set together with `source_type` and `source_id`.
    public var sourceScope: String??
    /// Kind of source object (for example `repository`). `null` when the task has no source.
    public var sourceType: String??
    /// Current status of the task. One of `"open"`, `"in_progress"`, or `"done"`.
    public var status: String
    /// Number of subtasks under this task. Computed on list/show reads; create/update responses may report 0 until the next read. Always 0 for subtasks.
    public var subtasksCount: Int?
    /// Labels for grouping and filtering, stored lowercase and de-duplicated. Empty array when untagged.
    public var tags: [String]?
    /// ID of the team that owns this task (`tem_...`). `null` if the task is not scoped to a team.
    public var team: String??
    /// ID of the thread this task is bound to (`thr_...`) the conversation it was filed from, or the thread passed at creation. `null` for tasks not tied to a thread.
    public var thread: String??
    /// When the task was last modified (ISO 8601).
    public var updatedAt: Date?
    /// ID of the user that owns this task (`usr_...`). `null` if the task is scoped to a team.
    public var user: String??

    public init(
        agent: String?? = nil,
        blockedByCount: Int? = nil,
        closedAt: Date?? = nil,
        commentsCount: Int? = nil,
        createdAt: Date? = nil,
        createdByActor: UserTaskListResponseDataItemCreatedByActor?? = nil,
        createdByAgent: String?? = nil,
        createdByUser: String?? = nil,
        currentLease: UserTaskListResponseDataItemCurrentLease?? = nil,
        description: String?? = nil,
        dueDate: Date?? = nil,
        epic: String?? = nil,
        id: String,
        isBlocked: Bool? = nil,
        links: [String: JSONValue]? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String,
        org: String?? = nil,
        ownerActor: UserTaskListResponseDataItemOwnerActor?? = nil,
        ownerAgent: String?? = nil,
        ownerUser: String?? = nil,
        parent: String?? = nil,
        priority: Int? = nil,
        sandbox: String?? = nil,
        sourceId: String?? = nil,
        sourceScope: String?? = nil,
        sourceType: String?? = nil,
        status: String,
        subtasksCount: Int? = nil,
        tags: [String]? = nil,
        team: String?? = nil,
        thread: String?? = nil,
        updatedAt: Date? = nil,
        user: String?? = nil
    ) {
        self.agent = agent
        self.blockedByCount = blockedByCount
        self.closedAt = closedAt
        self.commentsCount = commentsCount
        self.createdAt = createdAt
        self.createdByActor = createdByActor
        self.createdByAgent = createdByAgent
        self.createdByUser = createdByUser
        self.currentLease = currentLease
        self.description = description
        self.dueDate = dueDate
        self.epic = epic
        self.id = id
        self.isBlocked = isBlocked
        self.links = links
        self.metadata = metadata
        self.name = name
        self.org = org
        self.ownerActor = ownerActor
        self.ownerAgent = ownerAgent
        self.ownerUser = ownerUser
        self.parent = parent
        self.priority = priority
        self.sandbox = sandbox
        self.sourceId = sourceId
        self.sourceScope = sourceScope
        self.sourceType = sourceType
        self.status = status
        self.subtasksCount = subtasksCount
        self.tags = tags
        self.team = team
        self.thread = thread
        self.updatedAt = updatedAt
        self.user = user
    }

    private enum CodingKeys: String, CodingKey {
        case agent
        case blockedByCount = "blocked_by_count"
        case closedAt = "closed_at"
        case commentsCount = "comments_count"
        case createdAt = "created_at"
        case createdByActor = "created_by_actor"
        case createdByAgent = "created_by_agent"
        case createdByUser = "created_by_user"
        case currentLease = "current_lease"
        case description
        case dueDate = "due_date"
        case epic
        case id
        case isBlocked = "is_blocked"
        case links
        case metadata
        case name
        case org
        case ownerActor = "owner_actor"
        case ownerAgent = "owner_agent"
        case ownerUser = "owner_user"
        case parent
        case priority
        case sandbox
        case sourceId = "source_id"
        case sourceScope = "source_scope"
        case sourceType = "source_type"
        case status
        case subtasksCount = "subtasks_count"
        case tags
        case team
        case thread
        case updatedAt = "updated_at"
        case user
    }
}

/// Successful response
public struct UserTaskListResponse: Codable, Sendable {
    public var afterCursor: String??
    public var beforeCursor: String??
    /// Array of task objects matching the requested filters.
    public var data: [UserTaskListResponseDataItem]
    public var hasMore: Bool

    public init(
        afterCursor: String?? = nil,
        beforeCursor: String?? = nil,
        data: [UserTaskListResponseDataItem],
        hasMore: Bool
    ) {
        self.afterCursor = afterCursor
        self.beforeCursor = beforeCursor
        self.data = data
        self.hasMore = hasMore
    }

    private enum CodingKeys: String, CodingKey {
        case afterCursor = "after_cursor"
        case beforeCursor = "before_cursor"
        case data
        case hasMore = "has_more"
    }
}

public struct UserTaskBlockerCyclesResponseDataItemTasksItemCreatedByActorProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserTaskBlockerCyclesResponseDataItemTasksItemCreatedByActor: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserTaskBlockerCyclesResponseDataItemTasksItemCreatedByActorProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserTaskBlockerCyclesResponseDataItemTasksItemCreatedByActorProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskBlockerCyclesResponseDataItemTasksItemCurrentLease: Codable, Sendable {
    /// Server-calculated lease expiry in ISO 8601 format.
    public var expiresAt: Date
    /// Bounded harness identifier for the coding session.
    public var harness: String
    /// Display name supplied by the coding session that holds the lease.
    public var sessionName: String

    public init(expiresAt: Date, harness: String, sessionName: String) {
        self.expiresAt = expiresAt
        self.harness = harness
        self.sessionName = sessionName
    }

    private enum CodingKeys: String, CodingKey {
        case expiresAt = "expires_at"
        case harness
        case sessionName = "session_name"
    }
}

public struct UserTaskBlockerCyclesResponseDataItemTasksItemOwnerActorProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserTaskBlockerCyclesResponseDataItemTasksItemOwnerActor: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserTaskBlockerCyclesResponseDataItemTasksItemOwnerActorProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserTaskBlockerCyclesResponseDataItemTasksItemOwnerActorProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskBlockerCyclesResponseDataItemTasksItem: Codable, Sendable {
    /// ID of the agent that owns this task (`agi_...`). `null` if the task is scoped to a team or user.
    public var agent: String??
    /// Number of tasks marked as blocking this task, whether or not they are done (see `GET /tasks/{task}/blockers`). Computed on list/show reads; create/update responses may lag one read behind.
    public var blockedByCount: Int?
    /// When the task was marked as done or otherwise closed (ISO 8601). `null` if the task is still open.
    public var closedAt: Date??
    /// Total number of comments posted on this task.
    public var commentsCount: Int?
    /// When the task was created (ISO 8601).
    public var createdAt: Date?
    /// Resolved creator details including `id`, `name`, `alias`, and `profile_picture`. `null` if no creator is set or the creator cannot be resolved (e.g. creating agent was deleted).
    public var createdByActor: UserTaskBlockerCyclesResponseDataItemTasksItemCreatedByActor??
    /// ID of the agent that created this task (`agi_...`). `null` if the task was created by a human user, or if the creating agent was later deleted.
    public var createdByAgent: String??
    /// ID of the user who created this task (`usr_...`). `null` if the task was created by an agent, or if creator provenance was cleared after the creator was deleted.
    public var createdByUser: String??
    /// Viewer-safe live coding-session lease summary. `null` when the task is unleased or the projected lease has expired. Fencing identifiers are never included.
    public var currentLease: UserTaskBlockerCyclesResponseDataItemTasksItemCurrentLease??
    /// Long-form description or notes for the task. `null` if no description has been provided.
    public var description: String??
    /// Date and time by which the task should be completed (ISO 8601). `null` if no due date is set.
    public var dueDate: Date??
    /// Free-form grouping label. `null` when the task is not in an epic.
    public var epic: String??
    /// Task ID (`tsk_...`).
    public var id: String
    /// `true` while at least one blocking task is not yet done. Informational only a blocked task can still change status and derived at read time, so the task un-blocks automatically when its last open blocker completes. Computed on list/show reads; create/update responses report `false` until the next read.
    public var isBlocked: Bool?
    /// Key-value map of named URLs or references associated with the task. Returns an empty object when no links have been set.
    public var links: [String: JSONValue]?
    /// Arbitrary key-value map of application-specific data stored alongside the task. Returns an empty object when no metadata has been set.
    public var metadata: [String: JSONValue]?
    /// Human-readable title of the task.
    public var name: String
    /// ID of the organization this task belongs to (`org_...`). `null` for tasks outside an org context.
    public var org: String??
    /// Resolved owner details including `id`, `name`, `alias`, and `profile_picture`. `null` if the task is unassigned or the owner cannot be resolved (e.g. assigned agent was deleted).
    public var ownerActor: UserTaskBlockerCyclesResponseDataItemTasksItemOwnerActor??
    /// ID of the agent assigned as owner (`agi_...`). `null` if the owner is a human user, the task is unassigned, or the assigned agent was deleted.
    public var ownerAgent: String??
    /// ID of the user assigned as owner (`usr_...`). `null` if the owner is an agent, the task is unassigned, or the assigned agent was deleted.
    public var ownerUser: String??
    /// ID of the parent task when this task is a subtask (`tsk_...`). `null` for top-level tasks. Subtasks nest exactly one level.
    public var parent: String??
    /// Priority level of the task from `0` (highest) to `4` (lowest). Defaults to `2` (medium) when not explicitly set.
    public var priority: Int?
    /// ID of the developer sandbox this task is scoped to (`dsb_...`). `null` for tasks outside a sandbox environment.
    public var sandbox: String??
    /// Source object identity (for example `ArchAstro/firstlanding`). `null` when the task has no source.
    public var sourceId: String??
    /// Container of the work this task is about (for example `github.com`). `null` when the task has no source. Set together with `source_type` and `source_id`.
    public var sourceScope: String??
    /// Kind of source object (for example `repository`). `null` when the task has no source.
    public var sourceType: String??
    /// Current status of the task. One of `"open"`, `"in_progress"`, or `"done"`.
    public var status: String
    /// Number of subtasks under this task. Computed on list/show reads; create/update responses may report 0 until the next read. Always 0 for subtasks.
    public var subtasksCount: Int?
    /// Labels for grouping and filtering, stored lowercase and de-duplicated. Empty array when untagged.
    public var tags: [String]?
    /// ID of the team that owns this task (`tem_...`). `null` if the task is not scoped to a team.
    public var team: String??
    /// ID of the thread this task is bound to (`thr_...`) the conversation it was filed from, or the thread passed at creation. `null` for tasks not tied to a thread.
    public var thread: String??
    /// When the task was last modified (ISO 8601).
    public var updatedAt: Date?
    /// ID of the user that owns this task (`usr_...`). `null` if the task is scoped to a team.
    public var user: String??

    public init(
        agent: String?? = nil,
        blockedByCount: Int? = nil,
        closedAt: Date?? = nil,
        commentsCount: Int? = nil,
        createdAt: Date? = nil,
        createdByActor: UserTaskBlockerCyclesResponseDataItemTasksItemCreatedByActor?? = nil,
        createdByAgent: String?? = nil,
        createdByUser: String?? = nil,
        currentLease: UserTaskBlockerCyclesResponseDataItemTasksItemCurrentLease?? = nil,
        description: String?? = nil,
        dueDate: Date?? = nil,
        epic: String?? = nil,
        id: String,
        isBlocked: Bool? = nil,
        links: [String: JSONValue]? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String,
        org: String?? = nil,
        ownerActor: UserTaskBlockerCyclesResponseDataItemTasksItemOwnerActor?? = nil,
        ownerAgent: String?? = nil,
        ownerUser: String?? = nil,
        parent: String?? = nil,
        priority: Int? = nil,
        sandbox: String?? = nil,
        sourceId: String?? = nil,
        sourceScope: String?? = nil,
        sourceType: String?? = nil,
        status: String,
        subtasksCount: Int? = nil,
        tags: [String]? = nil,
        team: String?? = nil,
        thread: String?? = nil,
        updatedAt: Date? = nil,
        user: String?? = nil
    ) {
        self.agent = agent
        self.blockedByCount = blockedByCount
        self.closedAt = closedAt
        self.commentsCount = commentsCount
        self.createdAt = createdAt
        self.createdByActor = createdByActor
        self.createdByAgent = createdByAgent
        self.createdByUser = createdByUser
        self.currentLease = currentLease
        self.description = description
        self.dueDate = dueDate
        self.epic = epic
        self.id = id
        self.isBlocked = isBlocked
        self.links = links
        self.metadata = metadata
        self.name = name
        self.org = org
        self.ownerActor = ownerActor
        self.ownerAgent = ownerAgent
        self.ownerUser = ownerUser
        self.parent = parent
        self.priority = priority
        self.sandbox = sandbox
        self.sourceId = sourceId
        self.sourceScope = sourceScope
        self.sourceType = sourceType
        self.status = status
        self.subtasksCount = subtasksCount
        self.tags = tags
        self.team = team
        self.thread = thread
        self.updatedAt = updatedAt
        self.user = user
    }

    private enum CodingKeys: String, CodingKey {
        case agent
        case blockedByCount = "blocked_by_count"
        case closedAt = "closed_at"
        case commentsCount = "comments_count"
        case createdAt = "created_at"
        case createdByActor = "created_by_actor"
        case createdByAgent = "created_by_agent"
        case createdByUser = "created_by_user"
        case currentLease = "current_lease"
        case description
        case dueDate = "due_date"
        case epic
        case id
        case isBlocked = "is_blocked"
        case links
        case metadata
        case name
        case org
        case ownerActor = "owner_actor"
        case ownerAgent = "owner_agent"
        case ownerUser = "owner_user"
        case parent
        case priority
        case sandbox
        case sourceId = "source_id"
        case sourceScope = "source_scope"
        case sourceType = "source_type"
        case status
        case subtasksCount = "subtasks_count"
        case tags
        case team
        case thread
        case updatedAt = "updated_at"
        case user
    }
}

public struct UserTaskBlockerCyclesResponseDataItem: Codable, Sendable {
    /// Every unfinished task in this cyclic blocker component.
    public var tasks: [UserTaskBlockerCyclesResponseDataItemTasksItem]

    public init(tasks: [UserTaskBlockerCyclesResponseDataItemTasksItem]) {
        self.tasks = tasks
    }

    private enum CodingKeys: String, CodingKey {
        case tasks
    }
}

/// Successful response
public struct UserTaskBlockerCyclesResponse: Codable, Sendable {
    public var afterCursor: String?
    public var beforeCursor: String?
    public var data: [UserTaskBlockerCyclesResponseDataItem]
    public var hasMore: Bool

    public init(
        afterCursor: String? = nil,
        beforeCursor: String? = nil,
        data: [UserTaskBlockerCyclesResponseDataItem],
        hasMore: Bool
    ) {
        self.afterCursor = afterCursor
        self.beforeCursor = beforeCursor
        self.data = data
        self.hasMore = hasMore
    }

    private enum CodingKeys: String, CodingKey {
        case afterCursor = "after_cursor"
        case beforeCursor = "before_cursor"
        case data
        case hasMore = "has_more"
    }
}

public struct UserTaskReadyResponseDataItemTaskCreatedByActorProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserTaskReadyResponseDataItemTaskCreatedByActor: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserTaskReadyResponseDataItemTaskCreatedByActorProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserTaskReadyResponseDataItemTaskCreatedByActorProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskReadyResponseDataItemTaskCurrentLease: Codable, Sendable {
    /// Server-calculated lease expiry in ISO 8601 format.
    public var expiresAt: Date
    /// Bounded harness identifier for the coding session.
    public var harness: String
    /// Display name supplied by the coding session that holds the lease.
    public var sessionName: String

    public init(expiresAt: Date, harness: String, sessionName: String) {
        self.expiresAt = expiresAt
        self.harness = harness
        self.sessionName = sessionName
    }

    private enum CodingKeys: String, CodingKey {
        case expiresAt = "expires_at"
        case harness
        case sessionName = "session_name"
    }
}

public struct UserTaskReadyResponseDataItemTaskOwnerActorProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserTaskReadyResponseDataItemTaskOwnerActor: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserTaskReadyResponseDataItemTaskOwnerActorProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserTaskReadyResponseDataItemTaskOwnerActorProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskReadyResponseDataItemTask: Codable, Sendable {
    /// ID of the agent that owns this task (`agi_...`). `null` if the task is scoped to a team or user.
    public var agent: String??
    /// Number of tasks marked as blocking this task, whether or not they are done (see `GET /tasks/{task}/blockers`). Computed on list/show reads; create/update responses may lag one read behind.
    public var blockedByCount: Int?
    /// When the task was marked as done or otherwise closed (ISO 8601). `null` if the task is still open.
    public var closedAt: Date??
    /// Total number of comments posted on this task.
    public var commentsCount: Int?
    /// When the task was created (ISO 8601).
    public var createdAt: Date?
    /// Resolved creator details including `id`, `name`, `alias`, and `profile_picture`. `null` if no creator is set or the creator cannot be resolved (e.g. creating agent was deleted).
    public var createdByActor: UserTaskReadyResponseDataItemTaskCreatedByActor??
    /// ID of the agent that created this task (`agi_...`). `null` if the task was created by a human user, or if the creating agent was later deleted.
    public var createdByAgent: String??
    /// ID of the user who created this task (`usr_...`). `null` if the task was created by an agent, or if creator provenance was cleared after the creator was deleted.
    public var createdByUser: String??
    /// Viewer-safe live coding-session lease summary. `null` when the task is unleased or the projected lease has expired. Fencing identifiers are never included.
    public var currentLease: UserTaskReadyResponseDataItemTaskCurrentLease??
    /// Long-form description or notes for the task. `null` if no description has been provided.
    public var description: String??
    /// Date and time by which the task should be completed (ISO 8601). `null` if no due date is set.
    public var dueDate: Date??
    /// Free-form grouping label. `null` when the task is not in an epic.
    public var epic: String??
    /// Task ID (`tsk_...`).
    public var id: String
    /// `true` while at least one blocking task is not yet done. Informational only a blocked task can still change status and derived at read time, so the task un-blocks automatically when its last open blocker completes. Computed on list/show reads; create/update responses report `false` until the next read.
    public var isBlocked: Bool?
    /// Key-value map of named URLs or references associated with the task. Returns an empty object when no links have been set.
    public var links: [String: JSONValue]?
    /// Arbitrary key-value map of application-specific data stored alongside the task. Returns an empty object when no metadata has been set.
    public var metadata: [String: JSONValue]?
    /// Human-readable title of the task.
    public var name: String
    /// ID of the organization this task belongs to (`org_...`). `null` for tasks outside an org context.
    public var org: String??
    /// Resolved owner details including `id`, `name`, `alias`, and `profile_picture`. `null` if the task is unassigned or the owner cannot be resolved (e.g. assigned agent was deleted).
    public var ownerActor: UserTaskReadyResponseDataItemTaskOwnerActor??
    /// ID of the agent assigned as owner (`agi_...`). `null` if the owner is a human user, the task is unassigned, or the assigned agent was deleted.
    public var ownerAgent: String??
    /// ID of the user assigned as owner (`usr_...`). `null` if the owner is an agent, the task is unassigned, or the assigned agent was deleted.
    public var ownerUser: String??
    /// ID of the parent task when this task is a subtask (`tsk_...`). `null` for top-level tasks. Subtasks nest exactly one level.
    public var parent: String??
    /// Priority level of the task from `0` (highest) to `4` (lowest). Defaults to `2` (medium) when not explicitly set.
    public var priority: Int?
    /// ID of the developer sandbox this task is scoped to (`dsb_...`). `null` for tasks outside a sandbox environment.
    public var sandbox: String??
    /// Source object identity (for example `ArchAstro/firstlanding`). `null` when the task has no source.
    public var sourceId: String??
    /// Container of the work this task is about (for example `github.com`). `null` when the task has no source. Set together with `source_type` and `source_id`.
    public var sourceScope: String??
    /// Kind of source object (for example `repository`). `null` when the task has no source.
    public var sourceType: String??
    /// Current status of the task. One of `"open"`, `"in_progress"`, or `"done"`.
    public var status: String
    /// Number of subtasks under this task. Computed on list/show reads; create/update responses may report 0 until the next read. Always 0 for subtasks.
    public var subtasksCount: Int?
    /// Labels for grouping and filtering, stored lowercase and de-duplicated. Empty array when untagged.
    public var tags: [String]?
    /// ID of the team that owns this task (`tem_...`). `null` if the task is not scoped to a team.
    public var team: String??
    /// ID of the thread this task is bound to (`thr_...`) the conversation it was filed from, or the thread passed at creation. `null` for tasks not tied to a thread.
    public var thread: String??
    /// When the task was last modified (ISO 8601).
    public var updatedAt: Date?
    /// ID of the user that owns this task (`usr_...`). `null` if the task is scoped to a team.
    public var user: String??

    public init(
        agent: String?? = nil,
        blockedByCount: Int? = nil,
        closedAt: Date?? = nil,
        commentsCount: Int? = nil,
        createdAt: Date? = nil,
        createdByActor: UserTaskReadyResponseDataItemTaskCreatedByActor?? = nil,
        createdByAgent: String?? = nil,
        createdByUser: String?? = nil,
        currentLease: UserTaskReadyResponseDataItemTaskCurrentLease?? = nil,
        description: String?? = nil,
        dueDate: Date?? = nil,
        epic: String?? = nil,
        id: String,
        isBlocked: Bool? = nil,
        links: [String: JSONValue]? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String,
        org: String?? = nil,
        ownerActor: UserTaskReadyResponseDataItemTaskOwnerActor?? = nil,
        ownerAgent: String?? = nil,
        ownerUser: String?? = nil,
        parent: String?? = nil,
        priority: Int? = nil,
        sandbox: String?? = nil,
        sourceId: String?? = nil,
        sourceScope: String?? = nil,
        sourceType: String?? = nil,
        status: String,
        subtasksCount: Int? = nil,
        tags: [String]? = nil,
        team: String?? = nil,
        thread: String?? = nil,
        updatedAt: Date? = nil,
        user: String?? = nil
    ) {
        self.agent = agent
        self.blockedByCount = blockedByCount
        self.closedAt = closedAt
        self.commentsCount = commentsCount
        self.createdAt = createdAt
        self.createdByActor = createdByActor
        self.createdByAgent = createdByAgent
        self.createdByUser = createdByUser
        self.currentLease = currentLease
        self.description = description
        self.dueDate = dueDate
        self.epic = epic
        self.id = id
        self.isBlocked = isBlocked
        self.links = links
        self.metadata = metadata
        self.name = name
        self.org = org
        self.ownerActor = ownerActor
        self.ownerAgent = ownerAgent
        self.ownerUser = ownerUser
        self.parent = parent
        self.priority = priority
        self.sandbox = sandbox
        self.sourceId = sourceId
        self.sourceScope = sourceScope
        self.sourceType = sourceType
        self.status = status
        self.subtasksCount = subtasksCount
        self.tags = tags
        self.team = team
        self.thread = thread
        self.updatedAt = updatedAt
        self.user = user
    }

    private enum CodingKeys: String, CodingKey {
        case agent
        case blockedByCount = "blocked_by_count"
        case closedAt = "closed_at"
        case commentsCount = "comments_count"
        case createdAt = "created_at"
        case createdByActor = "created_by_actor"
        case createdByAgent = "created_by_agent"
        case createdByUser = "created_by_user"
        case currentLease = "current_lease"
        case description
        case dueDate = "due_date"
        case epic
        case id
        case isBlocked = "is_blocked"
        case links
        case metadata
        case name
        case org
        case ownerActor = "owner_actor"
        case ownerAgent = "owner_agent"
        case ownerUser = "owner_user"
        case parent
        case priority
        case sandbox
        case sourceId = "source_id"
        case sourceScope = "source_scope"
        case sourceType = "source_type"
        case status
        case subtasksCount = "subtasks_count"
        case tags
        case team
        case thread
        case updatedAt = "updated_at"
        case user
    }
}

public struct UserTaskReadyResponseDataItem: Codable, Sendable {
    /// One of `ready`, `blocked`, or `leased`.
    public var readiness: String
    /// Stable exclusion reason: `open_blockers` or `active_lease`; omitted when ready.
    public var reason: String??
    /// The task evaluated for readiness.
    public var task: UserTaskReadyResponseDataItemTask

    public init(
        readiness: String,
        reason: String?? = nil,
        task: UserTaskReadyResponseDataItemTask
    ) {
        self.readiness = readiness
        self.reason = reason
        self.task = task
    }

    private enum CodingKeys: String, CodingKey {
        case readiness
        case reason
        case task
    }
}

/// Successful response
public struct UserTaskReadyResponse: Codable, Sendable {
    public var afterCursor: String?
    /// Always false because projections can lag writes and a later claim can race this read.
    public var authoritative: Bool
    public var beforeCursor: String?
    public var data: [UserTaskReadyResponseDataItem]
    public var hasMore: Bool

    public init(
        afterCursor: String? = nil,
        authoritative: Bool,
        beforeCursor: String? = nil,
        data: [UserTaskReadyResponseDataItem],
        hasMore: Bool
    ) {
        self.afterCursor = afterCursor
        self.authoritative = authoritative
        self.beforeCursor = beforeCursor
        self.data = data
        self.hasMore = hasMore
    }

    private enum CodingKeys: String, CodingKey {
        case afterCursor = "after_cursor"
        case authoritative
        case beforeCursor = "before_cursor"
        case data
        case hasMore = "has_more"
    }
}

public struct UserTaskSearchResponseDataItemCreatedByActorProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserTaskSearchResponseDataItemCreatedByActor: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserTaskSearchResponseDataItemCreatedByActorProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserTaskSearchResponseDataItemCreatedByActorProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskSearchResponseDataItemCurrentLease: Codable, Sendable {
    /// Server-calculated lease expiry in ISO 8601 format.
    public var expiresAt: Date
    /// Bounded harness identifier for the coding session.
    public var harness: String
    /// Display name supplied by the coding session that holds the lease.
    public var sessionName: String

    public init(expiresAt: Date, harness: String, sessionName: String) {
        self.expiresAt = expiresAt
        self.harness = harness
        self.sessionName = sessionName
    }

    private enum CodingKeys: String, CodingKey {
        case expiresAt = "expires_at"
        case harness
        case sessionName = "session_name"
    }
}

public struct UserTaskSearchResponseDataItemOwnerActorProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserTaskSearchResponseDataItemOwnerActor: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserTaskSearchResponseDataItemOwnerActorProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserTaskSearchResponseDataItemOwnerActorProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserTaskSearchResponseDataItem: Codable, Sendable {
    /// ID of the agent that owns this task (`agi_...`). `null` if the task is scoped to a team or user.
    public var agent: String??
    /// Number of tasks marked as blocking this task, whether or not they are done (see `GET /tasks/{task}/blockers`). Computed on list/show reads; create/update responses may lag one read behind.
    public var blockedByCount: Int?
    /// When the task was marked as done or otherwise closed (ISO 8601). `null` if the task is still open.
    public var closedAt: Date??
    /// Total number of comments posted on this task.
    public var commentsCount: Int?
    /// When the task was created (ISO 8601).
    public var createdAt: Date?
    /// Resolved creator details including `id`, `name`, `alias`, and `profile_picture`. `null` if no creator is set or the creator cannot be resolved (e.g. creating agent was deleted).
    public var createdByActor: UserTaskSearchResponseDataItemCreatedByActor??
    /// ID of the agent that created this task (`agi_...`). `null` if the task was created by a human user, or if the creating agent was later deleted.
    public var createdByAgent: String??
    /// ID of the user who created this task (`usr_...`). `null` if the task was created by an agent, or if creator provenance was cleared after the creator was deleted.
    public var createdByUser: String??
    /// Viewer-safe live coding-session lease summary. `null` when the task is unleased or the projected lease has expired. Fencing identifiers are never included.
    public var currentLease: UserTaskSearchResponseDataItemCurrentLease??
    /// Long-form description or notes for the task. `null` if no description has been provided.
    public var description: String??
    /// Date and time by which the task should be completed (ISO 8601). `null` if no due date is set.
    public var dueDate: Date??
    /// Free-form grouping label. `null` when the task is not in an epic.
    public var epic: String??
    /// Task ID (`tsk_...`).
    public var id: String
    /// `true` while at least one blocking task is not yet done. Informational only a blocked task can still change status and derived at read time, so the task un-blocks automatically when its last open blocker completes. Computed on list/show reads; create/update responses report `false` until the next read.
    public var isBlocked: Bool?
    /// Key-value map of named URLs or references associated with the task. Returns an empty object when no links have been set.
    public var links: [String: JSONValue]?
    /// Arbitrary key-value map of application-specific data stored alongside the task. Returns an empty object when no metadata has been set.
    public var metadata: [String: JSONValue]?
    /// Human-readable title of the task.
    public var name: String
    /// ID of the organization this task belongs to (`org_...`). `null` for tasks outside an org context.
    public var org: String??
    /// Resolved owner details including `id`, `name`, `alias`, and `profile_picture`. `null` if the task is unassigned or the owner cannot be resolved (e.g. assigned agent was deleted).
    public var ownerActor: UserTaskSearchResponseDataItemOwnerActor??
    /// ID of the agent assigned as owner (`agi_...`). `null` if the owner is a human user, the task is unassigned, or the assigned agent was deleted.
    public var ownerAgent: String??
    /// ID of the user assigned as owner (`usr_...`). `null` if the owner is an agent, the task is unassigned, or the assigned agent was deleted.
    public var ownerUser: String??
    /// ID of the parent task when this task is a subtask (`tsk_...`). `null` for top-level tasks. Subtasks nest exactly one level.
    public var parent: String??
    /// Priority level of the task from `0` (highest) to `4` (lowest). Defaults to `2` (medium) when not explicitly set.
    public var priority: Int?
    /// ID of the developer sandbox this task is scoped to (`dsb_...`). `null` for tasks outside a sandbox environment.
    public var sandbox: String??
    /// Source object identity (for example `ArchAstro/firstlanding`). `null` when the task has no source.
    public var sourceId: String??
    /// Container of the work this task is about (for example `github.com`). `null` when the task has no source. Set together with `source_type` and `source_id`.
    public var sourceScope: String??
    /// Kind of source object (for example `repository`). `null` when the task has no source.
    public var sourceType: String??
    /// Current status of the task. One of `"open"`, `"in_progress"`, or `"done"`.
    public var status: String
    /// Number of subtasks under this task. Computed on list/show reads; create/update responses may report 0 until the next read. Always 0 for subtasks.
    public var subtasksCount: Int?
    /// Labels for grouping and filtering, stored lowercase and de-duplicated. Empty array when untagged.
    public var tags: [String]?
    /// ID of the team that owns this task (`tem_...`). `null` if the task is not scoped to a team.
    public var team: String??
    /// ID of the thread this task is bound to (`thr_...`) the conversation it was filed from, or the thread passed at creation. `null` for tasks not tied to a thread.
    public var thread: String??
    /// When the task was last modified (ISO 8601).
    public var updatedAt: Date?
    /// ID of the user that owns this task (`usr_...`). `null` if the task is scoped to a team.
    public var user: String??

    public init(
        agent: String?? = nil,
        blockedByCount: Int? = nil,
        closedAt: Date?? = nil,
        commentsCount: Int? = nil,
        createdAt: Date? = nil,
        createdByActor: UserTaskSearchResponseDataItemCreatedByActor?? = nil,
        createdByAgent: String?? = nil,
        createdByUser: String?? = nil,
        currentLease: UserTaskSearchResponseDataItemCurrentLease?? = nil,
        description: String?? = nil,
        dueDate: Date?? = nil,
        epic: String?? = nil,
        id: String,
        isBlocked: Bool? = nil,
        links: [String: JSONValue]? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String,
        org: String?? = nil,
        ownerActor: UserTaskSearchResponseDataItemOwnerActor?? = nil,
        ownerAgent: String?? = nil,
        ownerUser: String?? = nil,
        parent: String?? = nil,
        priority: Int? = nil,
        sandbox: String?? = nil,
        sourceId: String?? = nil,
        sourceScope: String?? = nil,
        sourceType: String?? = nil,
        status: String,
        subtasksCount: Int? = nil,
        tags: [String]? = nil,
        team: String?? = nil,
        thread: String?? = nil,
        updatedAt: Date? = nil,
        user: String?? = nil
    ) {
        self.agent = agent
        self.blockedByCount = blockedByCount
        self.closedAt = closedAt
        self.commentsCount = commentsCount
        self.createdAt = createdAt
        self.createdByActor = createdByActor
        self.createdByAgent = createdByAgent
        self.createdByUser = createdByUser
        self.currentLease = currentLease
        self.description = description
        self.dueDate = dueDate
        self.epic = epic
        self.id = id
        self.isBlocked = isBlocked
        self.links = links
        self.metadata = metadata
        self.name = name
        self.org = org
        self.ownerActor = ownerActor
        self.ownerAgent = ownerAgent
        self.ownerUser = ownerUser
        self.parent = parent
        self.priority = priority
        self.sandbox = sandbox
        self.sourceId = sourceId
        self.sourceScope = sourceScope
        self.sourceType = sourceType
        self.status = status
        self.subtasksCount = subtasksCount
        self.tags = tags
        self.team = team
        self.thread = thread
        self.updatedAt = updatedAt
        self.user = user
    }

    private enum CodingKeys: String, CodingKey {
        case agent
        case blockedByCount = "blocked_by_count"
        case closedAt = "closed_at"
        case commentsCount = "comments_count"
        case createdAt = "created_at"
        case createdByActor = "created_by_actor"
        case createdByAgent = "created_by_agent"
        case createdByUser = "created_by_user"
        case currentLease = "current_lease"
        case description
        case dueDate = "due_date"
        case epic
        case id
        case isBlocked = "is_blocked"
        case links
        case metadata
        case name
        case org
        case ownerActor = "owner_actor"
        case ownerAgent = "owner_agent"
        case ownerUser = "owner_user"
        case parent
        case priority
        case sandbox
        case sourceId = "source_id"
        case sourceScope = "source_scope"
        case sourceType = "source_type"
        case status
        case subtasksCount = "subtasks_count"
        case tags
        case team
        case thread
        case updatedAt = "updated_at"
        case user
    }
}

/// Successful response
public struct UserTaskSearchResponse: Codable, Sendable {
    public var afterCursor: String?
    public var beforeCursor: String?
    /// Array of task objects matching the query and filters.
    public var data: [UserTaskSearchResponseDataItem]
    public var hasMore: Bool
    public var query: String

    public init(
        afterCursor: String? = nil,
        beforeCursor: String? = nil,
        data: [UserTaskSearchResponseDataItem],
        hasMore: Bool,
        query: String
    ) {
        self.afterCursor = afterCursor
        self.beforeCursor = beforeCursor
        self.data = data
        self.hasMore = hasMore
        self.query = query
    }

    private enum CodingKeys: String, CodingKey {
        case afterCursor = "after_cursor"
        case beforeCursor = "before_cursor"
        case data
        case hasMore = "has_more"
        case query
    }
}

public struct UserThreadListResponseDataItemParentMessageAclAddItem: Codable, Sendable {
    /// Array of action strings the principal is permitted to perform, e.g. `["read", "write"]`. Must contain at least one entry.
    public var actions: [String]
    /// The identifier of the principal. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`; omit entirely when `principal_type` is `"everyone"`.
    public var principal: String?
    /// The kind of principal receiving the grant. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.
    public var principalType: String

    public init(actions: [String], principal: String? = nil, principalType: String) {
        self.actions = actions
        self.principal = principal
        self.principalType = principalType
    }

    private enum CodingKeys: String, CodingKey {
        case actions
        case principal
        case principalType = "principal_type"
    }
}

public struct UserThreadListResponseDataItemParentMessageAclGrantsItem: Codable, Sendable {
    /// Array of action strings the principal is permitted to perform, e.g. `["read", "write"]`. Must contain at least one entry.
    public var actions: [String]
    /// The identifier of the principal. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`; omit entirely when `principal_type` is `"everyone"`.
    public var principal: String?
    /// The kind of principal receiving the grant. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.
    public var principalType: String

    public init(actions: [String], principal: String? = nil, principalType: String) {
        self.actions = actions
        self.principal = principal
        self.principalType = principalType
    }

    private enum CodingKeys: String, CodingKey {
        case actions
        case principal
        case principalType = "principal_type"
    }
}

public struct UserThreadListResponseDataItemParentMessageAclRemoveItem: Codable, Sendable {
    /// The identifier of the principal to remove. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`. Omit when `principal_type` is `"everyone"`.
    public var principal: String?
    /// The kind of principal to remove. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.
    public var principalType: String

    public init(principal: String? = nil, principalType: String) {
        self.principal = principal
        self.principalType = principalType
    }

    private enum CodingKeys: String, CodingKey {
        case principal
        case principalType = "principal_type"
    }
}

public struct UserThreadListResponseDataItemParentMessageAcl: Codable, Sendable {
    /// Patch mode: grants to add or merge into the existing list. Cannot be combined with `grants`.
    public var add: [UserThreadListResponseDataItemParentMessageAclAddItem]?
    /// Replace mode: the complete new list of grants that replaces all existing entries. Send an empty array (`[]`) to clear all grants. Cannot be combined with `add` or `remove`.
    public var grants: [UserThreadListResponseDataItemParentMessageAclGrantsItem]?
    /// Patch mode: principals whose grants should be removed from the existing list. Cannot be combined with `grants`.
    public var remove: [UserThreadListResponseDataItemParentMessageAclRemoveItem]?

    public init(
        add: [UserThreadListResponseDataItemParentMessageAclAddItem]? = nil,
        grants: [UserThreadListResponseDataItemParentMessageAclGrantsItem]? = nil,
        remove: [UserThreadListResponseDataItemParentMessageAclRemoveItem]? = nil
    ) {
        self.add = add
        self.grants = grants
        self.remove = remove
    }

    private enum CodingKeys: String, CodingKey {
        case add
        case grants
        case remove
    }
}

public struct UserThreadListResponseDataItemParentMessageActorsItemProfilePicture: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserThreadListResponseDataItemParentMessageActorsItem: Codable, Sendable {
    /// Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured.
    public var alias: String??
    /// Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.
    public var id: String??
    /// Display name of the actor shown in the UI. `null` if no name is set.
    public var name: String??
    /// Profile picture for the actor. `null` if the actor has no profile picture.
    public var profilePicture: UserThreadListResponseDataItemParentMessageActorsItemProfilePicture??

    public init(
        alias: String?? = nil,
        id: String?? = nil,
        name: String?? = nil,
        profilePicture: UserThreadListResponseDataItemParentMessageActorsItemProfilePicture?? = nil
    ) {
        self.alias = alias
        self.id = id
        self.name = name
        self.profilePicture = profilePicture
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case id
        case name
        case profilePicture = "profile_picture"
    }
}

public struct UserThreadListResponseDataItemParentMessageAttachmentsItemImageSource: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserThreadListResponseDataItemParentMessageAttachmentsItemVariantsItemImageSource: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserThreadListResponseDataItemParentMessageAttachmentsItemVariantsItem: Codable, Sendable {
    /// MIME type of this variant's file (e.g., `"image/jpeg"`, `"video/mp4"`). `null` if the file is not loaded.
    public var contentType: String??
    /// When this variant was created (ISO 8601).
    public var createdAt: Date?
    /// ID of the underlying storage file that backs this variant (`fil_...`).
    public var file: String?
    /// Original filename of the uploaded file for this variant. `null` if the file is not loaded.
    public var filename: String??
    /// Height of this variant in pixels. `null` if not recorded.
    public var height: Int??
    /// Media variant ID (`mvr_...`).
    public var id: String
    /// Resolved image delivery metadata for this variant, including dimensions and CDN URL. `null` for non-image content types.
    public var imageSource: UserThreadListResponseDataItemParentMessageAttachmentsItemVariantsItemImageSource??
    /// When this variant was last updated (ISO 8601).
    public var updatedAt: Date?
    /// Signed download URL for this variant, resolved at request time. `null` if the file is unavailable.
    public var url: String??
    /// Identifier for this variant's processing tier. Common values include `"original"` (the unmodified upload) and `"thumbnail"` (a resized preview).
    public var variantKey: String?
    /// Width of this variant in pixels. `null` if not recorded.
    public var width: Int??

    public init(
        contentType: String?? = nil,
        createdAt: Date? = nil,
        file: String? = nil,
        filename: String?? = nil,
        height: Int?? = nil,
        id: String,
        imageSource: UserThreadListResponseDataItemParentMessageAttachmentsItemVariantsItemImageSource?? = nil,
        updatedAt: Date? = nil,
        url: String?? = nil,
        variantKey: String? = nil,
        width: Int?? = nil
    ) {
        self.contentType = contentType
        self.createdAt = createdAt
        self.file = file
        self.filename = filename
        self.height = height
        self.id = id
        self.imageSource = imageSource
        self.updatedAt = updatedAt
        self.url = url
        self.variantKey = variantKey
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case contentType = "content_type"
        case createdAt = "created_at"
        case file
        case filename
        case height
        case id
        case imageSource = "image_source"
        case updatedAt = "updated_at"
        case url
        case variantKey = "variant_key"
        case width
    }
}

public struct UserThreadListResponseDataItemParentMessageAttachmentsItem: Codable, Sendable {
    /// MIME type of the attached file, e.g. `"image/png"` or `"application/pdf"`. Present on `file`, `artifact`, and `media` types. `null` otherwise.
    public var contentType: String??
    /// Short description. The page meta-description for `scraped_link`, the artifact description for `artifact`, and the task description for `task` types. `null` on other types.
    public var description: String??
    /// Original filename of the attached file, e.g. `"report.pdf"`. Present on `file`, `artifact`, and `media` types. `null` otherwise.
    public var filename: String??
    /// Height in pixels of the media item. Present on `media` type only. `null` otherwise.
    public var height: Int??
    /// Unique identifier for this attachment within the message.
    public var id: String
    /// Height in pixels of the scraped preview image. Present on `scraped_link` type only. `null` otherwise.
    public var imageHeight: Int??
    /// Image source metadata for inline rendering. Present on `file`, `scraped_link`, `artifact`, and `media` types when the content is an image. `null` otherwise.
    public var imageSource: UserThreadListResponseDataItemParentMessageAttachmentsItemImageSource??
    /// URL of the preview image extracted from the scraped page. Present on `scraped_link` type only. `null` otherwise.
    public var imageUrl: String??
    /// Width in pixels of the scraped preview image. Present on `scraped_link` type only. `null` otherwise.
    public var imageWidth: Int??
    /// The media category, e.g. `"video"` or `"audio"`. Present on `media` type only; omitted otherwise.
    public var mediaType: String?
    /// Display name of the media item. Present on `media` type only. `null` otherwise.
    public var name: String??
    /// The full embedded object payload. For `task` type, contains the task record. For `action` type, contains the action definition. For `chart` type, contains the chart with its inline `spec`. Omitted on other types.
    public var object: [String: JSONValue]?
    /// Display title. The page title for `scraped_link`, the artifact name for `artifact`, and the task title for `task` types. `null` on other types.
    public var title: String??
    /// The attachment type. One of `"file"`, `"scraped_link"`, `"artifact"`, `"task"`, `"media"`, `"action"`, or `"chart"`. Determines which additional fields are present.
    public var type: String
    /// URL to access the resource. A signed download URL for `file` and `artifact` types; the original URL for `scraped_link`; a media playback URL for `media`. `null` on `task` and `action` types.
    public var url: String??
    /// Array of available encoding variants for the media item (e.g. different resolutions). Present on `media` type only; omitted otherwise.
    public var variants: [UserThreadListResponseDataItemParentMessageAttachmentsItemVariantsItem]?
    /// Version number of the attached artifact at the time of attachment. Present on `artifact` type only. `null` otherwise.
    public var version: Int??
    /// Width in pixels of the media item. Present on `media` type only. `null` otherwise.
    public var width: Int??

    public init(
        contentType: String?? = nil,
        description: String?? = nil,
        filename: String?? = nil,
        height: Int?? = nil,
        id: String,
        imageHeight: Int?? = nil,
        imageSource: UserThreadListResponseDataItemParentMessageAttachmentsItemImageSource?? = nil,
        imageUrl: String?? = nil,
        imageWidth: Int?? = nil,
        mediaType: String? = nil,
        name: String?? = nil,
        object: [String: JSONValue]? = nil,
        title: String?? = nil,
        type: String,
        url: String?? = nil,
        variants: [UserThreadListResponseDataItemParentMessageAttachmentsItemVariantsItem]? = nil,
        version: Int?? = nil,
        width: Int?? = nil
    ) {
        self.contentType = contentType
        self.description = description
        self.filename = filename
        self.height = height
        self.id = id
        self.imageHeight = imageHeight
        self.imageSource = imageSource
        self.imageUrl = imageUrl
        self.imageWidth = imageWidth
        self.mediaType = mediaType
        self.name = name
        self.object = object
        self.title = title
        self.type = type
        self.url = url
        self.variants = variants
        self.version = version
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case contentType = "content_type"
        case description
        case filename
        case height
        case id
        case imageHeight = "image_height"
        case imageSource = "image_source"
        case imageUrl = "image_url"
        case imageWidth = "image_width"
        case mediaType = "media_type"
        case name
        case object
        case title
        case type
        case url
        case variants
        case version
        case width
    }
}

public struct UserThreadListResponseDataItemParentMessageReactionsItem: Codable, Sendable {
    /// Type-specific reaction data. For `"emoji_reaction"` reactions, contains an `emoji` key with the Unicode emoji string (e.g., `" "`).
    public var payload: [String: JSONValue]?
    /// Reaction type identifier. Currently always `"emoji_reaction"` for emoji-based reactions.
    public var type: String
    /// Public ID of the user who added the reaction (`usr_...`).
    public var user: String?

    public init(payload: [String: JSONValue]? = nil, type: String, user: String? = nil) {
        self.payload = payload
        self.type = type
        self.user = user
    }

    private enum CodingKeys: String, CodingKey {
        case payload
        case type
        case user
    }
}

public struct UserThreadListResponseDataItemParentMessage: Codable, Sendable {
    /// Access control list for private messages (grants with `read` action). Only returned to resource owners (and privileged/org-admin viewers) via server-side `field_redactions: [acl: :owner]`; `null` for everyone else.
    public var acl: UserThreadListResponseDataItemParentMessageAcl??
    /// Resolved actor descriptors for the message sender, combining identity and display metadata. Always contains exactly one entry.
    public var actors: [UserThreadListResponseDataItemParentMessageActorsItem]?
    /// ID of the agent user that sent this message (`agi_...`). `null` for messages sent by human users.
    public var agent: String??
    /// Local agent execution mode for this message. One of `cli`, `embedded`, or `null` when the message was not created by a local agent execution path.
    public var agentMode: String??
    /// Files, links, tasks, media, artifacts, and actions attached to this message. Empty array if there are no attachments.
    public var attachments: [UserThreadListResponseDataItemParentMessageAttachmentsItem]?
    /// ID of the thread that was branched from this message (`thr_...`). `null` if this message has not spawned a branch thread.
    public var branchedThread: String??
    /// Text content of the message. `null` for messages that contain only attachments.
    public var content: String??
    /// When the message was posted (ISO 8601).
    public var createdAt: String?
    /// Whether this message has at least one reply. Only present when explicitly requested or computed by the server.
    public var hasReplies: Bool?
    /// Message ID (`msg_...`).
    public var id: String
    /// Client-supplied idempotency key used to deduplicate message sends. `null` if the sender did not provide one.
    public var idempotencyKey: String??
    /// Whether this message is a deletion tombstone. `true` only on the `message_updated` broadcast emitted when a message is deleted: the original content is replaced with a placeholder and the message no longer exists on the server. Always `false` for live messages.
    public var isDeleted: Bool?
    /// Identifier of the legacy chat agent that sent this message, if applicable. `null` for messages sent by users or modern agent users.
    public var legacyAgent: String??
    /// Arbitrary key-value metadata attached to the message. Always present; defaults to an empty object when no metadata has been set.
    public var metadata: [String: JSONValue]?
    /// ID of the organization that owns this message (`org_...`).
    public var org: String??
    /// Emoji and other reactions added to this message by users. Empty array if no reactions have been added or the association is not preloaded.
    public var reactions: [UserThreadListResponseDataItemParentMessageReactionsItem]?
    /// Display hint for how the message should be rendered. One of `"reply"`, `"direct"`, or `"inline"`. `null` for user-authored messages, which are always rendered as standard replies.
    public var renderingMode: String??
    /// Inline array of reply messages, each serialized as a full message object. Only present when the server has preloaded replies for this message.
    public var replies: [[String: JSONValue]]?
    /// Opaque pagination cursor to fetch replies posted after the current page. Only present when inline replies are included in the response.
    public var repliesAfterCursor: String??
    /// Opaque pagination cursor to fetch replies posted before the current page. Only present when inline replies are included in the response.
    public var repliesBeforeCursor: String??
    /// Total number of direct replies to this message. Only present when explicitly requested or computed by the server.
    public var replyCount: Int?
    /// The parent message this message is a reply to, expanded as a full message object when loaded. `null` if this is a top-level message or the association is not preloaded.
    public var replyTo: [String: JSONValue]??
    /// ID of the root message in this reply chain (`msg_...`). `null` for a top-level message. The value is persisted when the reply is created, so callers can correlate a multi-turn session without walking parent messages.
    public var rootMessageId: String??
    /// ID of the developer sandbox this message belongs to (`dsb_...`). `null` for non-sandbox messages.
    public var sandbox: String??
    /// ID of the team this message is scoped to (`tem_...`). `null` if the message is not team-scoped.
    public var team: String??
    /// ID of the thread this message belongs to (`thr_...`).
    public var thread: String?
    /// Optional client-defined classification for the message (for example `note` or `status`). Free-form string up to 64 characters. The value `system` is reserved for platform-authored messages and cannot be set by clients. `null` when unset.
    public var type: String??
    /// The human user who sent this message. Returns a public ID string (`usr_...`) when the association is not preloaded, or an expanded user object when it is. `null` for messages sent by agents.
    public var user: JSONValue??
    /// Message-level visibility. `default` is visible to anyone who can see the parent thread. `private` is restricted to the sender and explicit ACL `read` grantees.
    public var visibility: String?

    public init(
        acl: UserThreadListResponseDataItemParentMessageAcl?? = nil,
        actors: [UserThreadListResponseDataItemParentMessageActorsItem]? = nil,
        agent: String?? = nil,
        agentMode: String?? = nil,
        attachments: [UserThreadListResponseDataItemParentMessageAttachmentsItem]? = nil,
        branchedThread: String?? = nil,
        content: String?? = nil,
        createdAt: String? = nil,
        hasReplies: Bool? = nil,
        id: String,
        idempotencyKey: String?? = nil,
        isDeleted: Bool? = nil,
        legacyAgent: String?? = nil,
        metadata: [String: JSONValue]? = nil,
        org: String?? = nil,
        reactions: [UserThreadListResponseDataItemParentMessageReactionsItem]? = nil,
        renderingMode: String?? = nil,
        replies: [[String: JSONValue]]? = nil,
        repliesAfterCursor: String?? = nil,
        repliesBeforeCursor: String?? = nil,
        replyCount: Int? = nil,
        replyTo: [String: JSONValue]?? = nil,
        rootMessageId: String?? = nil,
        sandbox: String?? = nil,
        team: String?? = nil,
        thread: String? = nil,
        type: String?? = nil,
        user: JSONValue?? = nil,
        visibility: String? = nil
    ) {
        self.acl = acl
        self.actors = actors
        self.agent = agent
        self.agentMode = agentMode
        self.attachments = attachments
        self.branchedThread = branchedThread
        self.content = content
        self.createdAt = createdAt
        self.hasReplies = hasReplies
        self.id = id
        self.idempotencyKey = idempotencyKey
        self.isDeleted = isDeleted
        self.legacyAgent = legacyAgent
        self.metadata = metadata
        self.org = org
        self.reactions = reactions
        self.renderingMode = renderingMode
        self.replies = replies
        self.repliesAfterCursor = repliesAfterCursor
        self.repliesBeforeCursor = repliesBeforeCursor
        self.replyCount = replyCount
        self.replyTo = replyTo
        self.rootMessageId = rootMessageId
        self.sandbox = sandbox
        self.team = team
        self.thread = thread
        self.type = type
        self.user = user
        self.visibility = visibility
    }

    private enum CodingKeys: String, CodingKey {
        case acl
        case actors
        case agent
        case agentMode = "agent_mode"
        case attachments
        case branchedThread = "branched_thread"
        case content
        case createdAt = "created_at"
        case hasReplies = "has_replies"
        case id
        case idempotencyKey = "idempotency_key"
        case isDeleted = "is_deleted"
        case legacyAgent = "legacy_agent"
        case metadata
        case org
        case reactions
        case renderingMode = "rendering_mode"
        case replies
        case repliesAfterCursor = "replies_after_cursor"
        case repliesBeforeCursor = "replies_before_cursor"
        case replyCount = "reply_count"
        case replyTo = "reply_to"
        case rootMessageId = "root_message_id"
        case sandbox
        case team
        case thread
        case type
        case user
        case visibility
    }
}

public struct UserThreadListResponseDataItemParticipantsItem: Codable, Sendable {
    /// Short handle or alias for the user. `null` if not set.
    public var alias: String??
    /// ID of the app this user (and their access token) is scoped to (`dap_...`). `null` if the user is not scoped to an app.
    public var app: String??
    /// Display name of the user's app. `null` when the app association was not preloaded by the caller.
    public var appName: String??
    /// Agent user that created this account (`usr_...`). `null` unless an agent created it.
    public var createdByAgentUser: String??
    /// Developer account that created this user (`dva_...`). `null` unless created via a developer token.
    public var createdByDeveloper: String??
    /// Org of the principal that created this user (`org_...`). `null` on legacy rows.
    public var createdByOrg: String??
    /// Team that created this user (`tem_...`). `null` unless created as a team.
    public var createdByTeam: String??
    /// User who created this account (`usr_...`). `null` on self-signup or legacy rows.
    public var createdByUser: String??
    /// Email address of the user.
    public var email: String??
    /// User ID (`usr_...`).
    public var id: String
    /// `true` if this account is an internal system user rather than a human. System users are created automatically by the platform.
    public var isSystemUser: Bool?
    /// Arbitrary key-value metadata attached to the user. Defaults to an empty object.
    public var metadata: [String: JSONValue]?
    /// Full display name of the user. `null` if the user has not set a name.
    public var name: String??
    /// ID of the organization this user belongs to (`org_...`). `null` if the user is not a member of any organization.
    public var org: String??
    /// Display name of the user's organization. `null` when the user is not in an org, or when the org association was not preloaded by the caller.
    public var orgName: String??
    /// Role of the user within their organization. One of `"admin"`, `"member"`, or `"viewer"`. `null` when the user is not a member of any organization.
    public var orgRole: String??
    /// Stable workspace slug for the user's organization. `null` when the user is not in an org, or when the org association was not preloaded by the caller.
    public var orgSlug: String??
    /// ID of the sandbox environment this user is scoped to (`sbx_...`). `null` for production users.
    public var sandbox: String??
    /// Display name of the user's sandbox environment. `null` for production users, or when the sandbox association was not preloaded by the caller.
    public var sandboxName: String??

    public init(
        alias: String?? = nil,
        app: String?? = nil,
        appName: String?? = nil,
        createdByAgentUser: String?? = nil,
        createdByDeveloper: String?? = nil,
        createdByOrg: String?? = nil,
        createdByTeam: String?? = nil,
        createdByUser: String?? = nil,
        email: String?? = nil,
        id: String,
        isSystemUser: Bool? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String?? = nil,
        org: String?? = nil,
        orgName: String?? = nil,
        orgRole: String?? = nil,
        orgSlug: String?? = nil,
        sandbox: String?? = nil,
        sandboxName: String?? = nil
    ) {
        self.alias = alias
        self.app = app
        self.appName = appName
        self.createdByAgentUser = createdByAgentUser
        self.createdByDeveloper = createdByDeveloper
        self.createdByOrg = createdByOrg
        self.createdByTeam = createdByTeam
        self.createdByUser = createdByUser
        self.email = email
        self.id = id
        self.isSystemUser = isSystemUser
        self.metadata = metadata
        self.name = name
        self.org = org
        self.orgName = orgName
        self.orgRole = orgRole
        self.orgSlug = orgSlug
        self.sandbox = sandbox
        self.sandboxName = sandboxName
    }

    private enum CodingKeys: String, CodingKey {
        case alias
        case app
        case appName = "app_name"
        case createdByAgentUser = "created_by_agent_user"
        case createdByDeveloper = "created_by_developer"
        case createdByOrg = "created_by_org"
        case createdByTeam = "created_by_team"
        case createdByUser = "created_by_user"
        case email
        case id
        case isSystemUser = "is_system_user"
        case metadata
        case name
        case org
        case orgName = "org_name"
        case orgRole = "org_role"
        case orgSlug = "org_slug"
        case sandbox
        case sandboxName = "sandbox_name"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemAclAddItem: Codable, Sendable {
    /// Array of action strings the principal is permitted to perform, e.g. `["read", "write"]`. Must contain at least one entry.
    public var actions: [String]
    /// The identifier of the principal. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`; omit entirely when `principal_type` is `"everyone"`.
    public var principal: String?
    /// The kind of principal receiving the grant. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.
    public var principalType: String

    public init(actions: [String], principal: String? = nil, principalType: String) {
        self.actions = actions
        self.principal = principal
        self.principalType = principalType
    }

    private enum CodingKeys: String, CodingKey {
        case actions
        case principal
        case principalType = "principal_type"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemAclGrantsItem: Codable, Sendable {
    /// Array of action strings the principal is permitted to perform, e.g. `["read", "write"]`. Must contain at least one entry.
    public var actions: [String]
    /// The identifier of the principal. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`; omit entirely when `principal_type` is `"everyone"`.
    public var principal: String?
    /// The kind of principal receiving the grant. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.
    public var principalType: String

    public init(actions: [String], principal: String? = nil, principalType: String) {
        self.actions = actions
        self.principal = principal
        self.principalType = principalType
    }

    private enum CodingKeys: String, CodingKey {
        case actions
        case principal
        case principalType = "principal_type"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemAclRemoveItem: Codable, Sendable {
    /// The identifier of the principal to remove. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`. Omit when `principal_type` is `"everyone"`.
    public var principal: String?
    /// The kind of principal to remove. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.
    public var principalType: String

    public init(principal: String? = nil, principalType: String) {
        self.principal = principal
        self.principalType = principalType
    }

    private enum CodingKeys: String, CodingKey {
        case principal
        case principalType = "principal_type"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemAcl: Codable, Sendable {
    /// Patch mode: grants to add or merge into the existing list. Cannot be combined with `grants`.
    public var add: [UserThreadListResponseDataItemParticipatingAgentsItemAclAddItem]?
    /// Replace mode: the complete new list of grants that replaces all existing entries. Send an empty array (`[]`) to clear all grants. Cannot be combined with `add` or `remove`.
    public var grants: [UserThreadListResponseDataItemParticipatingAgentsItemAclGrantsItem]?
    /// Patch mode: principals whose grants should be removed from the existing list. Cannot be combined with `grants`.
    public var remove: [UserThreadListResponseDataItemParticipatingAgentsItemAclRemoveItem]?

    public init(
        add: [UserThreadListResponseDataItemParticipatingAgentsItemAclAddItem]? = nil,
        grants: [UserThreadListResponseDataItemParticipatingAgentsItemAclGrantsItem]? = nil,
        remove: [UserThreadListResponseDataItemParticipatingAgentsItemAclRemoveItem]? = nil
    ) {
        self.add = add
        self.grants = grants
        self.remove = remove
    }

    private enum CodingKeys: String, CodingKey {
        case add
        case grants
        case remove
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionOrgLogo: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContractParticipantsItem: Codable, Sendable {
    /// Workflow-authored explanation of the slot's role. `null` when the workflow declares none.
    public var description: String??
    /// The slot's name, as referenced by the workflow. Supply the chosen agent under the top-level `participants[name]` field when invoking.
    public var name: String
    /// Whether the workflow requires this slot to be filled for the run to complete its embedded stages.
    public var required: Bool
    /// The kind of principal the slot accepts. Currently always `"agent_user"` the value supplied at invoke is an agent ID (`agi_...`).
    public var type: String

    public init(description: String?? = nil, name: String, required: Bool, type: String) {
        self.description = description
        self.name = name
        self.required = required
        self.type = type
    }

    private enum CodingKeys: String, CodingKey {
        case description
        case name
        case required
        case type
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContractPrefills: Codable, Sendable {
    /// Participant slot-to-agent mappings applied by the platform. Caller values at these slots must match exactly.
    public var participants: [String: JSONValue]?
    /// Partial invocation payload applied by the platform. A caller may omit these values, but supplying a different value at any locked path is rejected.
    public var payload: [String: JSONValue]?

    public init(participants: [String: JSONValue]? = nil, payload: [String: JSONValue]? = nil) {
        self.participants = participants
        self.payload = payload
    }

    private enum CodingKeys: String, CodingKey {
        case participants
        case payload
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContract: Codable, Sendable {
    /// JSON Schema validated against the whole invoke payload, from the automation's `input_schema_config`. `null` when none is configured.
    public var inputSchema: [String: JSONValue]??
    /// Named participant slots declared by the workflow, sorted by name. `null` when the workflow declares none. Values supplied under the top-level `participants` field are agent IDs.
    public var participants: [UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContractParticipantsItem]??
    /// Owner-controlled payload and participant values the platform applies to every invocation. Supplying a conflicting value is rejected.
    public var prefills: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContractPrefills

    public init(
        inputSchema: [String: JSONValue]?? = nil,
        participants: [UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContractParticipantsItem]?? = nil,
        prefills: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContractPrefills
    ) {
        self.inputSchema = inputSchema
        self.participants = participants
        self.prefills = prefills
    }

    private enum CodingKeys: String, CodingKey {
        case inputSchema = "input_schema"
        case participants
        case prefills
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetails: Codable, Sendable {
    /// Automation execution type (`invoked`, `scheduled`, or `trigger`). `null` when the template body does not declare one.
    public var automationType: String??
    /// Schema-driven payload and participant inputs for an invoked automation. Used by installation clients to collect locked prefills before provisioning. `null` for non-invoked automation types.
    public var invokeContract: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContract??
    /// Template-details discriminator. Always `automation` for this variant.
    public var type: String

    public init(
        automationType: String?? = nil,
        invokeContract: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetailsInvokeContract?? = nil,
        type: String
    ) {
        self.automationType = automationType
        self.invokeContract = invokeContract
        self.type = type
    }

    private enum CodingKeys: String, CodingKey {
        case automationType = "automation_type"
        case invokeContract = "invoke_contract"
        case type
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItem: Codable, Sendable {
    /// Short prose blurb from the template body's `description:` field. `null` when the body doesn't set one. Used as the card subhead in the Library carousel.
    public var description: String??
    /// Template-kind-specific details selected by the `type` discriminator. `null` when this template kind has no additional details.
    public var details: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetails??
    /// Human-facing label from the template body's `display_name:` field. `null` when the body doesn't set one. Library carousels use this for the card title, falling back to a humanized `name`.
    public var displayName: String??
    /// Template config ID (`cfg_...`). `null` for inline-only templates.
    public var id: String??
    /// Template config kind, or `SolutionTemplateRef` / `SolutionTemplatePath` when unresolved.
    public var kind: String
    /// Lookup key stamped on the template config at import time. `null` when no lookup key was assigned.
    public var lookupKey: String??
    /// Canonical name from the template body. For `AgentTemplate` this doubles as the human-facing label; for `AgentToolTemplate` it's the LLM-facing tool function identifier (snake_case); for `AgentRoutineTemplate` it's the routine identifier (kebab-case). Clients rendering carousels should prefer `display_name` and fall back to humanizing `name`.
    public var name: String??
    /// Relative path to the public README endpoint with a signed token already embedded, scoped to this template's bundled markdown asset. `null` when the Solution body's `templates[].readme_path` is unset for this entry. Token expires in 1 hour refresh via `GET /api/v1/solutions/:solution`.
    public var readmeUrl: String??
    /// Stable virtual path assigned to the template config. `null` when no virtual path was set.
    public var virtualPath: String??

    public init(
        description: String?? = nil,
        details: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItemDetails?? = nil,
        displayName: String?? = nil,
        id: String?? = nil,
        kind: String,
        lookupKey: String?? = nil,
        name: String?? = nil,
        readmeUrl: String?? = nil,
        virtualPath: String?? = nil
    ) {
        self.description = description
        self.details = details
        self.displayName = displayName
        self.id = id
        self.kind = kind
        self.lookupKey = lookupKey
        self.name = name
        self.readmeUrl = readmeUrl
        self.virtualPath = virtualPath
    }

    private enum CodingKeys: String, CodingKey {
        case description
        case details
        case displayName = "display_name"
        case id
        case kind
        case lookupKey = "lookup_key"
        case name
        case readmeUrl = "readme_url"
        case virtualPath = "virtual_path"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolution: Codable, Sendable {
    /// Category tag keys declared in the Solution body, used to group Solutions in the catalog. An empty array when the body declares none.
    public var categoryKeys: [String]?
    /// When the Solution config was first imported (ISO 8601).
    public var createdAt: String?
    /// Short tagline or summary declared in the Solution body, used as the card subhead in catalog UIs. `null` when the Solution body does not set one.
    public var description: String??
    /// Custom analytics events declared in the Solution body's `events:` manifest a map of event key (snake_case) to its definition (`label`, optional `description`, optional typed `fields`). Dashboards use the `label` as the event's display name. Present as an empty object when the body declares none.
    public var events: [String: JSONValue]?
    /// Solution config ID (`cfg_...`).
    public var id: String
    /// Absolute URL of the Solution's cover image the bundled asset the body's `image:` field names. A stable, non-expiring capability URL (like `org_logo.url`), safe to hold in caches and OpenGraph tags; it 404s if the Solution stops declaring a cover. `null` when the Solution has no cover image, and always `null` for org-scoped rows the permanent URL is minted for system-scope (catalog) Solutions only.
    public var imageUrl: String??
    /// Resource type. Always `"Solution"`.
    public var kind: String
    /// When `upgrade_available` is `true`, the system-scope Solution config ID (`cfg_...`) that should be used as the upgrade source. `null` otherwise.
    public var latestSolution: String??
    /// When `upgrade_available` is `true`, the higher system-scope `solution_version` available to upgrade to. `null` otherwise.
    public var latestVersion: String??
    /// The lookup key stored on the Solution config, if one was assigned during import. `null` when no lookup key was set.
    public var lookupKey: String??
    /// Arbitrary key-value metadata declared in the Solution body (e.g. category or display hints). Present as an empty object when the body declares none.
    public var metadata: [String: JSONValue]?
    /// Human-facing display name declared in the Solution body. `null` when the Solution body does not set one.
    public var name: String??
    /// Organization ID (`org_...`) that owns this Solution config, when the Solution is scoped to a specific org. `null` for system-scope (app-level) Solutions.
    public var org: String??
    /// Canonical image-source object for the resolved `org`'s logo, used as the principal category section glyph. The `url` is a stable, non-expiring capability URL (`refresh_url` is `null` there is nothing to refresh). `null` when `org_slug` is `null` or the org has no logo.
    public var orgLogo: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionOrgLogo??
    /// Display name of the resolved `org`. Pairs with `org_slug` as the principal catalog category's label. `null` when `org_slug` is `null`.
    public var orgName: String??
    /// Resolved slug of the Solution body's `org` (the publishing organization), when set and it resolves to a real org visible to the viewer. When present this is the Solution's principal catalog category key clients group the Solution under this org ahead of `category_keys`. `null` when the body has no `org` or it doesn't resolve.
    public var orgSlug: String??
    /// Owner scopes this Solution appears under. Members: `"system"` (app-level system scope) and/or `"org"` (viewer's org scope).
    public var owners: [String]
    /// Relative path to the public README endpoint with a signed token already embedded. `null` when the Solution has no README. Token expires in 1 hour refresh via `GET /api/v1/solutions/:solution`.
    public var readmeUrl: String??
    /// Absolute URLs of the Solution's gallery screenshots the bundled assets the body's `screenshots:` field names, in declared order. Each is a stable, non-expiring capability URL with the same cacheability contract as `image_url` (one shared token, a `v` cache key, and a `file` param selecting the screenshot); a URL 404s if the Solution stops declaring its screenshot. An empty array when the Solution declares none, and always empty for org-scoped rows the permanent URLs are minted for system-scope (catalog) Solutions only.
    public var screenshotUrls: [String]?
    /// Stable UUID declared in the Solution body, used to identify the same logical Solution across multiple installed copies and owner scopes. `null` when the body omits it.
    public var solutionId: String??
    /// Semver string declared in the Solution body (e.g. `"1.2.0"`). `null` when the body does not declare a version.
    public var solutionVersion: String??
    /// Freeform tag keys declared in the Solution body. An empty array when the body declares none.
    public var tagKeys: [String]?
    /// Wrapped template kind `"AgentTemplate"`, `"AutomationTemplate"`, `"AgentRoutineTemplate"`, `"AgentToolTemplate"`, `"AgentComputerTemplate"`, or `"SolutionTemplateRef"` for ref-mode bundles.
    public var templateKind: String??
    /// Template configs bundled by this Solution, in declaration order the first entry is the deployable template the Solution wraps; the rest are sibling templates the wrapped template references.
    public var templates: [UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItem]
    /// When the Solution config was last modified (ISO 8601).
    public var updatedAt: String?
    /// `true` when this Solution is installed at the viewer's org scope and the app-level system scope carries a higher `solution_version`. Always `false` for system-only rows.
    public var upgradeAvailable: Bool
    /// The stable virtual path assigned to this Solution config, used as the deduplication key when the same Solution appears under multiple owner scopes. `null` when unset.
    public var virtualPath: String??

    public init(
        categoryKeys: [String]? = nil,
        createdAt: String? = nil,
        description: String?? = nil,
        events: [String: JSONValue]? = nil,
        id: String,
        imageUrl: String?? = nil,
        kind: String,
        latestSolution: String?? = nil,
        latestVersion: String?? = nil,
        lookupKey: String?? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String?? = nil,
        org: String?? = nil,
        orgLogo: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionOrgLogo?? = nil,
        orgName: String?? = nil,
        orgSlug: String?? = nil,
        owners: [String],
        readmeUrl: String?? = nil,
        screenshotUrls: [String]? = nil,
        solutionId: String?? = nil,
        solutionVersion: String?? = nil,
        tagKeys: [String]? = nil,
        templateKind: String?? = nil,
        templates: [UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolutionTemplatesItem],
        updatedAt: String? = nil,
        upgradeAvailable: Bool,
        virtualPath: String?? = nil
    ) {
        self.categoryKeys = categoryKeys
        self.createdAt = createdAt
        self.description = description
        self.events = events
        self.id = id
        self.imageUrl = imageUrl
        self.kind = kind
        self.latestSolution = latestSolution
        self.latestVersion = latestVersion
        self.lookupKey = lookupKey
        self.metadata = metadata
        self.name = name
        self.org = org
        self.orgLogo = orgLogo
        self.orgName = orgName
        self.orgSlug = orgSlug
        self.owners = owners
        self.readmeUrl = readmeUrl
        self.screenshotUrls = screenshotUrls
        self.solutionId = solutionId
        self.solutionVersion = solutionVersion
        self.tagKeys = tagKeys
        self.templateKind = templateKind
        self.templates = templates
        self.updatedAt = updatedAt
        self.upgradeAvailable = upgradeAvailable
        self.virtualPath = virtualPath
    }

    private enum CodingKeys: String, CodingKey {
        case categoryKeys = "category_keys"
        case createdAt = "created_at"
        case description
        case events
        case id
        case imageUrl = "image_url"
        case kind
        case latestSolution = "latest_solution"
        case latestVersion = "latest_version"
        case lookupKey = "lookup_key"
        case metadata
        case name
        case org
        case orgLogo = "org_logo"
        case orgName = "org_name"
        case orgSlug = "org_slug"
        case owners
        case readmeUrl = "readme_url"
        case screenshotUrls = "screenshot_urls"
        case solutionId = "solution_id"
        case solutionVersion = "solution_version"
        case tagKeys = "tag_keys"
        case templateKind = "template_kind"
        case templates
        case updatedAt = "updated_at"
        case upgradeAvailable = "upgrade_available"
        case virtualPath = "virtual_path"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionOrgLogo: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContractParticipantsItem: Codable, Sendable {
    /// Workflow-authored explanation of the slot's role. `null` when the workflow declares none.
    public var description: String??
    /// The slot's name, as referenced by the workflow. Supply the chosen agent under the top-level `participants[name]` field when invoking.
    public var name: String
    /// Whether the workflow requires this slot to be filled for the run to complete its embedded stages.
    public var required: Bool
    /// The kind of principal the slot accepts. Currently always `"agent_user"` the value supplied at invoke is an agent ID (`agi_...`).
    public var type: String

    public init(description: String?? = nil, name: String, required: Bool, type: String) {
        self.description = description
        self.name = name
        self.required = required
        self.type = type
    }

    private enum CodingKeys: String, CodingKey {
        case description
        case name
        case required
        case type
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContractPrefills: Codable, Sendable {
    /// Participant slot-to-agent mappings applied by the platform. Caller values at these slots must match exactly.
    public var participants: [String: JSONValue]?
    /// Partial invocation payload applied by the platform. A caller may omit these values, but supplying a different value at any locked path is rejected.
    public var payload: [String: JSONValue]?

    public init(participants: [String: JSONValue]? = nil, payload: [String: JSONValue]? = nil) {
        self.participants = participants
        self.payload = payload
    }

    private enum CodingKeys: String, CodingKey {
        case participants
        case payload
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContract: Codable, Sendable {
    /// JSON Schema validated against the whole invoke payload, from the automation's `input_schema_config`. `null` when none is configured.
    public var inputSchema: [String: JSONValue]??
    /// Named participant slots declared by the workflow, sorted by name. `null` when the workflow declares none. Values supplied under the top-level `participants` field are agent IDs.
    public var participants: [UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContractParticipantsItem]??
    /// Owner-controlled payload and participant values the platform applies to every invocation. Supplying a conflicting value is rejected.
    public var prefills: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContractPrefills

    public init(
        inputSchema: [String: JSONValue]?? = nil,
        participants: [UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContractParticipantsItem]?? = nil,
        prefills: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContractPrefills
    ) {
        self.inputSchema = inputSchema
        self.participants = participants
        self.prefills = prefills
    }

    private enum CodingKeys: String, CodingKey {
        case inputSchema = "input_schema"
        case participants
        case prefills
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetails: Codable, Sendable {
    /// Automation execution type (`invoked`, `scheduled`, or `trigger`). `null` when the template body does not declare one.
    public var automationType: String??
    /// Schema-driven payload and participant inputs for an invoked automation. Used by installation clients to collect locked prefills before provisioning. `null` for non-invoked automation types.
    public var invokeContract: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContract??
    /// Template-details discriminator. Always `automation` for this variant.
    public var type: String

    public init(
        automationType: String?? = nil,
        invokeContract: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetailsInvokeContract?? = nil,
        type: String
    ) {
        self.automationType = automationType
        self.invokeContract = invokeContract
        self.type = type
    }

    private enum CodingKeys: String, CodingKey {
        case automationType = "automation_type"
        case invokeContract = "invoke_contract"
        case type
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItem: Codable, Sendable {
    /// Short prose blurb from the template body's `description:` field. `null` when the body doesn't set one. Used as the card subhead in the Library carousel.
    public var description: String??
    /// Template-kind-specific details selected by the `type` discriminator. `null` when this template kind has no additional details.
    public var details: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetails??
    /// Human-facing label from the template body's `display_name:` field. `null` when the body doesn't set one. Library carousels use this for the card title, falling back to a humanized `name`.
    public var displayName: String??
    /// Template config ID (`cfg_...`). `null` for inline-only templates.
    public var id: String??
    /// Template config kind, or `SolutionTemplateRef` / `SolutionTemplatePath` when unresolved.
    public var kind: String
    /// Lookup key stamped on the template config at import time. `null` when no lookup key was assigned.
    public var lookupKey: String??
    /// Canonical name from the template body. For `AgentTemplate` this doubles as the human-facing label; for `AgentToolTemplate` it's the LLM-facing tool function identifier (snake_case); for `AgentRoutineTemplate` it's the routine identifier (kebab-case). Clients rendering carousels should prefer `display_name` and fall back to humanizing `name`.
    public var name: String??
    /// Relative path to the public README endpoint with a signed token already embedded, scoped to this template's bundled markdown asset. `null` when the Solution body's `templates[].readme_path` is unset for this entry. Token expires in 1 hour refresh via `GET /api/v1/solutions/:solution`.
    public var readmeUrl: String??
    /// Stable virtual path assigned to the template config. `null` when no virtual path was set.
    public var virtualPath: String??

    public init(
        description: String?? = nil,
        details: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItemDetails?? = nil,
        displayName: String?? = nil,
        id: String?? = nil,
        kind: String,
        lookupKey: String?? = nil,
        name: String?? = nil,
        readmeUrl: String?? = nil,
        virtualPath: String?? = nil
    ) {
        self.description = description
        self.details = details
        self.displayName = displayName
        self.id = id
        self.kind = kind
        self.lookupKey = lookupKey
        self.name = name
        self.readmeUrl = readmeUrl
        self.virtualPath = virtualPath
    }

    private enum CodingKeys: String, CodingKey {
        case description
        case details
        case displayName = "display_name"
        case id
        case kind
        case lookupKey = "lookup_key"
        case name
        case readmeUrl = "readme_url"
        case virtualPath = "virtual_path"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolution: Codable, Sendable {
    /// Category tag keys declared in the Solution body, used to group Solutions in the catalog. An empty array when the body declares none.
    public var categoryKeys: [String]?
    /// When the Solution config was first imported (ISO 8601).
    public var createdAt: String?
    /// Short tagline or summary declared in the Solution body, used as the card subhead in catalog UIs. `null` when the Solution body does not set one.
    public var description: String??
    /// Custom analytics events declared in the Solution body's `events:` manifest a map of event key (snake_case) to its definition (`label`, optional `description`, optional typed `fields`). Dashboards use the `label` as the event's display name. Present as an empty object when the body declares none.
    public var events: [String: JSONValue]?
    /// Solution config ID (`cfg_...`).
    public var id: String
    /// Absolute URL of the Solution's cover image the bundled asset the body's `image:` field names. A stable, non-expiring capability URL (like `org_logo.url`), safe to hold in caches and OpenGraph tags; it 404s if the Solution stops declaring a cover. `null` when the Solution has no cover image, and always `null` for org-scoped rows the permanent URL is minted for system-scope (catalog) Solutions only.
    public var imageUrl: String??
    /// Resource type. Always `"Solution"`.
    public var kind: String
    /// When `upgrade_available` is `true`, the system-scope Solution config ID (`cfg_...`) that should be used as the upgrade source. `null` otherwise.
    public var latestSolution: String??
    /// When `upgrade_available` is `true`, the higher system-scope `solution_version` available to upgrade to. `null` otherwise.
    public var latestVersion: String??
    /// The lookup key stored on the Solution config, if one was assigned during import. `null` when no lookup key was set.
    public var lookupKey: String??
    /// Arbitrary key-value metadata declared in the Solution body (e.g. category or display hints). Present as an empty object when the body declares none.
    public var metadata: [String: JSONValue]?
    /// Human-facing display name declared in the Solution body. `null` when the Solution body does not set one.
    public var name: String??
    /// Organization ID (`org_...`) that owns this Solution config, when the Solution is scoped to a specific org. `null` for system-scope (app-level) Solutions.
    public var org: String??
    /// Canonical image-source object for the resolved `org`'s logo, used as the principal category section glyph. The `url` is a stable, non-expiring capability URL (`refresh_url` is `null` there is nothing to refresh). `null` when `org_slug` is `null` or the org has no logo.
    public var orgLogo: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionOrgLogo??
    /// Display name of the resolved `org`. Pairs with `org_slug` as the principal catalog category's label. `null` when `org_slug` is `null`.
    public var orgName: String??
    /// Resolved slug of the Solution body's `org` (the publishing organization), when set and it resolves to a real org visible to the viewer. When present this is the Solution's principal catalog category key clients group the Solution under this org ahead of `category_keys`. `null` when the body has no `org` or it doesn't resolve.
    public var orgSlug: String??
    /// Owner scopes this Solution appears under. Members: `"system"` (app-level system scope) and/or `"org"` (viewer's org scope).
    public var owners: [String]
    /// Relative path to the public README endpoint with a signed token already embedded. `null` when the Solution has no README. Token expires in 1 hour refresh via `GET /api/v1/solutions/:solution`.
    public var readmeUrl: String??
    /// Absolute URLs of the Solution's gallery screenshots the bundled assets the body's `screenshots:` field names, in declared order. Each is a stable, non-expiring capability URL with the same cacheability contract as `image_url` (one shared token, a `v` cache key, and a `file` param selecting the screenshot); a URL 404s if the Solution stops declaring its screenshot. An empty array when the Solution declares none, and always empty for org-scoped rows the permanent URLs are minted for system-scope (catalog) Solutions only.
    public var screenshotUrls: [String]?
    /// Stable UUID declared in the Solution body, used to identify the same logical Solution across multiple installed copies and owner scopes. `null` when the body omits it.
    public var solutionId: String??
    /// Semver string declared in the Solution body (e.g. `"1.2.0"`). `null` when the body does not declare a version.
    public var solutionVersion: String??
    /// Freeform tag keys declared in the Solution body. An empty array when the body declares none.
    public var tagKeys: [String]?
    /// Wrapped template kind `"AgentTemplate"`, `"AutomationTemplate"`, `"AgentRoutineTemplate"`, `"AgentToolTemplate"`, `"AgentComputerTemplate"`, or `"SolutionTemplateRef"` for ref-mode bundles.
    public var templateKind: String??
    /// Template configs bundled by this Solution, in declaration order the first entry is the deployable template the Solution wraps; the rest are sibling templates the wrapped template references.
    public var templates: [UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItem]
    /// When the Solution config was last modified (ISO 8601).
    public var updatedAt: String?
    /// `true` when this Solution is installed at the viewer's org scope and the app-level system scope carries a higher `solution_version`. Always `false` for system-only rows.
    public var upgradeAvailable: Bool
    /// The stable virtual path assigned to this Solution config, used as the deduplication key when the same Solution appears under multiple owner scopes. `null` when unset.
    public var virtualPath: String??

    public init(
        categoryKeys: [String]? = nil,
        createdAt: String? = nil,
        description: String?? = nil,
        events: [String: JSONValue]? = nil,
        id: String,
        imageUrl: String?? = nil,
        kind: String,
        latestSolution: String?? = nil,
        latestVersion: String?? = nil,
        lookupKey: String?? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String?? = nil,
        org: String?? = nil,
        orgLogo: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionOrgLogo?? = nil,
        orgName: String?? = nil,
        orgSlug: String?? = nil,
        owners: [String],
        readmeUrl: String?? = nil,
        screenshotUrls: [String]? = nil,
        solutionId: String?? = nil,
        solutionVersion: String?? = nil,
        tagKeys: [String]? = nil,
        templateKind: String?? = nil,
        templates: [UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolutionTemplatesItem],
        updatedAt: String? = nil,
        upgradeAvailable: Bool,
        virtualPath: String?? = nil
    ) {
        self.categoryKeys = categoryKeys
        self.createdAt = createdAt
        self.description = description
        self.events = events
        self.id = id
        self.imageUrl = imageUrl
        self.kind = kind
        self.latestSolution = latestSolution
        self.latestVersion = latestVersion
        self.lookupKey = lookupKey
        self.metadata = metadata
        self.name = name
        self.org = org
        self.orgLogo = orgLogo
        self.orgName = orgName
        self.orgSlug = orgSlug
        self.owners = owners
        self.readmeUrl = readmeUrl
        self.screenshotUrls = screenshotUrls
        self.solutionId = solutionId
        self.solutionVersion = solutionVersion
        self.tagKeys = tagKeys
        self.templateKind = templateKind
        self.templates = templates
        self.updatedAt = updatedAt
        self.upgradeAvailable = upgradeAvailable
        self.virtualPath = virtualPath
    }

    private enum CodingKeys: String, CodingKey {
        case categoryKeys = "category_keys"
        case createdAt = "created_at"
        case description
        case events
        case id
        case imageUrl = "image_url"
        case kind
        case latestSolution = "latest_solution"
        case latestVersion = "latest_version"
        case lookupKey = "lookup_key"
        case metadata
        case name
        case org
        case orgLogo = "org_logo"
        case orgName = "org_name"
        case orgSlug = "org_slug"
        case owners
        case readmeUrl = "readme_url"
        case screenshotUrls = "screenshot_urls"
        case solutionId = "solution_id"
        case solutionVersion = "solution_version"
        case tagKeys = "tag_keys"
        case templateKind = "template_kind"
        case templates
        case updatedAt = "updated_at"
        case upgradeAvailable = "upgrade_available"
        case virtualPath = "virtual_path"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionTemplate: Codable, Sendable {
    /// When this template config was created (ISO 8601).
    public var createdAt: Date?
    /// Description of the template from the config body. `null` if the current version has no `description` field.
    public var description: String??
    /// Human-readable display name from the config body. `null` if the current version has no `display_name` field.
    public var displayName: String??
    /// Template config ID (`cfg_...`).
    public var id: String
    /// Config kind identifier for this template (e.g. `"agent_tool_template"`).
    public var kind: String
    /// Stable lookup key assigned to this template config. `null` if no lookup key is set.
    public var lookupKey: String??
    /// Template name as stored in the config body. `null` if the current version has no `name` field.
    public var name: String??
    /// When this template config was last modified (ISO 8601).
    public var updatedAt: Date?
    /// Virtual filesystem path for this template config. `null` if not set.
    public var virtualPath: String??

    public init(
        createdAt: Date? = nil,
        description: String?? = nil,
        displayName: String?? = nil,
        id: String,
        kind: String,
        lookupKey: String?? = nil,
        name: String?? = nil,
        updatedAt: Date? = nil,
        virtualPath: String?? = nil
    ) {
        self.createdAt = createdAt
        self.description = description
        self.displayName = displayName
        self.id = id
        self.kind = kind
        self.lookupKey = lookupKey
        self.name = name
        self.updatedAt = updatedAt
        self.virtualPath = virtualPath
    }

    private enum CodingKeys: String, CodingKey {
        case createdAt = "created_at"
        case description
        case displayName = "display_name"
        case id
        case kind
        case lookupKey = "lookup_key"
        case name
        case updatedAt = "updated_at"
        case virtualPath = "virtual_path"
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItemSourceSolution: Codable, Sendable {
    /// Summary of the current parent Solution config row. `solution` is the pinned Solution version the agent points at; `current_solution` is the source Solution config row as it exists now.
    public var currentSolution: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolution?
    /// Summary of the parent Solution, including `upgrade_available`, `latest_version`, and `latest_solution` when a newer system-scoped version is available for the agent's org-scoped Solution.
    public var solution: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolution
    /// Summary of the AgentTemplate config (`cfg_...`) the agent was last provisioned or updated from.
    public var template: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionTemplate

    public init(
        currentSolution: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionCurrentSolution? = nil,
        solution: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionSolution,
        template: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolutionTemplate
    ) {
        self.currentSolution = currentSolution
        self.solution = solution
        self.template = template
    }

    private enum CodingKeys: String, CodingKey {
        case currentSolution = "current_solution"
        case solution
        case template
    }
}

public struct UserThreadListResponseDataItemParticipatingAgentsItem: Codable, Sendable {
    /// Access control list for the agent. Contains a `grants` array where each entry specifies `principal_type`, `principal`, and `actions`. `null` when no ACL restrictions are applied and the agent is accessible to all members of its scope.
    public var acl: UserThreadListResponseDataItemParticipatingAgentsItemAcl??
    /// ID of the application that owns this agent (`dap_...`).
    public var app: String?
    /// When the agent was created (ISO 8601).
    public var createdAt: String?
    /// Default LLM model identifier used by this agent when no model is specified at runtime (e.g. `"claude-3-7-sonnet-latest"`).
    public var defaultModel: String??
    /// Human-readable description of what the agent does. `null` if not set.
    public var description: String??
    /// Email address provisioned for this agent. `null` if email delivery is not configured.
    public var email: String??
    /// Agent ID (`agi_...`).
    public var id: String
    /// System-level identity prompt that shapes the agent's persona and behavior.
    public var identity: String??
    /// ID of the AgentTemplate config (`cfg_...`) this agent was last provisioned or updated from. `null` for manually created agents.
    public var lastAppliedTemplateConfig: String??
    /// Stable, user-defined identifier for this agent within the application. Unique per app.
    public var lookupKey: String??
    /// Arbitrary key-value metadata attached to the agent. Not interpreted by the platform.
    public var metadata: [String: JSONValue]?
    /// Human-readable display name for the agent. `null` if not set.
    public var name: String?
    /// ID of the organization this agent belongs to (`org_...`). `null` if the agent is not org-scoped.
    public var org: String??
    /// Display name of the organization this agent belongs to. `null` when the agent is not org-scoped or when the org association was not preloaded.
    public var orgName: String??
    /// Free-form label identifying the source or author that created this agent (e.g. a username or pipeline name).
    public var originator: String??
    /// Phone number provisioned for this agent. `null` if SMS is not configured.
    public var phoneNumber: String??
    /// ID of the sandbox environment this agent is scoped to (`dsb_...`). `null` in production deployments.
    public var sandbox: String??
    /// Source Solution and AgentTemplate summary for agents provisioned from a Solution. Includes `upgrade_available`, `latest_version`, and `latest_solution` so you can render an upgrade badge without a separate dry-run call. `null` for hand-built agents and agents whose tracked template or parent Solution has been deleted. Populated only on single-agent GET responses, never on list endpoints.
    public var sourceSolution: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolution??
    /// ID of the team that owns this agent (`tem_...`). `null` if the agent is not team-scoped.
    public var team: String??
    /// True when the agent's last-applied template version is behind the current version of its AgentTemplate config i.e. reapplying the template (a per-agent upgrade) would bring it newer Solution content. Self-clears once the agent is reapplied. Computed on both the list endpoints and single-agent GET. Distinct from `source_solution.upgrade_available`, which compares Solution *versions*: an agent can lag its template (`template_upgrade_available: true`) while the org already holds the latest Solution version (`upgrade_available: false`).
    public var templateUpgradeAvailable: Bool??
    /// When the agent was last modified (ISO 8601).
    public var updatedAt: String?
    /// ID of the user that owns this agent (`usr_...`). `null` if the agent is not user-scoped.
    public var user: String??

    public init(
        acl: UserThreadListResponseDataItemParticipatingAgentsItemAcl?? = nil,
        app: String? = nil,
        createdAt: String? = nil,
        defaultModel: String?? = nil,
        description: String?? = nil,
        email: String?? = nil,
        id: String,
        identity: String?? = nil,
        lastAppliedTemplateConfig: String?? = nil,
        lookupKey: String?? = nil,
        metadata: [String: JSONValue]? = nil,
        name: String? = nil,
        org: String?? = nil,
        orgName: String?? = nil,
        originator: String?? = nil,
        phoneNumber: String?? = nil,
        sandbox: String?? = nil,
        sourceSolution: UserThreadListResponseDataItemParticipatingAgentsItemSourceSolution?? = nil,
        team: String?? = nil,
        templateUpgradeAvailable: Bool?? = nil,
        updatedAt: String? = nil,
        user: String?? = nil
    ) {
        self.acl = acl
        self.app = app
        self.createdAt = createdAt
        self.defaultModel = defaultModel
        self.description = description
        self.email = email
        self.id = id
        self.identity = identity
        self.lastAppliedTemplateConfig = lastAppliedTemplateConfig
        self.lookupKey = lookupKey
        self.metadata = metadata
        self.name = name
        self.org = org
        self.orgName = orgName
        self.originator = originator
        self.phoneNumber = phoneNumber
        self.sandbox = sandbox
        self.sourceSolution = sourceSolution
        self.team = team
        self.templateUpgradeAvailable = templateUpgradeAvailable
        self.updatedAt = updatedAt
        self.user = user
    }

    private enum CodingKeys: String, CodingKey {
        case acl
        case app
        case createdAt = "created_at"
        case defaultModel = "default_model"
        case description
        case email
        case id
        case identity
        case lastAppliedTemplateConfig = "last_applied_template_config"
        case lookupKey = "lookup_key"
        case metadata
        case name
        case org
        case orgName = "org_name"
        case originator
        case phoneNumber = "phone_number"
        case sandbox
        case sourceSolution = "source_solution"
        case team
        case templateUpgradeAvailable = "template_upgrade_available"
        case updatedAt = "updated_at"
        case user
    }
}

public struct UserThreadListResponseDataItemSettings: Codable, Sendable {
    /// Whether the AI agent is active for this thread. `true` enables AI responses; `false` disables them. Defaults to `true` when settings have not been explicitly configured. `null` when a client explicitly cleared the setting.
    public var agentEnabled: Bool??

    public init(agentEnabled: Bool?? = nil) {
        self.agentEnabled = agentEnabled
    }

    private enum CodingKeys: String, CodingKey {
        case agentEnabled = "agent_enabled"
    }
}

public struct UserThreadListResponseDataItem: Codable, Sendable {
    /// ID of the agent that owns this thread (`agt_...`). `null` for user-owned or team-owned threads.
    public var agentUser: String??
    /// When the thread was created (ISO 8601).
    public var createdAt: String?
    /// User who created this thread. Returns a user ID (`usr_...`) by default, or an expanded user object when the association is loaded. `null` if the creator is unknown.
    public var creator: JSONValue??
    /// Optional description or purpose statement for the thread. `null` if not set.
    public var description: String??
    /// Thread ID (`thr_...`).
    public var id: String
    /// Whether this thread operates as a channel a multi-member broadcast-style conversation.
    public var isChannel: Bool?
    /// Whether this is the default thread for its owner. Each user or team has at most one default thread.
    public var isDefault: Bool?
    /// Whether this thread is ephemeral and may be deleted automatically after a period of inactivity or when its TTL expires.
    public var isTransient: Bool?
    /// Whether this thread is hidden from public discovery. Unlisted threads are accessible only to direct participants.
    public var isUnlisted: Bool?
    /// Application-defined stable key that uniquely identifies the thread within its scope. Useful for idempotent creation. `null` if not set.
    public var key: String??
    /// Thread subtype: `"standard"` for ordinary threads, `"personal"` for a user-and-owned-agents roster, `"slack_mirror"` for the membership-strict mirror of a Slack channel, or `"slashwork_mirror"` for the membership-strict mirror of a Slashwork group. `personal` is an explicit user-thread creation option; mirror kinds are server-derived.
    public var kind: String??
    /// When the most recent message was posted in this thread, falling back to the thread's creation time if it has no messages. Always populated on thread list endpoints (which order by it, after default threads); `null` on endpoints that don't compute activity enrichment.
    public var lastActivity: String??
    /// Single-line snippet of the most recent message's text content (first non-empty line, truncated to 140 characters). Populated on thread list endpoints alongside `last_activity`; `null` when the thread has no messages, the latest message has no text content (e.g. attachment-only), or the endpoint doesn't compute activity enrichment.
    public var lastMessagePreview: String??
    /// Display name of the sender of the most recent message the same message `last_message_preview` snippets. Populated on thread list endpoints; `null` when the thread has no messages or the endpoint doesn't compute activity enrichment.
    public var lastMessageSender: String??
    /// Arbitrary key-value metadata attached to the thread. Shape is application-defined; `null` if no metadata has been set.
    public var metadata: [String: JSONValue]??
    /// Whether the authenticated user has muted notifications for this thread. `true` suppresses all notification delivery.
    public var muted: Bool?
    /// ID of the organization this thread belongs to (`org_...`). `null` for threads outside an org context.
    public var org: String??
    /// The message that spawned this thread as a sub-thread. `null` for top-level threads.
    public var parentMessage: UserThreadListResponseDataItemParentMessage??
    /// Array of participant user IDs (`usr_...`) who are members of this thread.
    public var participant: [String]?
    /// Expanded participant user objects for each member of this thread. Populated only when the association is loaded.
    public var participants: [UserThreadListResponseDataItemParticipantsItem]?
    /// Composite actor identifiers for all participants currently active in this thread. Present only when actor enrichment is requested.
    public var participatingActor: [String]?
    /// Expanded agent objects for all agents participating in this thread. Present only when agent enrichment is requested.
    public var participatingAgents: [UserThreadListResponseDataItemParticipatingAgentsItem]??
    /// The authenticated user's membership role in this thread, e.g. `"owner"`, `"member"`, or `"viewer"`. `null` if the user is not a member.
    public var role: String??
    /// ID of the developer sandbox this thread is scoped to (`dsb_...`). `null` for production threads.
    public var sandbox: String??
    /// Per-thread configuration settings controlling AI agent behavior for this thread.
    public var settings: UserThreadListResponseDataItemSettings?
    /// URL-safe slug for the thread, used in human-readable permalinks. `null` if not assigned.
    public var slug: String??
    /// Threads that are nested under this thread as replies to a parent message. Present only when sub-thread enrichment is requested.
    public var subThreads: [[String: JSONValue]]??
    /// Status tags on the thread (e.g. `"blocked"`, `"needs-review"`). Edited by any thread participant via the `/threads/:thread/tags` endpoints and filterable on the thread list endpoints. Empty array if none set.
    public var tags: [String]?
    /// ID of the team that owns this thread (`team_...`). `null` for user-owned or agent-owned threads.
    public var team: String??
    /// Human-readable name of the thread. `null` if no title has been set.
    public var title: String??
    /// Offset-free expiry timestamp after which the thread may be automatically cleaned up. `null` if the thread does not expire.
    public var ttl: String??
    /// Number of messages in this thread that the authenticated user has not yet read. Present only when read-state enrichment is requested.
    public var unreadCount: Int??
    /// When the thread was last modified (ISO 8601).
    public var updatedAt: String?
    /// ID of the user who owns this thread (`usr_...`). `null` for team-owned or agent-owned threads.
    public var user: String??
    /// Who can read the thread: `team` for every owning-team member, `restricted` for team-readable threads with an explicit roster, or `private` for roster-only access.
    public var visibility: String

    public init(
        agentUser: String?? = nil,
        createdAt: String? = nil,
        creator: JSONValue?? = nil,
        description: String?? = nil,
        id: String,
        isChannel: Bool? = nil,
        isDefault: Bool? = nil,
        isTransient: Bool? = nil,
        isUnlisted: Bool? = nil,
        key: String?? = nil,
        kind: String?? = nil,
        lastActivity: String?? = nil,
        lastMessagePreview: String?? = nil,
        lastMessageSender: String?? = nil,
        metadata: [String: JSONValue]?? = nil,
        muted: Bool? = nil,
        org: String?? = nil,
        parentMessage: UserThreadListResponseDataItemParentMessage?? = nil,
        participant: [String]? = nil,
        participants: [UserThreadListResponseDataItemParticipantsItem]? = nil,
        participatingActor: [String]? = nil,
        participatingAgents: [UserThreadListResponseDataItemParticipatingAgentsItem]?? = nil,
        role: String?? = nil,
        sandbox: String?? = nil,
        settings: UserThreadListResponseDataItemSettings? = nil,
        slug: String?? = nil,
        subThreads: [[String: JSONValue]]?? = nil,
        tags: [String]? = nil,
        team: String?? = nil,
        title: String?? = nil,
        ttl: String?? = nil,
        unreadCount: Int?? = nil,
        updatedAt: String? = nil,
        user: String?? = nil,
        visibility: String
    ) {
        self.agentUser = agentUser
        self.createdAt = createdAt
        self.creator = creator
        self.description = description
        self.id = id
        self.isChannel = isChannel
        self.isDefault = isDefault
        self.isTransient = isTransient
        self.isUnlisted = isUnlisted
        self.key = key
        self.kind = kind
        self.lastActivity = lastActivity
        self.lastMessagePreview = lastMessagePreview
        self.lastMessageSender = lastMessageSender
        self.metadata = metadata
        self.muted = muted
        self.org = org
        self.parentMessage = parentMessage
        self.participant = participant
        self.participants = participants
        self.participatingActor = participatingActor
        self.participatingAgents = participatingAgents
        self.role = role
        self.sandbox = sandbox
        self.settings = settings
        self.slug = slug
        self.subThreads = subThreads
        self.tags = tags
        self.team = team
        self.title = title
        self.ttl = ttl
        self.unreadCount = unreadCount
        self.updatedAt = updatedAt
        self.user = user
        self.visibility = visibility
    }

    private enum CodingKeys: String, CodingKey {
        case agentUser = "agent_user"
        case createdAt = "created_at"
        case creator
        case description
        case id
        case isChannel = "is_channel"
        case isDefault = "is_default"
        case isTransient = "is_transient"
        case isUnlisted = "is_unlisted"
        case key
        case kind
        case lastActivity = "last_activity"
        case lastMessagePreview = "last_message_preview"
        case lastMessageSender = "last_message_sender"
        case metadata
        case muted
        case org
        case parentMessage = "parent_message"
        case participant
        case participants
        case participatingActor = "participating_actor"
        case participatingAgents = "participating_agents"
        case role
        case sandbox
        case settings
        case slug
        case subThreads = "sub_threads"
        case tags
        case team
        case title
        case ttl
        case unreadCount = "unread_count"
        case updatedAt = "updated_at"
        case user
        case visibility
    }
}

/// Successful response
public struct UserThreadListResponse: Codable, Sendable {
    /// Array of thread objects matching the requested filters and agent narrowings.
    public var data: [UserThreadListResponseDataItem]

    public init(data: [UserThreadListResponseDataItem]) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data
    }
}

public struct TokenListResponseDataItem: Codable, Sendable {
    /// When this token was created (ISO 8601).
    public var createdAt: Date?
    /// Agent user that minted this token (`usr_...`). `null` unless an agent minted it.
    public var createdByAgentUser: String??
    /// Developer account that minted this token (`dva_...`). `null` unless minted with a developer token.
    public var createdByDeveloper: String??
    /// Org of the principal that minted this token (`org_...`). `null` on legacy rows.
    public var createdByOrg: String??
    /// Team that minted this token (`tem_...`). `null` unless minted as a team.
    public var createdByTeam: String??
    /// User who minted this token (`usr_...`). Distinct from the token subject. `null` on legacy rows.
    public var createdByUser: String??
    /// When the token expires. `null` on legacy rows that predate stored expiry.
    public var expiresAt: Date??
    /// Token ID (`sat_...`).
    public var id: String
    /// When this token was last used to authenticate a request. `null` if the token has never been used.
    public var lastUsedAt: Date??
    /// Human-readable label assigned to this token at creation time. `null` when no label was supplied.
    public var name: String??
    /// When this token was revoked. `null` if the token is still active.
    public var revokedAt: Date??
    /// Space-separated OAuth scopes stamped on the token. `null` on legacy rows; treat as `full_access`.
    public var scopes: String??
    /// Raw bearer token string. Present only in the response to the create request; never returned again after that.
    public var token: String?

    public init(
        createdAt: Date? = nil,
        createdByAgentUser: String?? = nil,
        createdByDeveloper: String?? = nil,
        createdByOrg: String?? = nil,
        createdByTeam: String?? = nil,
        createdByUser: String?? = nil,
        expiresAt: Date?? = nil,
        id: String,
        lastUsedAt: Date?? = nil,
        name: String?? = nil,
        revokedAt: Date?? = nil,
        scopes: String?? = nil,
        token: String? = nil
    ) {
        self.createdAt = createdAt
        self.createdByAgentUser = createdByAgentUser
        self.createdByDeveloper = createdByDeveloper
        self.createdByOrg = createdByOrg
        self.createdByTeam = createdByTeam
        self.createdByUser = createdByUser
        self.expiresAt = expiresAt
        self.id = id
        self.lastUsedAt = lastUsedAt
        self.name = name
        self.revokedAt = revokedAt
        self.scopes = scopes
        self.token = token
    }

    private enum CodingKeys: String, CodingKey {
        case createdAt = "created_at"
        case createdByAgentUser = "created_by_agent_user"
        case createdByDeveloper = "created_by_developer"
        case createdByOrg = "created_by_org"
        case createdByTeam = "created_by_team"
        case createdByUser = "created_by_user"
        case expiresAt = "expires_at"
        case id
        case lastUsedAt = "last_used_at"
        case name
        case revokedAt = "revoked_at"
        case scopes
        case token
    }
}

/// Successful response
public struct TokenListResponse: Codable, Sendable {
    /// Array of access token objects. Raw JWT values are not included.
    public var data: [TokenListResponseDataItem]

    public init(data: [TokenListResponseDataItem]) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data
    }
}

public struct UserArtifactsResponseDataItemImageSource: Codable, Sendable {
    /// ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.
    public var file: String??
    /// Height of the image in pixels. `null` if not known.
    public var height: Int??
    /// ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.
    public var media: String??
    /// MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.
    public var mimeType: String??
    /// Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.
    public var refreshUrl: String??
    /// Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.
    public var url: String??
    /// Width of the image in pixels. `null` if not known.
    public var width: Int??

    public init(
        file: String?? = nil,
        height: Int?? = nil,
        media: String?? = nil,
        mimeType: String?? = nil,
        refreshUrl: String?? = nil,
        url: String?? = nil,
        width: Int?? = nil
    ) {
        self.file = file
        self.height = height
        self.media = media
        self.mimeType = mimeType
        self.refreshUrl = refreshUrl
        self.url = url
        self.width = width
    }

    private enum CodingKeys: String, CodingKey {
        case file
        case height
        case media
        case mimeType = "mime_type"
        case refreshUrl = "refresh_url"
        case url
        case width
    }
}

public struct UserArtifactsResponseDataItem: Codable, Sendable {
    /// ID of the agent that produced this artifact (`agt_...`). `null` if not agent-produced.
    public var agent: String?
    /// MIME type of the current version's file, e.g. `"text/csv"` or `"image/png"`. `null` if no file is attached.
    public var contentType: String?
    /// When the artifact was first created (ISO 8601).
    public var createdAt: Date?
    /// ID of the current (latest published) artifact version (`artv_...`). `null` if no version has been published.
    public var currentVersion: String?
    /// Optional longer description of the artifact's contents or purpose. `null` if not set.
    public var description: String?
    /// Storage file ID for the current version (`fil_...`). `null` if no file is attached.
    public var file: String?
    /// Original filename of the current version's file, e.g. `"output.csv"`. `null` if no file is attached.
    public var fileName: String?
    /// Short-lived signed URL for downloading the current version's file. `null` if no file is attached.
    public var fileUrl: String?
    /// Artifact ID (`art_...`).
    public var id: String
    /// Image source metadata for rendering the current version's file inline. Present only when `content_type` starts with `"image/"`. `null` otherwise.
    public var imageSource: UserArtifactsResponseDataItemImageSource?
    /// Human-readable name for the artifact, e.g. `"Q2 Report"`. `null` if not set.
    public var name: String?
    /// ID of the organization this artifact belongs to (`org_...`).
    public var org: String?
    /// Identifier of the sandbox environment associated with this artifact. `null` if not sandbox-scoped.
    public var sandbox: String?
    /// ID of the team that owns this artifact (`tea_...`). `null` if not team-scoped.
    public var team: String?
    /// ID of the thread in which this artifact was created (`thr_...`). `null` if not thread-scoped.
    public var thread: String?
    /// When the artifact record was last modified (ISO 8601).
    public var updatedAt: Date?
    /// ID of the user who created this artifact (`usr_...`). `null` if not user-scoped.
    public var user: String?
    /// Current version number of the artifact. Increments each time a new version is published.
    public var version: Int?

    public init(
        agent: String? = nil,
        contentType: String? = nil,
        createdAt: Date? = nil,
        currentVersion: String? = nil,
        description: String? = nil,
        file: String? = nil,
        fileName: String? = nil,
        fileUrl: String? = nil,
        id: String,
        imageSource: UserArtifactsResponseDataItemImageSource? = nil,
        name: String? = nil,
        org: String? = nil,
        sandbox: String? = nil,
        team: String? = nil,
        thread: String? = nil,
        updatedAt: Date? = nil,
        user: String? = nil,
        version: Int? = nil
    ) {
        self.agent = agent
        self.contentType = contentType
        self.createdAt = createdAt
        self.currentVersion = currentVersion
        self.description = description
        self.file = file
        self.fileName = fileName
        self.fileUrl = fileUrl
        self.id = id
        self.imageSource = imageSource
        self.name = name
        self.org = org
        self.sandbox = sandbox
        self.team = team
        self.thread = thread
        self.updatedAt = updatedAt
        self.user = user
        self.version = version
    }

    private enum CodingKeys: String, CodingKey {
        case agent
        case contentType = "content_type"
        case createdAt = "created_at"
        case currentVersion = "current_version"
        case description
        case file
        case fileName = "file_name"
        case fileUrl = "file_url"
        case id
        case imageSource = "image_source"
        case name
        case org
        case sandbox
        case team
        case thread
        case updatedAt = "updated_at"
        case user
        case version
    }
}

/// Successful response
public struct UserArtifactsResponse: Codable, Sendable {
    /// Array of artifact objects belonging to the user.
    public var data: [UserArtifactsResponseDataItem]

    public init(data: [UserArtifactsResponseDataItem]) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data
    }
}

public struct UserOrgsResponseDataItem: Codable, Sendable {
    /// When this organization was created (ISO 8601).
    public var createdAt: Date?
    /// Short human-readable description of the organization. `null` if not set.
    public var description: String?
    /// Primary domain associated with the organization, e.g. `"acme.com"`. `null` if not configured.
    public var domain: String?
    /// Organization ID (`org_...`).
    public var id: String
    /// Industry category the organization belongs to, e.g. `"fintech"` or `"healthcare"`. `null` if not set.
    public var industry: String?
    /// Display name of the organization. `null` if the org has not set a name.
    public var name: String?
    /// Lookup key (`sol-...`) of the Solution currently driving this org's customer onboarding the active onboarding solution pointer. Stamped when the org is linked into a vendor's network via an explore-install and re-stamped by every later solution-driven link, so the latest install wins. `null` for vendor-track orgs and invite-driven customers. The onboarding UI reads the referenced Solution's `metadata.onboarding` block to tailor the customer checklist.
    public var onboardingSolutionLookupKey: String?
    /// The new-user experience track this org first completed. `"vendor"` for orgs that onboarded as service providers; `"customer"` for orgs that onboarded as buyers. `null` if onboarding was not tracked.
    public var onboardingTrack: String?
    /// Catalog product IDs this organization's plan includes, e.g. `["agent-rooms"]`, `["agent-solutions"]`, `["agent-customer-management"]`. Empty when the org has no plan. Clients use this to show which products the org actually has rather than inferring from feature flags. Derived from the org's plan, so it reflects what is currently paid for.
    public var ownedProducts: [String]?
    /// ID of the sandbox environment scoped to this organization (`snd_...`). `null` for organizations in production mode.
    public var sandbox: String?
    /// URL-safe identifier for the organization, used in vanity URLs and slug-based lookups.
    public var slug: String?
    /// Current lifecycle status of the organization, e.g. `"active"` or `"suspended"`. `null` if the status has not been set.
    public var status: String?
    /// When this organization was last modified (ISO 8601).
    public var updatedAt: Date?
    /// Public website URL for the organization. `null` if not set.
    public var website: String?

    public init(
        createdAt: Date? = nil,
        description: String? = nil,
        domain: String? = nil,
        id: String,
        industry: String? = nil,
        name: String? = nil,
        onboardingSolutionLookupKey: String? = nil,
        onboardingTrack: String? = nil,
        ownedProducts: [String]? = nil,
        sandbox: String? = nil,
        slug: String? = nil,
        status: String? = nil,
        updatedAt: Date? = nil,
        website: String? = nil
    ) {
        self.createdAt = createdAt
        self.description = description
        self.domain = domain
        self.id = id
        self.industry = industry
        self.name = name
        self.onboardingSolutionLookupKey = onboardingSolutionLookupKey
        self.onboardingTrack = onboardingTrack
        self.ownedProducts = ownedProducts
        self.sandbox = sandbox
        self.slug = slug
        self.status = status
        self.updatedAt = updatedAt
        self.website = website
    }

    private enum CodingKeys: String, CodingKey {
        case createdAt = "created_at"
        case description
        case domain
        case id
        case industry
        case name
        case onboardingSolutionLookupKey = "onboarding_solution_lookup_key"
        case onboardingTrack = "onboarding_track"
        case ownedProducts = "owned_products"
        case sandbox
        case slug
        case status
        case updatedAt = "updated_at"
        case website
    }
}

/// Successful response
public struct UserOrgsResponse: Codable, Sendable {
    /// Array of organization objects the user belongs to. Contains at most one item.
    public var data: [UserOrgsResponseDataItem]

    public init(data: [UserOrgsResponseDataItem]) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data
    }
}

public final class UserTaskResource: Sendable {
    private let http: HttpClient

    public init(http: HttpClient) {
        self.http = http
    }

    /// List an owner's tasks
    ///
    /// Returns tasks owned by the specified user or team. You can narrow results using the
    /// optional filters below. By default results are returned in reverse chronological
    /// order (most recently created first); use `sort` and `order` to sort by due date or
    /// priority instead.
    /// User-authenticated callers may list their personal tasks or tasks for teams they
    /// have joined. Privileged callers provide the owner in the route; the owner's
    /// organization is implied by that principal. An explicit `org` is optional and,
    /// when set, must match the owner's organization.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) for user-scoped tasks.
    ///   - team: Team ID (`tem_...`). Only tasks belonging to this team are returned.
    ///   - org: Optional organization (`org_...`) for developer and server-to-server calls. When omitted, the org is taken from the owner principal (team, user, or agent). When set, it must match that principal's org; pass null for an owner outside an organization.
    ///   - status: Filter tasks by status. One of `"open"`, `"in_progress"`, or `"done"`. Omit to return tasks in all statuses.
    ///   - ownerUser: Filter tasks assigned to a specific user. Provide the user's public ID (`usr_...`).
    ///   - ownerAgent: Filter tasks assigned to a specific agent. Provide the agent's public ID (`agi_...`).
    ///   - priority: Filter tasks by priority, from 0 (highest) to 4 (lowest).
    ///   - tag: Return only tasks carrying this tag (matched against the canonical lowercase form).
    ///   - parent: Return only subtasks of the given task (`tsk_...`), or pass `none` to return only top-level tasks.
    ///   - sourceScope: Return only tasks whose source matches this container. Must be supplied with `source_type` and `source_id`.
    ///   - sourceType: Return only tasks whose source matches this object kind. Must be supplied with the other source filters.
    ///   - sourceId: Return only tasks whose source matches this object identity. Must be supplied with the other source filters.
    ///   - epic: Return only tasks with this exact epic label.
    ///   - search: Restrict results to tasks whose name or description contains this string.
    ///   - sort: Sort key. One of `"created"` (default most recently created first), `"due_date"` (soonest due first; tasks without a due date always sort last), or `"priority"` (most urgent first). Ties break by most recently created.
    ///   - order: Sort direction, `"asc"` or `"desc"`. Defaults to `"desc"` for `created` and `"asc"` for `due_date` and `priority`.
    ///   - dueBefore: Return only tasks with a due date strictly before this ISO 8601 datetime (`2026-08-01T00:00:00Z`) or date (`2026-08-01`, meaning midnight UTC). Tasks without a due date are excluded.
    ///   - dueAfter: Return only tasks with a due date strictly after this ISO 8601 datetime or date. Tasks without a due date are excluded.
    ///   - overdue: When `true`, return only overdue tasks: a due date before the current UTC day and a status other than `"done"`. A task due today is not overdue.
    ///   - ready: When true, return only open tasks with no unfinished blockers and no active session lease. This is a projection snapshot; claim a lease before starting work.
    ///   - limit: Maximum number of tasks to return. Capped at 100.
    ///   - afterCursor: Opaque cursor returned by the previous page.
    /// - Returns: Successful response
    public func list(user: String, team: String? = nil, org: String? = nil, status: String? = nil, ownerUser: String? = nil, ownerAgent: String? = nil, priority: Int? = nil, tag: String? = nil, parent: String? = nil, sourceScope: String? = nil, sourceType: String? = nil, sourceId: String? = nil, epic: String? = nil, search: String? = nil, sort: String? = nil, order: String? = nil, dueBefore: String? = nil, dueAfter: String? = nil, overdue: Bool? = nil, ready: Bool? = nil, limit: Int? = nil, afterCursor: String? = nil) async throws -> UserTaskListResponse {
        var query: [(String, String)] = []
        if let team = team {
            query.append(("team", team))
        }
        if let org = org {
            query.append(("org", org))
        }
        if let status = status {
            query.append(("status", status))
        }
        if let ownerUser = ownerUser {
            query.append(("owner_user", ownerUser))
        }
        if let ownerAgent = ownerAgent {
            query.append(("owner_agent", ownerAgent))
        }
        if let priority = priority {
            query.append(("priority", String(priority)))
        }
        if let tag = tag {
            query.append(("tag", tag))
        }
        if let parent = parent {
            query.append(("parent", parent))
        }
        if let sourceScope = sourceScope {
            query.append(("source_scope", sourceScope))
        }
        if let sourceType = sourceType {
            query.append(("source_type", sourceType))
        }
        if let sourceId = sourceId {
            query.append(("source_id", sourceId))
        }
        if let epic = epic {
            query.append(("epic", epic))
        }
        if let search = search {
            query.append(("search", search))
        }
        if let sort = sort {
            query.append(("sort", sort))
        }
        if let order = order {
            query.append(("order", order))
        }
        if let dueBefore = dueBefore {
            query.append(("due_before", dueBefore))
        }
        if let dueAfter = dueAfter {
            query.append(("due_after", dueAfter))
        }
        if let overdue = overdue {
            query.append(("overdue", overdue ? "true" : "false"))
        }
        if let ready = ready {
            query.append(("ready", ready ? "true" : "false"))
        }
        if let limit = limit {
            query.append(("limit", String(limit)))
        }
        if let afterCursor = afterCursor {
            query.append(("after_cursor", afterCursor))
        }
        return try await http.request("/api/v1/users/\(user)/tasks", query: query)
    }

    /// Create a task for an owner
    ///
    /// Creates a new task owned by the specified user or team and returns the full
    /// task object. User-authenticated calls are attributed to the authenticated
    /// user or agent. App-scoped developer and server-to-server callers must provide
    /// the task's explicit `org` scope and an explicit `user` or `agent` actor for
    /// team tasks; a user-owned task reuses the user in the route unless an explicit
    /// agent is supplied. Every referenced principal is validated against the app,
    /// owner, and team membership before creation.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) for user-scoped tasks.
    ///   - input: Request body.
    /// - Returns: The newly created task.
    public func create(user: String, input: UserTaskCreateInput) async throws -> Task2 {
        return try await http.request("/api/v1/users/\(user)/tasks", method: "POST", body: input)
    }

    /// List task blocker cycles
    ///
    /// Runs an on-demand diagnostic over unfinished tasks owned by the specified
    /// team or user and returns a forward cursor-paginated page of complete cyclic
    /// blocker components. Detection is bounded to owners with at most 100
    /// unfinished tasks. This endpoint is read-only: cycles do not prevent task
    /// updates, lease acquisition, or completion.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) for user-scoped tasks.
    ///   - team: Team ID (`tem_...`) owning the tasks.
    ///   - org: Optional organization context for privileged callers.
    ///   - limit: Maximum cycle components to return. Defaults to 50; maximum is 100.
    ///   - afterCursor: Opaque cursor returned by the preceding page.
    /// - Returns: Successful response
    public func blockerCycles(user: String, team: String? = nil, org: String? = nil, limit: Int? = nil, afterCursor: String? = nil) async throws -> UserTaskBlockerCyclesResponse {
        var query: [(String, String)] = []
        if let team = team {
            query.append(("team", team))
        }
        if let org = org {
            query.append(("org", org))
        }
        if let limit = limit {
            query.append(("limit", String(limit)))
        }
        if let afterCursor = afterCursor {
            query.append(("after_cursor", afterCursor))
        }
        return try await http.request("/api/v1/users/\(user)/tasks/blocker_cycles", query: query)
    }

    /// List an owner's ready tasks
    ///
    /// Returns open tasks with no unfinished blockers and no active session lease.
    /// Readiness is calculated by the server from the current task projection. It is
    /// a snapshot, not a reservation; claim a task lease before starting work.
    /// Pass `explain=true` to include every open task with a stable readiness reason.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) for user-scoped tasks.
    ///   - team: Team ID (`tem_...`) owning the tasks.
    ///   - org: Optional organization context for privileged callers.
    ///   - explain: Include blocked and actively leased open tasks with exclusion reasons.
    ///   - assignedToMe: Only include tasks assigned to the authenticated user.
    ///   - sourceScope: Only include tasks whose source matches this container. Must be supplied with `source_type` and `source_id`.
    ///   - sourceType: Only include tasks whose source matches this object kind.
    ///   - sourceId: Only include tasks whose source matches this object identity.
    ///   - epic: Only include tasks with this exact epic label.
    ///   - limit: Maximum number of readiness entries to return. Capped at 100.
    ///   - afterCursor: Opaque cursor returned by the previous page.
    /// - Returns: Successful response
    public func ready(user: String, team: String? = nil, org: String? = nil, explain: Bool? = nil, assignedToMe: Bool? = nil, sourceScope: String? = nil, sourceType: String? = nil, sourceId: String? = nil, epic: String? = nil, limit: Int? = nil, afterCursor: String? = nil) async throws -> UserTaskReadyResponse {
        var query: [(String, String)] = []
        if let team = team {
            query.append(("team", team))
        }
        if let org = org {
            query.append(("org", org))
        }
        if let explain = explain {
            query.append(("explain", explain ? "true" : "false"))
        }
        if let assignedToMe = assignedToMe {
            query.append(("assigned_to_me", assignedToMe ? "true" : "false"))
        }
        if let sourceScope = sourceScope {
            query.append(("source_scope", sourceScope))
        }
        if let sourceType = sourceType {
            query.append(("source_type", sourceType))
        }
        if let sourceId = sourceId {
            query.append(("source_id", sourceId))
        }
        if let epic = epic {
            query.append(("epic", epic))
        }
        if let limit = limit {
            query.append(("limit", String(limit)))
        }
        if let afterCursor = afterCursor {
            query.append(("after_cursor", afterCursor))
        }
        return try await http.request("/api/v1/users/\(user)/tasks/ready", query: query)
    }

    /// Search an owner's tasks
    ///
    /// Performs a full-text search over tasks owned by the specified user or team and returns
    /// matching results. Combine `q` with the optional filters to narrow the result set
    /// further. When no query is provided, the endpoint behaves like a filtered list.
    /// The `query` field in the response echoes the effective search query.
    /// User-authenticated callers may search their personal tasks or tasks for teams
    /// they have joined. Privileged callers provide the owner in the route; the owner's
    /// organization is implied by that principal. An explicit `org` is optional and,
    /// when set, must match the owner's organization.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) for user-scoped tasks.
    ///   - team: Team ID (`tem_...`). Only tasks belonging to this team are searched.
    ///   - org: Optional organization (`org_...`) for developer and server-to-server calls. When omitted, the org is taken from the owner principal (team, user, or agent). When set, it must match that principal's org; pass null for an owner outside an organization.
    ///   - q: Full-text search query matched against task names and descriptions. Takes precedence over `query` when both are provided.
    ///   - query2: Alias for `q`. Use `q` when possible; this parameter exists for compatibility.
    ///   - status: Filter results by status. One of `"open"`, `"in_progress"`, or `"done"`. Omit to include all statuses.
    ///   - ownerUser: Restrict results to tasks assigned to the user with this public ID (`usr_...`).
    ///   - ownerAgent: Restrict results to tasks assigned to the agent with this public ID (`agi_...`).
    ///   - priority: Filter results by priority, from 0 (highest) to 4 (lowest).
    ///   - tag: Return only tasks carrying this tag (matched against the canonical lowercase form).
    ///   - parent: Return only subtasks of the given task (`tsk_...`), or pass `none` to return only top-level tasks.
    ///   - sourceScope: Return only tasks whose source matches this container. Must be supplied with `source_type` and `source_id`.
    ///   - sourceType: Return only tasks whose source matches this object kind.
    ///   - sourceId: Return only tasks whose source matches this object identity.
    ///   - epic: Return only tasks with this exact epic label.
    ///   - limit: Maximum number of tasks to return. Capped at 100.
    ///   - afterCursor: Opaque cursor returned by the previous page.
    /// - Returns: Successful response
    public func search(user: String, team: String? = nil, org: String? = nil, q: String? = nil, query2: String? = nil, status: String? = nil, ownerUser: String? = nil, ownerAgent: String? = nil, priority: Int? = nil, tag: String? = nil, parent: String? = nil, sourceScope: String? = nil, sourceType: String? = nil, sourceId: String? = nil, epic: String? = nil, limit: Int? = nil, afterCursor: String? = nil) async throws -> UserTaskSearchResponse {
        var query: [(String, String)] = []
        if let team = team {
            query.append(("team", team))
        }
        if let org = org {
            query.append(("org", org))
        }
        if let q = q {
            query.append(("q", q))
        }
        if let query2 = query2 {
            query.append(("query", query2))
        }
        if let status = status {
            query.append(("status", status))
        }
        if let ownerUser = ownerUser {
            query.append(("owner_user", ownerUser))
        }
        if let ownerAgent = ownerAgent {
            query.append(("owner_agent", ownerAgent))
        }
        if let priority = priority {
            query.append(("priority", String(priority)))
        }
        if let tag = tag {
            query.append(("tag", tag))
        }
        if let parent = parent {
            query.append(("parent", parent))
        }
        if let sourceScope = sourceScope {
            query.append(("source_scope", sourceScope))
        }
        if let sourceType = sourceType {
            query.append(("source_type", sourceType))
        }
        if let sourceId = sourceId {
            query.append(("source_id", sourceId))
        }
        if let epic = epic {
            query.append(("epic", epic))
        }
        if let limit = limit {
            query.append(("limit", String(limit)))
        }
        if let afterCursor = afterCursor {
            query.append(("after_cursor", afterCursor))
        }
        return try await http.request("/api/v1/users/\(user)/tasks/search", query: query)
    }
}

public final class UserThreadResource: Sendable {
    private let http: HttpClient

    public init(http: HttpClient) {
        self.http = http
    }

    /// List threads for a user
    ///
    /// Returns all threads visible to the specified user. The authenticated caller must
    /// have access to the target user's account; a 403 is returned otherwise.
    /// Pass one or more `agent` IDs to narrow results to threads where at least one of
    /// the listed agents is also a member useful for displaying every thread a user
    /// shares with a particular agent. Pass one or more `filter` objects to narrow
    /// results by thread metadata key/value pairs. Both narrowings may be combined in
    /// a single request.
    /// Results are returned as a flat array; no cursor-based pagination is applied.
    /// Threads are ordered with default threads first, then by most recent activity
    /// (newest first), each carrying a `last_activity` timestamp.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) whose threads should be listed.
    ///   - agent: Array of agent user IDs (`usr_...`). When provided, only threads where at least one of the listed agents is also a member are returned. Omit or pass an empty array to return all threads regardless of agent membership.
    ///   - filter: Array of metadata filter objects. Each filter matches threads whose `metadata` map contains the specified key/value pair. All filters must match (logical AND). Omit to return threads regardless of metadata.
    /// - Returns: Successful response
    public func list(user: String, agent: [String]? = nil, filter: [[String: JSONValue]]? = nil) async throws -> UserThreadListResponse {
        var query: [(String, String)] = []
        if let agent = agent {
            for item in agent {
                query.append(("agent", item))
            }
        }
        if let filter = filter {
            for item in filter {
                query.append(("filter", JSONValue.object(item).queryString))
            }
        }
        return try await http.request("/api/v1/users/\(user)/threads", query: query)
    }

    /// Create a thread for a user
    ///
    /// Creates a new thread owned by the specified user. The authenticated caller must
    /// have access to the target user's account; a 403 is returned otherwise.
    /// An automatic welcome message is sent into the thread upon creation unless
    /// `skip_welcome_message` is set to `true`. The thread is immediately visible to
    /// the owning user and any members added at creation time.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) whose threads should be listed.
    ///   - input: Request body.
    /// - Returns: The newly created thread object.
    public func create(user: String, input: UserThreadCreateInput) async throws -> Thread {
        return try await http.request("/api/v1/users/\(user)/threads", method: "POST", body: input)
    }
}

public final class TokenResource: Sendable {
    private let http: HttpClient

    public init(http: HttpClient) {
        self.http = http
    }

    /// List personal access tokens
    ///
    /// Returns all access tokens associated with the authenticated user, including
    /// active and revoked tokens. Tokens are returned without their raw JWT values
    /// the plaintext JWT is only available at creation time.
    /// The caller must be the user identified by `user` and must present a
    /// first-party session (or a `full_access` access token).
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) or `me` for the authenticated user.
    /// - Returns: Successful response
    public func list(user: String) async throws -> TokenListResponse {
        return try await http.request("/api/v1/users/\(user)/tokens")
    }

    /// Create a personal access token
    ///
    /// Issues a new long-lived access token for the authenticated user. The raw
    /// JWT is returned in the `token` field of the response exactly once and
    /// cannot be retrieved again store it securely immediately after creation.
    /// `scopes` is optional. When omitted the token receives `full_access`.
    /// Known catalog scopes (for example `profile`) restrict the token through
    /// the same `ScopeGuard` used by OAuth.
    /// `expires_in_days` is optional and must be one of `7`, `30`, `60`, `90`,
    /// or `365`. When omitted the token lasts 30 days. Each user may hold at
    /// most 50 active tokens; exceeding that limit returns 429.
    /// The caller must be the user identified by `user` and must present a
    /// first-party session (or a `full_access` access token). A restricted
    /// access token cannot mint another token.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) or `me` for the authenticated user.
    ///   - input: Request body.
    /// - Returns: The newly created access token. The `token` field contains the raw JWT and is present only in this response it is not stored and cannot be retrieved later.
    public func create(user: String, input: TokenCreateInput) async throws -> SystemAccessToken {
        return try await http.request("/api/v1/users/\(user)/tokens", method: "POST", body: input)
    }

    /// Revoke a personal access token
    ///
    /// Permanently revokes the specified access token belonging to the
    /// authenticated user. Once revoked, the token is immediately rejected by
    /// all API endpoints and cannot be reinstated. The token record is retained
    /// and returned in the response with `revoked_at` populated.
    /// The caller must be the user identified by `user` and must present a
    /// first-party session (or a `full_access` access token). Returns 404 if
    /// the token does not exist or does not belong to the caller.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) or `me` for the authenticated user.
    ///   - token: Access token ID (`sat_...`). Must belong to the authenticated user.
    /// - Returns: The revoked access token. The `revoked_at` field is populated with the time of revocation.
    public func delete(user: String, token: String) async throws -> SystemAccessToken {
        return try await http.request("/api/v1/users/\(user)/tokens/\(token)", method: "DELETE")
    }
}

public final class UserResource: Sendable {
    private let http: HttpClient
    public let tasks: UserTaskResource
    public let threads: UserThreadResource
    public let tokens: TokenResource

    public init(http: HttpClient) {
        self.http = http
        self.tasks = UserTaskResource(http: http)
        self.threads = UserThreadResource(http: http)
        self.tokens = TokenResource(http: http)
    }

    /// Retrieve the current user
    ///
    /// Returns the user associated with the authenticated session or bearer
    /// token. This is the canonical way to resolve "who am I?" after
    /// authentication.
    /// The response includes the user's profile, notification settings, and
    /// profile picture, along with the app, organization, and sandbox the
    /// token is scoped to and their display names enough to establish full
    /// session context in a single call. Unauthenticated requests return 401.
    ///
    /// - Returns: The authenticated user object.
    public func me() async throws -> User {
        return try await http.request("/api/v1/users/me")
    }

    /// Retrieve a user by ID
    ///
    /// Returns the user identified by `user`. The authenticated user must share
    /// at least one team with the target user; requests for users outside any
    /// shared team are rejected with 403.
    /// A user may always retrieve their own profile with this endpoint. Use the
    /// `GET /users/me` endpoint as a convenience alias for retrieving the
    /// authenticated user without specifying an ID.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) of the user to retrieve.
    /// - Returns: The requested user object.
    public func get(user: String) async throws -> User {
        return try await http.request("/api/v1/users/\(user)")
    }

    /// List a user's artifacts
    ///
    /// Returns all artifacts owned by the specified user. Artifacts represent
    /// AI-generated or user-uploaded files associated with agent sessions,
    /// threads, or sandboxes such as images, documents, and code outputs.
    /// The authenticated user must be requesting their own artifacts or must
    /// have administrative access. Attempting to list artifacts for a user
    /// the caller is not authorized to access returns 403.
    /// Results are returned in a single page without cursor pagination. Each
    /// artifact in the response reflects the state of its current version,
    /// including a short-lived signed `file_url` for direct download.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`). The authenticated user must be this user or have access to their artifacts.
    /// - Returns: Successful response
    public func artifacts(user: String) async throws -> UserArtifactsResponse {
        return try await http.request("/api/v1/users/\(user)/artifacts")
    }

    /// Create a user invite
    ///
    /// Creates a new invite for the authenticated user. The invite can optionally be
    /// scoped to a specific thread, a persona, or carry arbitrary metadata. The
    /// caller receives the new invite object at HTTP 201.
    /// The invite key is always generated server-side (192-bit URL-safe random
    /// string) and cannot be supplied by the caller.
    /// The path `:user` must match the authenticated user. If a `thread_id` is
    /// provided, the authenticated user must have permission to invite others to that
    /// thread; team threads are not supported and return an error. Supplying a
    /// `thread_id` that does not exist or that belongs to a different user returns
    /// an error. If a key collision occurs during creation the call returns a 409
    /// conflict simply retry to generate a new key.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`). Must match the authenticated user.
    ///   - input: Request body.
    /// - Returns: The newly created invite object.
    public func invites(user: String, input: UserInvitesInput) async throws -> UserInvite {
        return try await http.request("/api/v1/users/\(user)/invites", method: "POST", body: input)
    }

    /// List organizations for a user
    ///
    /// Returns the organizations the specified user belongs to. A user can belong
    /// to at most one organization, so the `data` array contains either zero or one
    /// items.
    /// The authenticated viewer must have permission to inspect the target user.
    /// Returns an empty `data` array when the user has no organization membership.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) whose organization membership you want to retrieve.
    /// - Returns: Successful response
    public func orgs(user: String) async throws -> UserOrgsResponse {
        return try await http.request("/api/v1/users/\(user)/orgs")
    }

    /// Update the current user's profile
    ///
    /// Updates one or more profile fields for the authenticated user. All
    /// fields are optional; omit any you do not want to change.
    /// When `profile_picture` is supplied, the image is uploaded and replaces
    /// the existing picture. The previous picture is deleted after the new one
    /// is stored. Image upload failures return 422 without modifying other
    /// profile fields.
    ///
    /// - Parameters:
    ///   - user: User ID (`usr_...`) or `"me"` for the authenticated user.
    ///   - input: Request body.
    /// - Returns: The user object with updated profile fields.
    public func profile(user: String, input: UserProfileInput) async throws -> User {
        return try await http.request("/api/v1/users/\(user)/profile", method: "PUT", body: input)
    }
}
