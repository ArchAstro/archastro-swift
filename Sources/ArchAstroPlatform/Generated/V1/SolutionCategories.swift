// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: c5d483ce7690

import Foundation

public final class SolutionCategoryResource: Sendable {
    private let http: HttpClient

    public init(http: HttpClient) {
        self.http = http
    }

    /// List solution categories
    ///
    /// Returns a paginated list of solution category definitions visible to the authenticated caller.
    /// Use filters to narrow results by key, parent key, lookup key, or virtual path prefix.
    /// Results are scoped to categories accessible under the caller's viewer context. Pass `app` to
    /// restrict the listing to categories associated with a specific app scope. Omit `owners` to
    /// return categories from all ownership scopes (`"system"` and `"org"`) that the caller can see.
    /// Pagination is page-based. Use `page` and `page_size` to navigate large result sets. The
    /// response includes `total_entries`, `total_pages`, `has_next`, and `has_prev` to support
    /// pagination controls. `total_entries` reflects the count after key deduplication.
    ///
    /// - Parameters:
    ///   - page: Page number to retrieve, 1-indexed. Defaults to `1`.
    ///   - pageSize: Number of solution categories to return per page. Defaults to `25`.
    ///   - keys: Return only categories whose key exactly matches one of the provided values.
    ///   - parentKeys: Return only categories whose parent key matches one of the provided values. Pass an empty array to return root-level categories.
    ///   - lookupKey: Return only the category whose `lookup_key` exactly matches this value.
    ///   - pathPrefix: Return only categories whose `virtual_path` starts with this prefix.
    ///   - owners: Restrict results to categories owned by the specified scopes. Accepted values are `"system"` and `"org"`. Omit to include all ownership scopes visible to the caller.
    /// - Returns: Paginated list of solution category summaries matching the applied filters.
    public func list(page: Int? = nil, pageSize: Int? = nil, keys: [String]? = nil, parentKeys: [String]? = nil, lookupKey: String? = nil, pathPrefix: String? = nil, owners: [String]? = nil) async throws -> SolutionCategoryListResponse {
        var query: [(String, String)] = []
        if let page = page {
            query.append(("page", String(page)))
        }
        if let pageSize = pageSize {
            query.append(("page_size", String(pageSize)))
        }
        if let keys = keys {
            for item in keys {
                query.append(("keys", item))
            }
        }
        if let parentKeys = parentKeys {
            for item in parentKeys {
                query.append(("parent_keys", item))
            }
        }
        if let lookupKey = lookupKey {
            query.append(("lookup_key", lookupKey))
        }
        if let pathPrefix = pathPrefix {
            query.append(("path_prefix", pathPrefix))
        }
        if let owners = owners {
            for item in owners {
                query.append(("owners", item))
            }
        }
        return try await http.request("/api/v1/solution_categories", query: query)
    }
}
